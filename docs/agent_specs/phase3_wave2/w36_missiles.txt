TASK 4.2b - missiles with lock-on (weapon 3), plus the ore scanner effect. REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome; do not wait to finish everything before reporting.

Context: combat/weapons has a physical blaster and a mining beam (read docs/COMBAT.md and package/modules/combat/weapons/), inventory/mining/crafting exist (docs/INVENTORY.md, MINING.md, CRAFTING.md: the Missile Pack item (+3 missiles) is craftable but USE currently refuses "not available yet"), asteroids have health and destroying them yields ore (docs/WORLD.md), fx particles exist (docs/FX.md). There are no enemies yet: missiles are for now a way to blow up BIG asteroids and for future NPCs. Same physics rules for everyone: a missile is a real object with finite fuel and limited thrust/turn rate, no cheating homing.

RULES: read docs/WORKFLOW.md, docs/COMBAT.md, docs/PERFORMANCE.md (laptop: GPU fill-rate bound; tiny draw calls only, no per-frame allocation), docs/QUALITY.md, docs/INPUT.md, docs/CRAFTING.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh, do not run the game fullscreen or --quality=low on this desktop except with a small --frames; use --ui-click, saved games and dev flags (weapons have --auto-fire/--auto-weapon). SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

CHANGE:
1. MISSILE AMMO: an ammo counter `missiles` held by combat/weapons (ICombat::ammo(i) -1 = unlimited for the blaster/beam), starts at 0 (tunable combat.start_missiles default 0; test flag --give-missiles=N), saved in the save file (ISaveable id "combat/weapons": missiles + selected weapon). Using a Missile Pack from the game menu (gameplay/crafting: replace the "not available yet" refusal for the `missiles` effect with a call through a small service `combat::IAmmo { addMissiles(n) -> accepted; missiles(); maxMissiles(); }` in weapons_api.h; cap combat.max_missiles default 12: refuse "missile rack full" and do not consume the pack) . Edit ONLY the missiles branch of crafting.
2. MISSILE WEAPON (weapon 3, key 3, data/weapons.json entry kind "missile"): fire consumes 1 missile (refused with a log + HUD-visible state "NO MISSILES" when empty), rate of fire slow (1 per 1.5 s), NOT heat based. A missile is a pooled entity (max combat.max_missiles_alive default 8; struct-of-arrays, no allocation): position/velocity (double), inherits the ship velocity at launch plus a launch kick; limited FUEL burn time (default 8 s) and thrust (default 60 m/s^2 along its heading), turn rate limit (default 90 deg/s), lifetime, arming distance (default 30 units: does not detonate before), proximity fuse radius. GUIDANCE: if a target is locked it steers with proportional navigation (pure function: lead the target using relative position/velocity: PN law a = N * closing_speed * LOS_rate, clamp to turn rate and thrust) else it flies straight along the launch heading. Collides (swept segment vs sphere, like bolts) with asteroids, planets/moons/sun, stations; on impact or proximity fuse or when fuel and lifetime run out it EXPLODES: area damage to asteroids within a blast radius (default 60 units, damage falls off linearly with distance, max damage default 400: a radius-9 rock has ~100 HP so one missile destroys it, big rocks need more), emits combat::ProjectileHit per damaged asteroid (targetKind "asteroid") and combat::MissileExploded{position, radius}, an fx "debris" + "spark" burst plus a new short "explosion" particle preset in data/particles.json (few large soft additive particles, kept small: budget-friendly). Damage to the player ship from your own missile: none for now (no friendly-fire model yet; document).
3. LOCK-ON: a new input action `lock_target` (key T; add to config/input/default.json + docs/INPUT.md + docs/QUICKSTART.md; also `lock_clear` = key Y or a second press of T on the same target) selects the target: candidates = asteroids (alive, within combat.lock_range default 2500 units) and stations (not lockable: skip) ranked by angle from the ship's forward within a lock cone (combat.lock_cone_deg default 12) then distance; T locks the best candidate (or cycles to the next candidate if already locked), the lock is dropped when the target dies, goes out of range, or leaves a wider cone. A lock takes a short acquisition time combat.lock_time default 1.0 s (angle must stay inside the cone), state machine in pure code (idle -> acquiring -> locked -> lost). Missiles use the locked target at launch (and keep tracking it by asteroid id; asteroids are static so PN reduces to steering toward a point, fine; write the guidance generally for moving targets and test it with a moving fake target).
4. HUD/UI for the missile state through the ICombat service (extend it: lockState(), lockTargetName/distance/angle, ammo(i), missileCount); the HUD worker will display it later: do NOT edit the HUD, but add a debug log and DRAW A WORLD-SPACE LOCK MARKER yourself in the weapons pass: a small bracket/diamond drawn around the locked asteroid (camera-relative quad lines, one batched draw, shown while acquiring (blinking) and locked (solid)), and missiles drawn as small additive streaks with a bright head (same style as bolts, different colour). No full-screen layers.
5. ORE SCANNER effect (data/items.json "ore_scanner": effect hud_ore_labels): implement the flag through gameplay/inventory's saved data (a `perms` set: "ore_scanner") and a small service or the existing IInventory extension `hasPerk(id)`, so USE on the item sets it once (refuse if already set), saved/loaded; the effect itself (ore labels in the HUD/radar) is NOT implemented here (the HUD worker does it later): expose `IInventory::hasPerk("ore_scanner")` and document it. Edit only the minimal inventory/crafting lines for this.
6. Quality/tunables: rows in core/quality/quality_rules.h: combat.max_missiles_alive (low 4, medium 8, high 8, ultra 12); dev flags --give-missiles=N, --auto-lock=FRAME (locks the best candidate at that frame, for scripted scenarios); combat.enabled still switches everything off.
7. Pure logic in weapons_rules.h/missile_rules.h (no GL/SDL/engine types) with unit tests (tolerances): the lock state machine (cone, acquisition time, loss conditions, cycling), proportional navigation (hits a stationary target from an offset; a target moving at constant velocity; turn rate clamp; fuel-out coasting), missile step + arming + swept collision, blast damage falloff, ammo/rack cap rules, save round trip. Update docs/COMBAT.md (missile section, controls table with 3 / T / Y, tunables, events), docs/CRAFTING.md (missile pack now works), docs/INVENTORY.md (perks), docs/INPUT.md, docs/QUICKSTART.md, docs/QUALITY.md.

OWNERSHIP: package/modules/combat/weapons/**, data/weapons.json, data/particles.json (explosion preset only), config/input/default.json (lock_target / lock_clear / weapon_3 only), package/modules/gameplay/crafting/** (the missiles branch only), package/modules/gameplay/inventory/** (perks only), package/modules/core/quality/quality_rules.h (rows only), tests (test_weapons.cpp additions or test_missiles.cpp new, globbed), docs above. Do not edit the HUD, cockpit, ship_core, asteroids, docking or game_menu.

ACCEPTANCE: smoke + make test-san pass; saved-game scenarios with --give-missiles=3 --auto-weapon=3 --auto-lock=FRAME --auto-fire=FRAME,FRAMES (docs/COMBAT.md): a missile launched at a locked radius-9+ asteroid steering and destroying it (log positions/speed/fuel over time, AsteroidDestroyed, screenshot of the lock bracket and of the missile streak and the explosion), an unlocked missile flying straight, a missile that runs out of fuel and explodes, the no-ammo refusal, the lock cone/acquisition (logs), the Missile Pack USE adding missiles and the rack-full refusal (via the menu flags --open-menu with --ui-click), the ore scanner USE setting the perk once, save/load of missiles. CPU of 8 live missiles + lock marker per frame; laptop A/B keys (combat.max_missiles_alive 0 or combat.enabled false).
