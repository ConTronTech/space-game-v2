TASK 5.1c + 4.3b - cargo rework and easier ore collection (user playtest feedback). RULES: read docs/WORKFLOW.md and the docs named. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh, do not run the game fullscreen or --quality=low on this desktop except with a small --frames; use --ui-click, saved games and dev flags. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Performance rules (docs/PERFORMANCE.md): the target laptop is GPU fill-rate bound; tiny draw calls only, no full-screen layers, no per-frame allocation. Report per item + 3-sentence summary + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Docs: docs/INVENTORY.md, docs/MINING.md, docs/CRAFTING.md, docs/GAME_MENU.md, docs/HUD.md, docs/SAVES.md, docs/DATA.md, docs/QUESTIONS.md.

USER FEEDBACK (verbatim intent): (1) "with the ore collection system we can keep it if we make a template flashing capsule for a few seconds with the ore, or just place the ore in the player's inventory, because it is hard to collect the ore at the moment". (2) "I think we need to rework the cargo because you can get SOFT-LOCKED out of the game if you fill up the cargo with X resource, so a PER-RESOURCE cargo hold and a GENERAL cargo hold for crafted items would rework the system nicely, for example 0-100 iron, 0-50 gold, 0-10 uranium. Any of the more rare resources we are allowed to keep less of unless upgraded for bigger storage capacity, and for the total storage just SUM UP ALL THE VALUES to display as the cargo value."

CHANGE:
1. PER-RESOURCE HOLDS + GENERAL HOLD in gameplay/inventory (pure rules in inventory_rules.h, unit-tested, tolerances): every ore id has its OWN cap (new optional field "cargo_cap" in data/ores.json: iron 100, copper 80, titanium 60, cobalt 50, gold 50, platinum 10, uranium 10, crystal 10; missing = 20; the filler "rock" = 100), so filling iron can NEVER block gold, uranium or crafting. Crafted ITEMS (data/items.json) go to a GENERAL hold with its own capacity (inventory.general_capacity default 30 units; items may carry "volume", default 1). Upgrade levels (data/cargo.json, 4 levels) now multiply BOTH: add per-level fields resource_mult and general_mult (defaults 1, 2, 4, 8; keep the old capacity_mult field readable as a fallback for both) so the rare ores gain room when upgraded. add(id, amount) accepts up to the free room of THAT pool (partial accept), remove atomic, plus new queries: capacity(id) (this pool's cap incl. level), free(id), and TOTALS for the UI: totalUsed(), totalCapacity() = sum of every resource cap + the general capacity (only ores that exist in data/ores.json are counted), so the displayed cargo is "SUM of all values / SUM of all caps". Keep IInventory backwards compatible where possible (used()/capacity()/free() now mean the totals), events unchanged (CargoFull carries the id that was refused). SAVE FORMAT: bump the inventory save version; load old saves (single pool stacks + level) by putting each stack in its pool and clipping any overflow with a logged warning; never crash on bad data.
2. ANTI-SOFTLOCK: (a) crafting: ingredients come out of the resource pools (frees room) and the result needs room only in the GENERAL hold: if the general hold is full, refuse with the reason "general hold full (use or discard an item)"; (b) add a DISCARD button next to USE in the game menu CARGO tab for every stack (ore and items; discards the whole stack or 1 unit: pick one and label it, e.g. shift-click = all, or two buttons "-1" and "ALL"), via a new IInventory-level remove (no events other than InventoryChanged), so the player can always make room; (c) audit for any other state where the player cannot progress (e.g. general hold full of unusable items like the not-available-yet Missile Pack / Ore Scanner: they must be discardable), and list findings. Update the game menu CARGO tab (per-resource rows with a bar "iron 45/100", a general hold bar, the summed total "CARGO 233 / 340" at the top) and the HUD cargo line (sum of all / sum of caps, keep the colour rules: cyan, amber above 80%, red when ALL is full; a per-resource FULL is shown by the pickup logic, see 4) in ui/ship_hud (edit ONLY the cargo parts).
3. EASIER COLLECTION (gameplay/mining): new tunable mining.mode = capsule (DEFAULT) | instant | chunks (the old scattered chunks, kept as an option). capsule: a destroyed asteroid leaves ONE capsule holding all its ore (amount as now), drawn as a small FLASHING marker (pulsing size and brightness, ore colour with a white core; a tiny template quad/diamond, one batched draw for all capsules) that lives for mining.capsule_seconds (default 30) and is pulled toward the ship by a MAGNET when the ship is within mining.magnet_radius (default 120 units; the capsule accelerates toward the ship, capped speed, no need to match speed) and collected on contact within mining.scoop_radius (default 20) with NO speed restriction (mining.scoop_speed default 0 = ignore); if that ore's pool is full the capsule stays (partial pickup fills to the cap, the remainder stays in the capsule) and flashes RED until it expires, with the CargoFull event once; the flash gets faster in the last 5 s so the player sees it is about to expire. instant: ore is added straight to the inventory at the moment of destruction (partial if the pool is full: the remainder becomes a capsule) with the OreMined event and the same banner. Keep OreMined/CargoFull events, the pool size tunable (mining.max_chunks reused for capsules) and the quality rows (Low: fewer). A destroyed rock far from the ship (beyond mining.chunk_range) must not spawn an unreachable capsule: it goes straight to the inventory in instant style if in range of the beam (the beam range is 500) — simply: the capsule spawns at the rock and the magnet radius is large enough; document.
4. HUD pickup feedback stays (+N ORE banner; CARGO FULL banner now says "IRON FULL" with the ore name, rate limited per ore).
5. Tests for all pure logic (pools, caps, upgrade multipliers, totals, partial accept, overflow on load of an old save, crafting refusal reasons with a full general hold, discard, capsule lifetime/flash phase/magnet motion/collect/partial remainder/expiry, instant mode remainder). Update the existing inventory/mining/crafting/game menu/HUD tests that the API change touches (minimal). Docs: INVENTORY.md, MINING.md, CRAFTING.md, GAME_MENU.md, HUD.md, DATA.md, SAVES.md.

OWNERSHIP: package/modules/gameplay/inventory/**, package/modules/gameplay/mining/**, package/modules/gameplay/crafting/**, package/modules/ui/game_menu/**, package/modules/ui/ship_hud/** (cargo line + pickup/FULL banners ONLY; another worker edits other HUD parts: keep it tiny and tell me the exact lines), data/ores.json (cargo_cap fields), data/cargo.json, tests, docs above.

ACCEPTANCE: smoke + make test-san pass; scenarios: --give flags (add a way to give per-ore amounts, e.g. --give=iron:100,gold:50,uranium:10 already exists) filling iron to its cap then still mining/crafting gold or uranium (log), crafting with a full general hold (refusal) then discard fixes it, an old-format save loading into the new pools (craft one by hand from an old saved file), a mined rock leaving a flashing capsule that the magnet pulls in (screenshots of the capsule at two flash phases and the log of the pickup), instant mode, a full-pool red capsule; screenshots of the CARGO tab and the HUD line. Report the softlock audit.
