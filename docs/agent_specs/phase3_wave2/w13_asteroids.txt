TASK 3.6 - world/asteroids: belts and clusters, cheap enough for the LOW-END target laptop (docs/VISION.md "Target hardware": i5 M 560 2c/4t, Intel Ironlake shared-memory GPU, OpenGL 2.1 fixed function ONLY, 7.6 GB RAM, generic x86-64, 60 fps at 1280x720 target, 30 fps floor). You know ship_core (it owns the 12-ish demo rocks + their physics bodies) and world/star_system (IStarSystem, planet meshes, seeded generation, camera-relative double->float rendering).

RULES: read docs/WORKFLOW.md, docs/MODULES.md, docs/WORLD.md, docs/PHYSICS.md, docs/VISION.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST): use --ui-click=X,Y,FRAME, saved games and dev flags. Never `rm -rf` with a variable path; delete scratch files by literal path. Report per item ("N: done | needs decision") + 3-sentence summary (built / verified how / could NOT verify) + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Old game (read-only): /home/contolis/Documents/Space-Game-V2/space-game/space-game - include/world/asteroids.h: port the BEHAVIOUR (belts, clusters, sizes, ore types), not the structure.

DESIGN:
1. New module package/modules/world/asteroids/ (name "world/asteroids", optional dependency world/star_system, core/physics_world; requires core/render_engine). Pure logic in asteroid_rules.h (no GL/SDL/engine types), unit-tested with tolerances: seeded deterministic generation from world.seed: (a) belts: a torus/ring band around the sun in the shared orbital plane between two planets' orbits (from IStarSystem::bodies() orbit radii; without a star system, a fallback belt around the origin region is NOT needed: just generate nothing), (b) clusters: small groups near/around some planets and moons. Each asteroid: position (double), radius (tiers matching the ship damage tiers: <5 small, 5-30 medium, >30 big), 1 of ~4 mesh variants, spin axis + rate, ore type id (string from data/ores.json via core::IData if available, else "rock"), all stored in a compact struct-of-arrays or plain vector, NO per-frame allocation. Tunables: asteroids.enabled, asteroids.belt_count (default 1), asteroids.belt_asteroids (default 1500), asteroids.cluster_count (default 4), asteroids.cluster_asteroids (default 60), asteroids.draw_distance (default 6000), asteroids.max_drawn (default 400), asteroids.physics_radius (default 1500: only asteroids this close to the ship get physics bodies).
2. Positions are static (asteroids do not orbit; document that; the belt is a shape, not a simulation). Rendering: camera-relative (subtract camera in double first), frustum/distance culled, at most asteroids.max_drawn per frame, nearest first, 3 LOD levels of a small set of shared lumpy-icosphere meshes (built once at startup, cheap: level 0 = 20 tris, 1 = 80, 2 = 320; noise displaced, seeded) drawn with glDrawElements + client vertex arrays, one glPushMatrix per asteroid (translate, rotate by spin angle (function of time), scale). Distant asteroids beyond the LOD range are drawn as points (one glDrawArrays batch). Budget the triangles per frame (tunable asteroids.triangle_budget default 15000). Lit by GL_LIGHT0 direction toward the sun if star system exists (light as star_system does; do not break its state: restore all GL state), else a fixed light.
3. Physics: a pool of static "asteroid" bodies (core::IPhysics::addBody kind "asteroid", radius = asteroid radius x 0.9) registered only for asteroids within asteroids.physics_radius of the ship (ship position from ship::IShip, per-use get), added/removed incrementally (hysteresis: register at R, unregister at 1.2R, at most N adds/removes per fixed step); the physics world must never hold more than a few hundred asteroid bodies. Ship damage already works by the kind "asteroid" and the radius tiers in ship_core: verify a collision at an asteroid gives the right tier damage.
4. Demo rocks in ship_core: add a tunable `flight.demo_rocks` (default false) that gates the old demo rocks (drawing, physics bodies) BUT keeps the compat random loop only if needed; if the default flips, saved-game scenarios referencing rock 0 in docs (docs/WARP.md etc.) need a mention; update the docs that mention the rocks. Keep everything else in ship_core unchanged. Make sure tests still pass and smoke still passes; if any test relies on the rocks, adapt it minimally and say so.
5. The radar (cockpit) is NOT changed; mention in the report what it would need (an IAsteroids service exposing the nearest N). Do expose a tiny read-only service world::IAsteroids { count(); position(i); radius(i); ore(i); nearest(point, n, out) } in asteroids_api.h for later mining/radar (Phase 4.3).
6. Docs: docs/WORLD.md (asteroids section: generation, tunables, LOD, budget, physics pool, laptop notes), docs/MODULES.md row.

OWNERSHIP: package/modules/world/asteroids/**, package/modules/ship/ship_core/ship_core.cpp (ONLY the demo-rock gating), tests (test_asteroids.cpp new, globbed; any existing test that needs the small adaptation), docs/WORLD.md, docs/MODULES.md, docs/WARP.md/other docs only for the demo-rock mention. Do NOT touch star_system or cockpit.

ACCEPTANCE: (a) smoke + make test-san pass. (b) Saved-game screenshots (docs/WARP.md method): inside the belt (dense field of rocks with lighting), near a cluster, and the spawn point; describe. (c) Benchmark `./space_game_v2 --benchmark=5 --no-vsync` inside the belt with asteroids on/off (--disable=world/asteroids): fps, triangles drawn, drawn count, physics bodies registered; worst frame. (d) A collision scenario with a medium asteroid: log the damage and tier. (e) Report memory and generation time for the default field (est. for the laptop). (f) Delete scratch files by literal path.
