TASK 2.1c - BUG: holding a direction at low speed against something drains HP fast (user playtest). "Holding a direction while at low speeds rapidly hits the collision frames and can drain your HP quickly." Likely cause (verify first!): ship_core resolves a collision by pushing the ship out (pos = other - n*(hull + other + 0.02)) and bouncing; while the player keeps holding thrust the ship re-enters the surface every fixed step, physics reports a new 'start touching' Collided each step, and each event applies at least ship.damage_min (1 HP): 60 HP per second. RULES: read docs/WORKFLOW.md and the docs named. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh, do not run the game fullscreen or --quality=low on this desktop except with a small --frames; use --ui-click, saved games and dev flags. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Performance rules (docs/PERFORMANCE.md): the target laptop is GPU fill-rate bound; tiny draw calls only, no full-screen layers, no per-frame allocation. Report per item + 3-sentence summary + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Docs: docs/PHYSICS.md, docs/SHIP.md, docs/STATIONS.md, docs/COMBAT.md.

CHANGE:
1. REPRODUCE with a saved-game scenario (docs/WARP.md method, --auto-* or fake-joystick/--fake-* style hooks if needed, e.g. hold thrust into a rock / a planet surface / a station / a pad: find how to hold an action without desktop input: the fake stick flags --fake-joystick=sidewinder --fake-axis=... exist, or add a tiny dev flag --hold=thrust:1 in ship_core if that is the smallest way) and LOG the hits per second and the HP over time. Report the real cause.
2. FIX with the smallest correct change in the collision response, all tunable and unit-tested (pure rules in ship_rules.h): (a) SCRAPE THRESHOLD: hits below ship.damage_min_speed (default 3 m/s closing speed) do NO damage (keep the bounce/pushout so the ship cannot tunnel); (b) CONTACT COOLDOWN: damage from the same body can only apply once per ship.contact_cooldown seconds (default 0.6) even at higher speed, so a bounce/repeat cannot chain-hit; (c) RESTING CONTACT: when the ship keeps pushing into a surface at low speed, remove the velocity component into the surface after pushout (sliding along it, no bounce jitter, no repeated re-entry); keep the bounce for real impacts; (d) real impacts (high closing speed) must still hurt exactly as before (the tiers / speed formula and the minimum 1 HP above the threshold) and the sun still kills. Do NOT change warp-collision behaviour or docking (a held/docked ship ignores station hits already).
3. Tests (tolerances): threshold edges, cooldown timing across steps, resting contact velocity projection, repeated re-entry simulated for 5 s of held thrust deals ZERO damage, a 100 m/s impact deals the same damage as before. Update docs/PHYSICS.md and docs/SHIP.md (tunables, behaviour).

OWNERSHIP: package/modules/ship/ship_core/** (collision response and rules only), tests (test_ship_rules.cpp etc.), docs above. Do not touch weapons, stations, docking, asteroids or the HUD.

ACCEPTANCE: smoke + make test-san pass; the before/after log of holding thrust against a rock, a planet and a station for 5 s (HP over time: before = fast drain, after = no drain), a real 100 m/s impact still doing its old damage, the bounce still visible; state the root cause and the tunables.
