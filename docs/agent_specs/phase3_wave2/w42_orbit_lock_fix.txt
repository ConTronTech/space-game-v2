TASK 3.5d - BUG FIXES (user playtest) for the orbit lock and its guide (ship/orbit_lock, docs/ORBIT_LOCK.md; guide built in Leap 32). The user tested on a PLANET and reports: (a) "the orbit lock glitches out and needs a slightly more forgiving angle"; (b) "the trajectory was constantly in the CENTER of the planet"; (c) "when we trigger the orbit lock button THAT'S when it shows the vector line, hit it again to lock": i.e. the ring / arc / vector line only appears after pressing O once, then a second O locks. Expected: the guide (ring + bright arc ahead + markers + HUD block) is visible WHENEVER the ship is within the guide range of a body, before any key press, and O locks when aligned. REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome. RULES: read docs/WORKFLOW.md and the docs named. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh, do not run the game fullscreen or --quality=low on this desktop except with a small --frames; use saved games (docs/WARP.md method), --auto-* dev flags and --ui-click. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Performance rules (docs/PERFORMANCE.md): the target laptop is GPU fill-rate bound; tiny draw calls only, no per-frame allocation. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Docs: docs/ORBIT_LOCK.md, docs/HUD.md, docs/WORLD.md.
INVESTIGATE FIRST (report the findings in your first heartbeat): reproduce with saved games near Planet 1 and near the sun (docs/WARP.md method, --auto-orbit-lock=FRAME, log lines from the orbit module): (1) why the guide is not shown until O is pressed (check the visibility conditions: guide range = 6 body radii above the surface: for a planet of radius 400 that is 2400 units; is the body choice using the nearest SURFACE (not centre)? does the guide need the ship to have relative speed, is it hidden by a 'locked/refused' state machine, is there a frame/priority ordering problem, is the render pass gated by an event?); (2) the "trajectory through the centre" symptom: the guide plane is defined by the ship's position and its velocity relative to the body: when the ship flies mostly RADIALLY (toward the planet) the plane contains the centre and the ring is edge-on / the arc and velocity marker collapse into a line through the centre: define a better plane for that case (fall back to the ship's up/forward plane or the plane containing the ship position and the ship's RIGHT vector when the tangential component of the relative velocity is small, with hysteresis so it does not flip), and never draw a degenerate line; also make sure the tangent marker and the arc-ahead direction are consistent; (3) what "glitches out" means: watch the settle phase (orbit.settle_seconds) and the alignment test: does the lock flip between refused/accepted on consecutive frames when values hover near the tolerance (add hysteresis: once aligned stay aligned until the error exceeds tolerance x 1.25 for 0.3 s), does the settle jump when the plane fallback changes, does the ship get pulled INTO the planet (altitude band), does a moving planet (orbital velocity ~10-15 units/s) make the relative velocity noisy?
CHANGE: 1. Guide visible in range always (no key press needed); if the cause is the range, make the range generous: max(orbit.guide_range x radius, 2500 units) tunable. 2. Plane fallback for radial approach (above) with hysteresis + tests. 3. FORGIVING ALIGNMENT: defaults orbit.align_speed_tol 12% -> 25%, orbit.align_angle_tol 10 deg -> 25 deg (tunables stay; document), aligned state with hysteresis, the settle blends smoothly even from the wider error (limit the correction acceleration so it looks like a curve, not a snap; settle_seconds may scale up to 3 s for larger errors). 4. Refusal reasons stay (log + HUD) but say what to do ("turn 30 deg toward the green arc"). 5. Unit tests (tolerances): plane fallback for radial and near-radial velocity, hysteresis of the aligned state, wider tolerance edges, settle continuity when the plane changes, guide visibility conditions (pure function). Update docs/ORBIT_LOCK.md.
OWNERSHIP: package/modules/ship/orbit_lock/**, package/modules/ui/ship_hud/** (ONLY the orbit guide block), core/quality/quality_rules.h only if a row is needed, tests, docs above. Do not edit ship_core, star_system or other modules.
ACCEPTANCE: smoke + make test-san pass; saved-game screenshots near a PLANET (chase view, the ring is edge-on from the cockpit) of: the guide visible before any key press, a radial approach (ring no longer collapses through the centre), an aligned lock with a forgiving 20-degree error accepted, the settle log (radius error shrinking smoothly), and the refusal text; state the root causes of (a), (b), (c).
