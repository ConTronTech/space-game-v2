TASK 3.5 - ship/orbit_lock (Phase 3). You know ship_core, warp_drive and the star system. Add a new module ship/orbit_lock: one input action `toggle_orbit_lock` (key O) that puts the ship into a stable circular orbit around the nearest planet/moon/sun (whichever body's surface is nearest), and releases it on the second press.

RULES: read docs/WORKFLOW.md, docs/MODULES.md, docs/WARP.md (the model for a small ship module using IShip), docs/WORLD.md, docs/INPUT.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys or mouse into the desktop (no xdotool/XTEST): for menu loads use the game's --ui-click=X,Y,FRAME flag or the saved-game + --auto-* dev-flag method; never `rm -rf` with a variable path. Report per item ("N: done | needs decision") + 3-sentence summary (built / verified how / could NOT verify) + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Old game (read-only): /home/contolis/Documents/Space-Game-V2/space-game/space-game - read how its orbit lock behaved and port the BEHAVIOUR.

DESIGN (Newtonian, same rules for everyone: no cheating physics):
1. Engage: only if a body is within `orbit.engage_range` (default 3x the body's radius above its surface... tunable, pick sensible) and the ship is alive, not paused, not warping. Take the body's current position + velocity (world::IStarSystem; per-use get, optional service) and put the ship on a circular orbit at its CURRENT altitude: speed v = sqrt(mu / r) with mu a per-body tunable derived from radius (orbit.gravity_scale * radius^k - design it, document it, keep it simple and unit-tested); velocity tangential in the plane defined by the ship's current position/velocity (fallback plane if degenerate). Each fixed step the ship's position is advanced ANALYTICALLY around the moving body (body-relative angle += omega*dt), so the orbit is stable forever with no integration drift, and follows the planet as it moves. Apply through IShip::setVelocity and (if needed) a small documented addition to the IShip contract - prefer NOT changing ship_core; if position control is needed, say so in the report as a "needs decision" and propose the minimal API instead of hacking.
2. While locked: the player can still rotate; thrust input releases the lock (Newtonian: you burned) - tunable orbit.release_on_thrust true. Collision/death/respawn/warp toggle also release it. The lock is not saved (documented) or is saved cleanly - your choice, say which.
3. HUD text: emit an event ship::OrbitLockChanged{locked, bodyName} (in a new orbit_lock_api.h) so the HUD can show "ORBIT LOCKED: PLANET 1". Do NOT edit ui/ship_hud or cockpit; just the event + docs.
4. Pure rules in orbit_lock_rules.h (speed for circular orbit, angular rate, nearest-body choice, engage-range test, plane fallback, position-on-orbit) with unit tests using tolerances; add them to a test file (existing test_ship_rules.cpp or a new test_orbit_lock.cpp + Makefile TEST_SRCS entry, say so). Add `toggle_orbit_lock` to config/input/default.json (and any other shipped profiles that list actions, docs/INPUT.md).
5. Docs: new docs/ORBIT_LOCK.md, docs/MODULES.md row, ROADMAP tick left to the coordinator.

OWNERSHIP: package/modules/ship/orbit_lock/**, tests (one file), Makefile TEST_SRCS line only if a new test file is added, config/input/*.json (add the action only), docs/ORBIT_LOCK.md, docs/MODULES.md, docs/INPUT.md. Anything else: report as needs decision.

ACCEPTANCE: smoke + make test-san pass; saved-game scenario near Planet 1 (see docs/WARP.md for the method and the --auto-* flags; if you need an --auto-orbit-lock=FRAME dev flag in your module, add it like --auto-warp): engage, log the orbit radius/speed at several later frames to show it is stable (radius drift ~0) while the planet moves, then thrust releases it. Describe with log lines / screenshots. Benchmark unchanged. Delete scratch files by literal path.
