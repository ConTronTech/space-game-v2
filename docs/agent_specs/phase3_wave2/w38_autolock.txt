TASK 4.2d - missile lock: AUTODETECT the nearest target (user request: "lock should be autodetected nearest for now"). REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome.

Missiles (weapon 3) currently need T (lock_target) to lock a candidate inside a cone after an acquisition time (docs/COMBAT.md, package/modules/combat/weapons/). Wheels and pads have no spare buttons for that, so for now the lock must be AUTOMATIC: while the missile weapon is selected (and also when no lock exists at launch time), the module picks the NEAREST valid target by itself.

RULES: read docs/WORKFLOW.md, docs/COMBAT.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh, do not run the game fullscreen or --quality=low on this desktop except with a small --frames; use saved games + the combat dev flags (--give-missiles, --auto-weapon, --auto-fire, --auto-lock). SCRATCH FILES only under logs/ with literal names; never rm with a variable path: list them in the report. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Performance: the candidate scan must reuse the existing asteroid cache (no O(count) query per frame).

CHANGE:
1. New tunable combat.auto_lock (default TRUE): the lock state machine gets an automatic acquisition: every combat.auto_lock_hz (default 4) while weapon 3 is selected and there is no lock (idle) or the lock was lost, pick the NEAREST alive asteroid within combat.lock_range (2500) that is ahead of the ship inside combat.auto_lock_cone_deg (default 80 degrees half-angle: wide, because missiles turn) and start acquiring it (the normal acquisition time applies, keep the visual bracket blinking then solid). While a lock exists it is KEPT until the target dies, leaves range or leaves the wide cone (do NOT hop to a nearer rock every scan: hysteresis: only re-target if the current lock is lost). Stations, planets and the sun are never auto-targets. T / Y stay working as before (T cycles to the next candidate, Y clears; a manual clear suppresses auto-lock for combat.auto_lock_pause seconds, default 3, so Y is useful).
2. Missile launch: if the fire button is pressed with a missile and no lock exists yet, auto-pick the nearest target (skipping the acquisition time only if combat.auto_lock_instant is true, default false: normal behaviour = the missile flies unguided when there is no LOCKED target at launch, as now).
3. ICombat/HUD: nothing to change (lock state is already exposed); add a log line when auto-lock picks/loses a target.
4. Pure logic in missile_rules.h/weapons_rules.h (no GL/SDL/engine types) with unit tests (tolerances): nearest-in-cone selection (behind excluded, out of range excluded, ties), hysteresis (no hopping), pause after manual clear, the scan rate limiter, disabled when combat.auto_lock false. Update docs/COMBAT.md (lock section, tunables), docs/QUICKSTART.md if it lists T/Y.

OWNERSHIP: package/modules/combat/weapons/**, tests (test_missiles.cpp additions), docs/COMBAT.md, docs/QUICKSTART.md. Do not edit other modules.

ACCEPTANCE: smoke + make test-san pass; saved-game scenario with --give-missiles=3 --auto-weapon=3 (NO --auto-lock) near an asteroid cluster: the log shows auto-lock picking the nearest rock, acquiring, locked, the missile launched with --auto-fire destroying it; a nearer rock behind the ship is ignored; Y clears and the pause works; combat.auto_lock false restores the old manual behaviour.
