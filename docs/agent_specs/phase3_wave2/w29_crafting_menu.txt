TASK 5.3 + 5.4 + 5.5 - UI tabs, the game menu and crafting: use the ore you mine. This closes the core loop the user wants: mine asteroids (done) -> carry ore (inventory, done) -> CRAFT fuel/shield/repair items (this task) -> use them. Fuel, shield and repair are NEVER free (user decision; docking gives nothing by default): everything comes from crafting with mined ore.

RULES: read docs/WORKFLOW.md, docs/MODULES.md, docs/UI.md, docs/INPUT.md, docs/DATA.md, docs/INVENTORY.md, docs/MINING.md, docs/SHIP.md, docs/WARP.md (IWarpDrive levels), docs/SAVES.md, docs/PERFORMANCE.md (laptop: GPU fill-rate bound; menus are only drawn while open; a few glass quads are fine; no new full-screen layers when closed), docs/QUALITY.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh, do not run the game fullscreen or --quality=low on this desktop except with a small --frames; for menu scenarios use --ui-click=X,Y,FRAME[;...] and saved games (docs/WARP.md method) and dev flags. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Report per item + 3-sentence summary + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

DESIGN:
1. UI TABS WIDGET (5.3) in core/ui_handler (minimal, reusable): `tabs(labels, selected, x, y, w, h)` returning the new selected index, plus a simple `listRow(label, value, x, y, w, h, selected)` helper if useful; same glass look as buttons; text via the existing cache. Tests for any pure layout math.
2. GAME MENU (5.4): new module ui/game_menu (name "ui/game_menu"): a tabbed overlay panel (glass, centred, about 60% of the screen, scaled with the UI scale) opened with a new action `toggle_menu` (key I; add to config/input/default.json + docs/INPUT.md + docs/QUICKSTART.md) and closed with the same key or Esc (Esc must not ALSO open the pause menu on that same press: check how pause_menu handles Esc). While open: the mouse is FREE (release capture, restore it on close if it was captured), ship input is ignored by the UI (the ship keeps flying: the game does NOT pause by default; tunable menu.pause_game default false; if true use Engine::setPaused like pause_menu and unpause on close), no firing (weapons already refuse when the mouse is not captured). Other modules ADD TABS by registering them (`ui::IGameMenu { addTab(name, order, drawFn); removeTab(name); open(); close(); isOpen(); }` in game_menu_api.h, the way panels register) so later features (map, ship, stats) are drop-ins. Built-in tabs: CARGO and CRAFTING (below). Tab key for the menu tabs: mouse click on the tab labels; also Q/E or Left/Right keys cycling while open (ui_left/ui_right actions already exist).
3. CARGO TAB: the hold as a list (icon colour square from data/ores.json / items.json colour, name, count), capacity bar "used / capacity" (bar coloured by fill), the ship stats summary (hull, shield, warp fuel from ship::IShip::status()), and a USE button on usable items (below).
4. CRAFTING (5.5): new module gameplay/crafting (name "gameplay/crafting"; requires gameplay/inventory + core/data_registry). Recipes from data/recipes.json (name, result item id, ingredients {ore id: amount}; read the real file: it also has missile_pack etc.), items from data/items.json (effect keys: warp_fuel, hp, hp_full, missiles, max_hp + max_hp_cap, shield_enabled, hud_ore_labels; permanent flag). `gameplay::ICrafting { recipes(); canCraft(id); craft(id) -> bool; use(itemId) -> bool; }`, events `CraftResult{recipe, ok}` and `ItemUsed{item}`. craft(): atomic: all ingredients present (via IInventory::count/remove) AND room in the hold for the result (volume from items.json optional 'volume', default 1), else refused with a reason ("missing 3 uranium", "cargo full"); the result goes into the inventory. use(item): applies the effect through the existing services ONLY (ship::IShip::addWarpFuel(amount), heal, installShield(true), addMaxHp with cap, etc.; hp_full = heal to max; warp fuel cell/canister clamp at max and REFUSE with a message if already full: do not consume in that case; permanent items apply once: shield_generator refuses if the shield is already installed; hull_plating refuses at max_hp_cap; ore_scanner / missile_pack effects that nothing implements yet: store a flag in the inventory save ("perm" set) for hud_ore_labels and keep missiles as a counter in a small `gameplay::IInventory` extra or simply refuse with "not available yet": pick the simplest honest behaviour and document it). Keep the effect interpretation table-driven in a pure header (crafting_rules.h: recipe check/consume math, effect parsing, clamp/refuse rules) with unit tests (tolerances, edge cases: partial ingredients, exactly enough, cargo full on result, duplicate use of permanents, warp fuel at max).
   Crafting location rule (hard-game switch): tunable crafting.require_dock (default FALSE for now: craft anywhere; TRUE = only while ship::IDocking::docked()). Put the decision in docs/QUESTIONS.md as an open question for the user (append, do not rewrite): "Should crafting need a docked station (workbench) in the real game?". The CRAFTING tab shows the recipe list (name, ingredients with have/need counts coloured green/red, a CRAFT button greyed with the reason when not possible) and, if require_dock is on and not docked, a line "Dock at a station to craft".
5. ICONS: none needed (coloured squares). Sounds: use core/audio if a sound named "ui_confirm" exists (pause menu uses it), else silent.
6. SAVE: crafting keeps no state except through the inventory (permanent upgrades applied are saved: shield installed and max HP are already saved by ship_core; the ore_scanner flag goes in the inventory's saved data if you implement it; NOTHING else).
7. Tunables + docs: menu.pause_game, crafting.require_dock, ui.menu_scale? (only if needed). Docs: docs/GAME_MENU.md (new: API, adding a tab, controls), docs/CRAFTING.md (new: recipe format, effect table, refusal reasons, the hard-game switch), docs/UI.md (tabs widget), docs/MODULES.md, docs/INPUT.md, docs/QUICKSTART.md, docs/QUESTIONS.md (append).

OWNERSHIP: package/modules/core/ui_handler/** (tabs/list helpers only, additive), package/modules/ui/game_menu/** (new), package/modules/gameplay/crafting/** (new), config/input/default.json (toggle_menu only), data/recipes.json / items.json ONLY to add an optional "volume" field or fix an obvious error (say so), tests (test_crafting.cpp, test_game_menu.cpp new, globbed), docs above. Do NOT edit pause_menu, HUD, cockpit, ship_core, inventory or mining: report "needs decision" (if Esc handling requires a change in pause_menu, propose the smallest change and say so).

ACCEPTANCE: smoke + make test-san pass; saved-game scenarios: --give=iron:50,copper:20,uranium:10,titanium:10 (test cargo flag, see docs/INVENTORY.md), open the menu (a dev flag --open-menu=FRAME or an --ui-click path; add `--open-menu=FRAME[,TAB]` if needed), screenshots of the CARGO tab and the CRAFTING tab (chase and cockpit views are the same overlay), craft a warp fuel cell then use it with the tank low (log: fuel before/after; use --fake-ship flags or a saved game with low fuel), craft refusal reasons (missing ingredients, cargo full), the permanent-item refusals, click selection of tabs via --ui-click, the mouse-capture restore on close (log lines), Esc handling. Describe them.
