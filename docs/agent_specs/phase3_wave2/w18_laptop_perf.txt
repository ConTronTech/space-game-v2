TASK 3.8 - performance pass for the low-end laptop. Real numbers from the target laptop (Intel HD Graphics ILK, Mesa, OpenGL 2.1, 2c/4t i5 M 560, 1366x768; preset auto = Low) are in docs/DEVLOG.md "Leap 15": 42.7 fps average, 1% low 14.9 fps, worst frame 67 ms; target 60 fps average and a 30 fps floor. Measured by disabling parts (average fps): everything on 42.7; skybox off 52.0 (skybox ~4 ms/frame); cockpit off 58.4 (cockpit ~6.5 ms/frame); star_system+asteroids off 41.5; HUD off 41.9; ALL of skybox/star_system/asteroids/starfield/cockpit off 78.3 (the bare engine + UI + ship still costs 12.8 ms/frame); and the 1% low stays ~15 fps in EVERY variant: about 6 of 640 frames take ~65 ms (a periodic hitch that is not one of those modules). You cannot run on the laptop (the coordinator does that); optimise by reasoning + measuring on the main rig with tools you build, and the coordinator will benchmark your result on the laptop and send you the numbers for a second round.

RULES: read docs/WORKFLOW.md, docs/VISION.md (Target hardware), docs/BENCHMARK.md, docs/QUALITY.md, docs/COCKPIT.md, docs/WORLD.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST); do not ssh anywhere. Never rm -rf with a variable path. Report per item + 3-sentence summary (built / verified how / could NOT verify) + what to benchmark on the laptop. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Constraints: OpenGL 2.1 fixed function only, generic x86-64, no visual regressions on High/Ultra, do not change gameplay behaviour.

CHANGE:
1. PROFILER (do this first, small and inert unless `--profile` is passed): per-frame timing of every engine phase per module (onUpdate/onFixedUpdate/onRender/onRenderUI...) and per render-engine pass name (RenderEngine passes are named), measured with a steady clock and, for GPU-heavy passes, optionally with glFinish() around the pass ONLY under --profile (document that it perturbs timing). At the end of --benchmark (or on exit) print a table: name, average ms, worst ms, % of frame, and list every frame slower than 33 ms with its frame number and the slowest 3 modules/passes in that frame. Write it to logs/profile.txt. This is what will find the ~65 ms hitch (find out WHAT it is: GC-like allocation? logging? audio? texture upload? a save-system autosave? glGetError? swap?). Report the finding.
2. COCKPIT (~6.5 ms on the laptop): profile it and cut it: static ShipV2 geometry into display lists (glGenLists; GL 2.1 fixed function; keep custom @ tagged quads working) or vertex arrays; cache the HUD screen canvases (only redraw the live screens at a reduced rate such as 15-20 Hz into a retained display list/texture, or skip screens not visible/too small; keep text crisp); no per-frame std::string / vector allocation in the draw path; fewer GL state changes; cull. Tunable cockpit.screen_hz (default by quality preset: low 15, others 30 or 60 - add the key to the quality presets in core/quality ONLY as a minimal table row) so nothing looks different on a fast PC.
3. SKYBOX (~4 ms on the laptop, fill-rate bound): draw only the faces that intersect the view frustum (cheap angle test using the camera FOV/aspect), sort/bind minimally, disable everything unneeded (fog, lighting, depth, blending, dithering, GL_TEXTURE filters: GL_LINEAR without mipmaps is right), consider uploading with a compressed-friendly / smaller internal format (GL_RGB, maybe GL_RGB5? only if visually fine on low preset) and at Low a max_size that keeps the sky looking okay (report screenshots low vs high).
4. BASE COST (12.8 ms/frame with everything off): find it with the profiler: candidates: the vignette/UI glass drawing, SwapBuffers with vsync off, the physics world, the audio update, glClear of a large buffer, per-frame string formatting in the HUD/UI, ui_handler text rendering (SDL_ttf surface -> texture every frame? cache glyph textures/strings), the debug logging. Fix what you find (cache text textures keyed by string+size, etc.).
5. Report frame-time results on the main rig for --quality=low with each fix (fps is very high here so compare ms/frame and the profiler percentages; use `taskset -c 0` and/or a reduced resolution to get a more laptop-like CPU-bound picture if you like).
6. Tests for any pure logic you add (frustum face-visibility test, screen-rate scheduler, string-cache keying) with tolerances; docs: docs/PERFORMANCE.md (new: how to run --profile, what each fix does, how to benchmark on the laptop via package/tools/laptop_bench.sh), docs/BENCHMARK.md, docs/COCKPIT.md, docs/QUALITY.md.

OWNERSHIP: package/modules/core/benchmark/**, package/engine/** (ONLY the minimal profiling hooks: engine.cpp phase timers behind a flag), package/modules/core/render_engine/** (pass timing hook only), package/modules/ship/cockpit/**, package/modules/world/skybox/**, package/modules/core/ui_handler/** (text caching only), package/modules/core/quality/quality_rules.h (one table row + tests), tests (new test_perf.cpp, globbed, plus small edits to existing test files as needed), docs above. Anything else: report as needs decision.

ACCEPTANCE: smoke + make test-san pass; the profile table printed and pasted in the report; before/after ms per pass on the main rig at --quality=low; screenshots (cockpit view, skybox) at low showing no visible regression; a statement of what the hitch was (or the strongest candidate with evidence).
