TASK 2.6c - support the new ship model ShipV3 with a tagged thruster jet. REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome; do not wait to finish everything before reporting.

The user made a new ship in assets/models/ship/shipv3/ (ShipV3.obj/.mtl, ship.json name "ShipV3"; the assets folder is git-ignored, run package/tools/link_assets.sh in your worktree). "Nothing major changed": same idea as ShipV2 (assets/models/ship/shipv2) but a better look, objects SHIP, SCREEN, Cockpit, Thruster, and a NEW tagged material `@THRUST-JET` on the thruster (usemtl @THRUST-JET near the end of ShipV3.obj): the user marked the SPOT WHERE THE ENGINE PARTICLES COME FROM. The three screen tags are the same as V2 (@HUD-INFO, @HUD-SYSTEMS, @HUD-RADAR). Custom '@GROUP-NAME' materials are extracted by the OBJ parser as tagged quads (docs/COCKPIT.md).

RULES: read docs/WORKFLOW.md, docs/COCKPIT.md, docs/FX.md, docs/SHIP.md, docs/STATIONS.md (docking.rest_height), docs/PERFORMANCE.md (laptop is GPU fill-rate bound: no extra full-screen layers, the model is display-listed once). Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh, do not run the game fullscreen or --quality=low on this desktop except with a small --frames; use saved games (docs/WARP.md) and flags. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

CHANGE:
1. SELECT AND LOAD ShipV3: the cockpit module picks the ship by config `cockpit.ship` (default "ShipV2", from ship.json "name"). Make ShipV3 the DEFAULT (keep V2/V1 selectable by config). Check EVERYTHING that depended on V2's geometry still works with V3: the eye-at-view-origin convention, scale/units and orientation (forward -z, up +y), the cockpit view (interior, canopy glass, the three @ screens with live content: FLIGHT_DATA, SHIP_SYSTEMS, PROXIMITY_RADAR: verify the screen quads are found and drawn, their orientation and aspect are right and the text is readable), the chase view (world-space model from outside, camera distance/height defaults were tuned to V2's 11.8 m length: retune camera.chase_distance/chase_height defaults for V3's real size), the model matrix/sun lighting, the display list cost (report triangle count V2 vs V3 and the cockpit pass ms with --profile on this machine; V3 has ~2.5x the vertices: if it is heavy for the laptop add a simple LOD (e.g. skip the interior detail in chase view) or report). Screenshots cockpit and chase, V2 vs V3.
2. THRUSTER JET TAG: extract the `@THRUST-JET` tagged quad(s) into thruster emitter data: for every tagged quad compute the centre position (ship model space, eye at origin) and the outward normal (the direction the exhaust should go = away from the ship's body, normally toward the rear; verify with the model, handle both quads and triangles, more than one jet = several emitters, no tag = the old fallback constants). Put the pure geometry in a header (model_thrusters.h or in model_pose.h) with unit tests (tolerances): centre, normal orientation from vertex winding, degenerate quad, multiple quads grouped into distinct emitters (e.g. two engines left and right are far apart: cluster by distance), no tag -> empty. Expose it through the existing cockpit::IShipModel service (ship_model_api.h): `thrusters()` returns a vector/span of {position (ship frame), direction (ship frame), radius}, valid after the model loaded (optional per-use service, null-safe).
3. fx/particles USES IT (edit ONLY the exhaust origin code in package/modules/fx/particles/particles.cpp): exhaust particles spawn at each thruster emitter (position transformed to world from the ship pose: core::ITransformSource pose fwd/up/right), emitted along the jet normal, share the particle budget across emitters (fx.exhaust amount split), replacing the fx.nozzle_back/fx.nozzle_down constants (keep them as the fallback when the model has no tag or the service is absent). The exhaust already only shows in chase view. Check with a screenshot in chase view with --fx-test-thrust that the plume starts at the jet(s) and points backwards.
4. DOCKING/STATIONS: with V3 report the model's belly height below the eye (min y of the hull) and set docking.rest_height default accordingly (edit ONLY that one default in package/modules/ship/docking/docking.cpp and its docs line); verify the ship sits flat on the pad in chase view (screenshot, docs/STATIONS.md scenario).
5. Docs: docs/COCKPIT.md (ShipV3, @THRUST-JET tag: how to add jets to a model, the emitter data), docs/FX.md (exhaust from the model jets), docs/STATIONS.md if the rest height changed.

OWNERSHIP: package/modules/ship/cockpit/** (loading, model_pose/thrusters geometry, IShipModel, camera defaults for chase are in package/modules/core/camera/** : edit only the two chase defaults), package/modules/fx/particles/particles.cpp (exhaust origin only), package/modules/ship/docking/docking.cpp (rest_height default only), tests (test_cockpit.cpp / new test_thrusters.cpp, globbed), docs above. Do not edit the assets (they are the user's), ship_core, HUD, or other modules.

ACCEPTANCE: smoke + make test-san pass; screenshots: ShipV3 in cockpit view (screens readable) and chase view (with the exhaust from the jets), the docked-on-pad chase view; V2 selectable by config still works; triangle count and cockpit pass cost V2 vs V3; the jet emitters found (log their positions/normals at info level once at load).
