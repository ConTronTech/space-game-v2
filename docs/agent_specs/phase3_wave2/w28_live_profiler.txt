TASK 3.9 - live in-game profiler. The user's request: "it would be smart to add a profiler for the game so we can easily find what bottlenecks are causing issues". A flag-based profiler already exists (docs/PERFORMANCE.md: --profile, --profile=gpu, --profile-slow, logs/profile.txt; package/engine/profiler.{h,cpp}, timers in Engine::run and RenderEngine::onRender). It only works from the command line and prints at exit. The user live-tests builds on their own PC/laptop and needs to find stutters WITHOUT restarting with flags. Build the LIVE version on top of the existing code (reuse it, do not duplicate).

RULES: read docs/WORKFLOW.md, docs/PERFORMANCE.md, docs/BENCHMARK.md, docs/INPUT.md, docs/UI.md, docs/QUALITY.md, docs/PERFORMANCE.md's profiler section carefully. The overhead when the overlay is closed must be ~zero (a handful of steady_clock reads per frame at most, and no allocation): this must not cost the laptop anything measurable (laptop reference: 76-80 fps avg, ~29 fps 1% low: see docs/DEVLOG.md). Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh, do not run the game fullscreen or --quality=low on this desktop except with a small --frames. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Report per item + 3-sentence summary + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

CHANGE:
1. ALWAYS-ON LITE RECORDER (default on, tunable profiler.lite default true): a ring buffer of the last ~600 frames (fixed size, no allocation) holding, per frame: frame time (ms) and the per-module-hook / per-render-pass CPU time that the existing profiler already measures (it must record in lite mode WITHOUT glFinish; the GPU mode stays flag/toggle only). Keep the per-frame recording cheap: index by a small fixed slot per known module/pass registered at startup.
2. IN-GAME OVERLAY (key F3 = action `toggle_profiler`, add to config/input/default.json + docs/INPUT.md + docs/QUICKSTART.md): a compact panel in a corner (drawn through ui_handler like other panels, few small quads + cached text; NOT a full-screen layer) showing: fps (avg over 1 s), frame ms now / average / worst of the last 5 s, 1% low fps over the last 10 s, a tiny frame-time graph (the last ~120 frames as thin bars, bars over the 33 ms line drawn red, over 16.7 ms amber), and the top 6 consumers (module hooks and render passes, avg ms over the last second with a percentage bar), plus a line with quality preset, render size, window/fullscreen, GL renderer string, and live counters if cheap (particles alive, asteroids drawn, projectiles alive: only from services that already expose them; skip what needs edits outside your ownership). While the overlay is open, a second key F4 (`toggle_profiler_gpu`) toggles GPU attribution (glFinish around passes, like --profile=gpu) and the panel says "GPU MODE (slower, ranks passes)" in amber, so the user can find whether a pass is GPU-bound.
3. HITCH CAPTURE: whenever a frame takes more than profiler.hitch_ms (default 40 ms; not during the first 60 frames, not while paused, not on the frame right after a pause/unpause or window resize) remember it in a small bounded list (last 50) with frame number, time, ms and its three slowest parts (same as SLOW FRAMES). A key F5 (`dump_profile`) writes logs/profile_live.txt = a snapshot: header (build, quality preset, GL renderer, resolution, fullscreen, uptime), the aggregate table over the ring buffer (avg/worst/% per part), the hitch list, and the last 120 frame times. It also shows a one-line toast/banner "profile written to logs/profile_live.txt" (a small line in the overlay panel if the overlay is open, else a log line at info level and a short on-screen line via the HUD-free ui_handler if simple). The overlay also auto-notes "N hitches in the last 30 s" (count of hitches) so a stutter is visible at a glance even when the top list looks fine. The user sends logs/profile_live.txt back after a stutter: make that file self-explanatory.
4. Keep --profile / --benchmark behaviour working exactly as before (reuse the same Profiler class; the exit-time table still works; --profile still means full detail incl. slow frames; --profile=gpu unchanged). Add `--profile-overlay` (start with the overlay open) for tests/screenshots.
5. Pure logic in a header (ring buffer, rolling avg/worst/1%-low, top-N ranking, hitch detection rules, graph bar heights/colours, text formatting) with unit tests (tolerances): rolling windows across wrap-around, 1% low with few frames, hitch suppression rules (warm-up, pause, resize), top-N stability. Update test_perf.cpp (or add test_profiler_live.cpp, globbed).
6. Perf preset: none needed; document that the overlay costs X ms while open and ~0 closed (measure: report both on this machine and with taskset -c 0,1 LIBGL_ALWAYS_SOFTWARE=1 --frames small).
7. Docs: docs/PERFORMANCE.md (new section "Live profiler": keys, how to read it, how to send a capture), docs/INPUT.md, docs/QUICKSTART.md, docs/QUALITY.md if you add rows.

OWNERSHIP: package/engine/profiler.{h,cpp} and new profiler_live*.h/cpp, package/engine/engine.cpp (hooks only), package/modules/core/render_engine/** (hook only), a new module package/modules/core/profiler_overlay/** (the panel + input + dump; name "core/profiler_overlay", optional deps only, must work when ui_handler is absent by just recording), config/input/default.json (the three actions), tests, docs above. Do not edit other modules.

ACCEPTANCE: smoke + make test-san; screenshots (small --frames, settings file, --profile-overlay) of the overlay in cockpit and chase views: describe what it shows; the dump file pasted (short) in the report; overhead numbers; the F3/F4/F5 action names.
