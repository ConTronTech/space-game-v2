TASK 2.2b - warp speed rebalance (user request after playtest): "warp speed we should make it 5 km/s since in the dinky ship I can easily get to 2000 m/s in 30 s [with normal flight], or base it off upgradeability since later there's an inventory UI". You own ship/warp_drive.
RULES: read docs/WORKFLOW.md and the docs named below. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST): use --ui-click=X,Y,FRAME, saved games and dev flags. Never rm -rf with a variable path; delete scratch files by literal path. Report per item ("N: done | needs decision") + 3-sentence summary (built / verified how / could NOT verify) + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Target hardware: docs/VISION.md (i5 M 560, Intel Ironlake, OpenGL 2.1 fixed function, 60 fps at 1280x720). Docs: docs/WARP.md, docs/SHIP.md, docs/WORLD.md, docs/ROADMAP.md (Phase 5 inventory).
CHANGE: 1) warp.speed default 5000 m/s (check the normal flight top speed in ship_core and report what it is; warp must be clearly faster than anything you reach with normal thrust; if normal flight can exceed 2000 easily, say what cap/drag exists and DO NOT change ship_core's flight feel without asking). Scale the warp acceleration so reaching warp speed takes a sensible time (report seconds), keep fuel drain per second and report how far one full tank goes now. 2) Make the speed upgrade-ready: the effective warp speed = base speed x a ship stat multiplier. Add a small documented, data-driven hook: a new tunable warp.upgrade_level (0..N, default 0) with a table in data/ (e.g. data/warp_drive.json: per level speed multiplier, accel multiplier, fuel efficiency; a new file read through core::IData if present, hard-coded defaults if missing) so Phase 5 inventory/crafting can raise the level later; expose the current level/speed via ship::ShipStatus-independent means (do not change the IShip contract; a tiny ship::IWarpDrive service in warp_api.h is fine: level(), maxSpeed(), setLevel()) and save the level in the save file if a save hook is cheap. 3) Everything that assumed 2000 must follow: starfield warp streaks (world/starfield scales by speed/2000: make it relative to the drive's max speed via IWarpDrive when present), HUD/docs/tests, the collision/tunnelling check (physics sweep at 5000 m/s = 83 units per step: verify no tunnelling at a planet, the sun and a 3-unit-radius asteroid: swept test with saved-game scenarios), warp.exit_speed unchanged. 4) Unit tests for the level table (tolerances). 5) Docs incl. a table of upgrade levels (invent 4 levels: 5, 7.5, 10, 15 km/s, with rising fuel efficiency).
OWNERSHIP: package/modules/ship/warp_drive/**, package/modules/world/starfield/** (streak scaling only), data/warp_drive.json (new), tests (warp test file), docs/WARP.md, docs/SHIP.md.
ACCEPTANCE: smoke + make test-san; saved-game --auto-warp scenarios: warp at 5 km/s in open space (log speed vs time, seconds to reach it), warp into a planet and into a small asteroid (hit registered, no tunnelling), fuel: distance per tank. Delete scratch files.
