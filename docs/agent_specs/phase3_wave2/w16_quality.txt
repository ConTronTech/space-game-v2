TASK 1.7 - automatic graphics quality (user request): "no quality detection so we can run the best graphics for the PC/config in settings". Add auto-detected quality presets so the game picks the best settings for the machine, with a manual override in the Settings menu.
RULES: read docs/WORKFLOW.md and the docs named below. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST): use --ui-click=X,Y,FRAME, saved games and dev flags. Never rm -rf with a variable path; delete scratch files by literal path. Report per item ("N: done | needs decision") + 3-sentence summary (built / verified how / could NOT verify) + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Target hardware: docs/VISION.md (i5 M 560, Intel Ironlake, OpenGL 2.1 fixed function, 60 fps at 1280x720). Docs: docs/CONFIG.md, docs/MODULES.md, docs/BENCHMARK.md, docs/WORLD.md, docs/UI.md.
DESIGN: 1) New module core/quality (name "core/quality", pure logic in quality_rules.h, unit-tested): presets Low / Medium / High / Ultra (+ Auto, the default). Auto decides at startup from the hardware facts core/window already logs (GL renderer/vendor/version string, max texture size, CPU threads, RAM, display size; a software renderer such as llvmpipe/softpipe = Low; Intel integrated / Ironlake-class = Low or Medium; dedicated NVIDIA/AMD = High/Ultra; unknown = Medium). Heuristic table in code, documented, tested with sample renderer strings (including "Mesa DRI Intel(R) Ironlake Mobile", "llvmpipe", "NVIDIA GeForce RTX 2060 SUPER/PCIe/SSE2", "AMD Radeon", empty). 2) A preset is a set of DEFAULT values for existing tunables: skybox.max_size, starfield.count, world.sphere_detail, world.planet_max_lod, world.planet_triangle_budget, world.terrain_height stays, asteroids.belt_asteroids / cluster_asteroids / max_drawn / draw_distance / triangle_budget, vsync, and anything else you find that scales with quality (grep eng.config.get across modules; list what you tied in). CRITICAL: the player's/ game.json explicit values always win over the preset; the preset only replaces the built-in default. Find the smallest engine change for this (e.g. engine::Config gets a 'preset layer' consulted between game.json and the code default: package/engine/config.h; keep it tiny, backward compatible, unit-tested) and make sure the preset is applied BEFORE other modules call config.get in init (priority/ordering: window/quality init early; document how). 3) Setting quality.preset (auto|low|medium|high|ultra) in ISettings/config with a row in the pause menu Settings page ("Graphics: Auto (High)" cycling Auto/Low/Medium/High/Ultra; note that some changes apply on next launch; say so in the label, e.g. "(restart)"); the log prints the detected hardware and the chosen preset and why on startup. 4) Optional runtime safety net (only if simple and safe): if Auto and the average fps over the first ~10 s of play is below 25, log a warning suggesting a lower preset; do NOT change settings behind the player's back. 5) Add a dev flag --quality=low|medium|high|ultra|auto for testing and make the benchmark output print the active preset. 6) Docs: docs/CONFIG.md and a new docs/QUALITY.md with the table of what each preset sets.
OWNERSHIP: package/modules/core/quality/** (new), package/engine/config.h/.cpp (minimal), package/modules/core/settings/**, package/modules/ui/pause_menu/** (one row + label), package/modules/core/benchmark/** (print preset), tests (new test_quality.cpp, globbed), docs/CONFIG.md, docs/QUALITY.md, docs/MODULES.md. Do NOT edit the other modules; they just call config.get as before.
ACCEPTANCE: smoke + make test-san; run the game with --quality=low and --quality=ultra and show from the log that the tunables differ (e.g. skybox.max_size, planet_max_lod); a game.json value overriding a preset wins; describe the Settings row via a --ui-click screenshot; the detected preset on this machine (RTX 2060 SUPER) is reported. Delete scratch files.
