TASK 3.3 - world/planet_mesh: real planet surfaces with LOD, built for the LOW-END target laptop (docs/VISION.md "Target hardware": i5 M 560 2c/4t, Intel Ironlake shared-memory GPU, OpenGL 2.1 fixed function ONLY, 7.6 GB RAM, generic x86-64, aim 60 fps at 1280x720, 30 fps floor). You wrote world/star_system (placeholder UV-sphere planets); replace those with terrain meshes.

RULES: read docs/WORKFLOW.md, docs/MODULES.md, docs/WORLD.md, docs/VISION.md, docs/BENCHMARK.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST): use --ui-click=X,Y,FRAME, saved games and dev flags. Never `rm -rf` with a variable path; delete scratch files by literal path. Report per item ("N: done | needs decision") + 3-sentence summary (built / verified how / could NOT verify) + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. The old game (read-only): /home/contolis/Documents/Space-Game-V2/space-game/space-game - look for planet_mesh / terrain / biome code and port the BEHAVIOUR (icosphere, FBM noise, biome colours).

DESIGN:
1. New pure header package/modules/world/star_system/planet_mesh.h (no GL/SDL/engine types): icosphere generation by subdivision level (0..4, vertex sharing, indexed), deterministic FBM/value noise (own implementation, seeded per planet from world seed + body id), height displacement (amplitude a small fraction of the radius, tunable world.terrain_height default ~0.03), biome colouring from height + latitude (ocean/sand/grass/rock/snow; the palette per planet seeded from the body colour), per-vertex normals from the displaced mesh. Vertex format: interleaved position float3 + normal float3 + colour float3 (or separate arrays) sized for GL vertex arrays. Unit tests (tolerances, never exact float equality): vertex/triangle counts per level (icosphere: 10*4^L+2 vertices), all vertices on radius within amplitude, normals unit length, determinism (same seed = identical mesh; different seed differs), no NaN.
2. LOD: 4 levels; level chosen from the body's projected angular size / distance (pure function with hysteresis so it does not flicker at boundaries; unit-test). Budget for the laptop: cap total triangles drawn per frame (tunable world.planet_triangle_budget, default about 20000; the highest level of a planet must be affordable: level 4 = 5120 triangles... choose levels so that the near planet is good and the sum of all visible planets stays under budget). Far bodies (clamped, drawn as backdrop dots) always use the lowest level or the existing sphere.
3. Generation cost: do NOT generate every level of every planet at startup on a slow CPU. Generate lazily the first time a level is needed, at most ONE mesh build per frame (spread over frames), keep meshes in memory cached per (body, level); free levels not used for N seconds; log build times (LOG_D) and the total mesh memory. Report the measured build time for level 4 on this dev machine and estimate the laptop's (~5-10x slower: budget it).
4. Rendering in star_system.cpp: replace the placeholder sphere for planets and moons (keep sun as is) with the LOD mesh via glVertexPointer/NormalPointer/ColorPointer + glDrawElements (client vertex arrays or, if you can do it safely in GL 2.1 without extensions headaches, VBOs - report which; vertex arrays are acceptable), lit by the existing sun light, GL_COLOR_MATERIAL, all state restored. Keep the camera-relative double->float path and the far clamp untouched. The physics sphere stays the base radius (terrain height is small; say what the biggest mismatch is).
5. Optional tunables: world.terrain_height, world.planet_max_lod (0-4, default 3 on this machine; explain the default), world.planet_triangle_budget, planet_mesh.enabled (false = the old placeholder spheres).
6. Docs: docs/WORLD.md (section on planet meshes, LOD, budget, laptop notes), docs/MODULES.md if needed.

OWNERSHIP: package/modules/world/star_system/** (planet_mesh.h new, star_system.cpp, star_system_rules.h if needed), tests: package/tests/test_world.cpp or a new test_planet_mesh.cpp (globbed), docs/WORLD.md, docs/MODULES.md. Do NOT touch physics, ship_core or the cockpit.

ACCEPTANCE: (a) smoke + make test-san pass. (b) Screenshots via saved games (docs/WARP.md method): a planet from 1500 units (highest LOD: terrain, biomes, terminator), from 15000 units (a mid level), from spawn (backdrop only), and planet_mesh.enabled=false for comparison; describe them. (c) Benchmarks: `./space_game_v2 --benchmark=5 --no-vsync` at the near-planet saved position with planet_mesh on vs off; report fps and triangles drawn; no frame-time spikes from mesh building (report the worst frame from the benchmark). (d) Report memory used by meshes. (e) Delete scratch files by literal path.
