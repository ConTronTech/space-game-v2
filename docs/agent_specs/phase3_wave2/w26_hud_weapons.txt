TASK 4.2c - HUD for weapons. You wrote ui/ship_hud (hud_logic.h pure logic + ship_hud.cpp: crosshair, banners, orbit-lock and dock lines). combat/weapons is merged: read package/modules/combat/weapons/weapons_api.h (combat::ICombat: weaponCount/selected/weaponName/heat/overheated/firing/hitMarkerAge, events WeaponChanged, Overheated, ProjectileHit) and docs/COMBAT.md.

RULES: read docs/WORKFLOW.md, docs/HUD.md, docs/COMBAT.md, docs/PERFORMANCE.md (laptop is GPU fill-rate bound: no full-screen layers, a few small quads only). Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh, do not run the game fullscreen or --quality=low on this desktop except with a small --frames. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Report per item + 3-sentence summary + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

CHANGE (optional services, per-use get, the HUD must work without combat/weapons):
1. Crosshair heat: around the existing crosshair draw a thin heat ring or two short arc/bar segments that fill with the SELECTED weapon's heat (0..1), colour ramp calm cyan -> amber -> red, flashing red while overheated (locked out), dim when the weapon cannot fire; nothing extra when no combat service. Keep it small (the crosshair area only).
2. Weapon line: a small status line near the bottom/left (same style as other status lines/banners): "BLASTER  [1]  ||||||    " style or "MINING BEAM [2]" with the heat as a mini bar, the unselected weapons dim, and the key labels (fixed "1" / "2" from the actions weapon_1 / weapon_2: read the real binding names only if IInput exposes them, else fixed). Hide the whole weapon block when combat/weapons is absent.
3. Hit marker: when ICombat::hitMarkerAge() (or the ProjectileHit event, whichever is cleaner) says the player just hit something, flash four tiny tick marks around the crosshair for ~0.25 s (kill flash slightly longer if you can tell an asteroid was destroyed from world::AsteroidDestroyed: optional).
4. Banners: "BLASTER OVERHEATED" (warning colour, flashing, like LOW WARP FUEL) on combat::Overheated, and a short "WEAPON: MINING BEAM" banner on WeaponChanged. While docked/approaching (ship::IDocking::busy()) show the weapon block dimmed with "DOCKED" instead of the heat.
5. Cockpit view: the flat overlay steps back there (minimal mode), but the crosshair, banners and the weapon line still show as ORBIT LOCKED and the dock prompt already do: verify in both views with screenshots.
6. Pure logic in hud_logic.h (heat colour ramp, heat-ring segment count, flash timing, weapon line text, hit marker fade) with unit tests in the HUD test file (no exact float equality). Docs: docs/HUD.md, docs/COMBAT.md (HUD section, minimal).

OWNERSHIP: package/modules/ui/ship_hud/**, the HUD test file under package/tests/, docs/HUD.md, docs/COMBAT.md. Do not touch combat/weapons, cockpit, docking or other modules: report "needs decision".

ACCEPTANCE: smoke + make test-san; saved-game screenshots (docs/WARP.md method; --auto-fire=FRAME,FRAMES and --auto-weapon=N from docs/COMBAT.md; chase and cockpit views): heat rising on the crosshair, overheated state, weapon line with each weapon selected, hit marker frame. Describe them.
