TASK 3.7 - world/stations + ship/docking (Phase 3, last item). You know ship_core, star_system (analytic orbits, IStarSystem, physics registration with setBody+velocity), orbit_lock (position steered through IShip::setVelocity), warp_drive and the radar. Add simple orbital + planetary stations and a simple dock/undock.

USER DECISIONS (2026-09-21): keep docking SIMPLE: a CUBE and a CYLINDER are the placeholder platform until a proper model exists (the old OBJs in assets/models/stations can wait; do NOT load them). Docking is a KEY PRESS (not automatic). Docking must NOT give free refuel/shield/repair in the real game (players make fuel and shield by mining asteroids later): put any refill behind a test tunable `docking.test_refill` (default FALSE) that refills hull/shield/warp fuel while docked, documented as test-only.

RULES: read docs/WORKFLOW.md, docs/MODULES.md, docs/WORLD.md, docs/PHYSICS.md, docs/ORBIT_LOCK.md, docs/INPUT.md, docs/VISION.md (target laptop: OpenGL 2.1 fixed function, cheap). Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST): use --ui-click=X,Y,FRAME, saved games and dev flags (add --auto-dock=FRAME / --auto-undock=FRAME like --auto-warp). Never rm -rf with a variable path; delete scratch files by literal path. Report per item ("N: done | needs decision") + 3-sentence summary (built / verified how / could NOT verify) + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Old game (read-only): /home/contolis/Documents/Space-Game-V2/space-game/space-game/include/world/stations.h: port the BEHAVIOUR (1-3 stations, 50/50 orbital vs planetary, orbital stations outside the planet's moons and asteroid cluster on a tilted orbit, planetary ones at latitude within +-60 degrees, dockRadius = scale*60), not the structure.

DESIGN:
1. world/stations (new module, name "world/stations", requires world/star_system via dependencies; optional core/physics_world, core/render_engine required). Pure logic in stations_rules.h (no GL/SDL/engine types), unit-tested with tolerances: seeded generation from world.seed (1-3 stations, tunable stations.count default 2), kind orbital|planetary, parent body (a planet), scale (2 or 3 by parent radius > 600), orbital: radius outside the parent's moons + the asteroids cluster shell (read the parent's moons from IStarSystem; the cluster shell radii from asteroid generation if you can read them via world::IAsteroids or replicate the formula, say which), tilt +-10 degrees, analytic circular position (double) as a function of sim time from IStarSystem::simTime() so saves/time_scale/pausing all work, own spin; planetary: fixed lat/lon on the parent's base-radius sphere (planets do not rotate in V2: say so), lifted by a small offset above the surface, orientation = surface normal up. Expose world::IStations { count(); info(i) -> {name, kind, position (double), radius (dock zone), scale, up}; nearest(pos) } in stations_api.h, plus velocity for orbital ones.
2. Placeholder model: a cube (habitat) + a cylinder (docking hub/ring axis) built procedurally once (no assets), lit, flat-coloured (kind colours), scale by station scale (base size ~30-60 units so it reads in the dock zone). Draw camera-relative (double subtract first), distance culled, one glPushMatrix per station, restore all GL state; far stations as a small point/dot. Give each a distinct simple silhouette: orbital = cube + cylinder through it spinning slowly; planetary = cube on a cylinder pillar.
3. Physics: register each station as a static "station" body (radius ~ model half-extent) moved with setBody+velocity like the planets do (orbital), with a documented ship damage tier `ship.damage_station` (default like "other": add the key only if ship_core needs a change - prefer NOT touching ship_core: the existing 'other' tier applies; mention it).
4. ship/docking (new module, name "ship/docking"): a new input action `dock` (key G; add to config/input/default.json, docs/INPUT.md, docs/QUICKSTART.md) toggles docking. Dock only if alive, not warping, not orbit-locked (the orbit lock releases first or refuse: choose refuse with a log line), within a station's dock zone (station.radius = scale*60) and relative speed below docking.max_speed (default 15 m/s) - otherwise log a reason (too fast / too far / no station). While docked the ship is held at the dock point and moves with the station (orbital: velocity = station velocity, position steered analytically each step through IShip::setVelocity exactly like ship/orbit_lock, no ship_core change; planetary: fixed at the pad), thrust/strafe/lift input or pressing G again undocks (gently: leave with the station velocity + a small push out, tunable docking.undock_push default 3 m/s). Damage/death/respawn/warp toggle release it. Events in a new docking_api.h: ship::Docked{station name}, ship::Undocked{station name}, and a service ship::IDocking { docked(); stationName(); nearestDockable(out name, out distance, out ok, out reason) } so the HUD/radar can show "DOCK [G]" prompts later (do NOT edit HUD/cockpit here). Refill behind docking.test_refill (default false).
5. Save: docked state is not saved (documented) - the station list is derived from the seed.
6. Docs: docs/STATIONS.md (new), docs/MODULES.md rows, docs/WORLD.md pointer.

OWNERSHIP: package/modules/world/stations/** (new), package/modules/ship/docking/** (new), tests (test_stations.cpp new, globbed), config/input/default.json (add `dock` only), docs/STATIONS.md, docs/MODULES.md, docs/WORLD.md, docs/INPUT.md, docs/QUICKSTART.md. Do NOT edit star_system, ship_core, orbit_lock, HUD or cockpit: if you need a change there report "needs decision".

ACCEPTANCE: (a) smoke + make test-san pass. (b) Saved-game screenshots (docs/WARP.md method) near an orbital station and a planetary station showing the cube+cylinder model; describe them. (c) Scenarios with the --auto-* flags and debug logs: dock at low speed inside the zone (Docked event, ship follows an orbital station for several seconds: log the distance to the station staying constant), refuse when too fast, refuse when too far, undock. (d) Collision with a station registers (damage tier logged). (e) Benchmark near a station on vs off. (f) Delete scratch files by literal path.
