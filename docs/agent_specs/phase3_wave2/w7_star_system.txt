TASK 3.2 - world/star_system (Phase 3). You know the world/starfield, skybox and ship_core code. Add a seeded, deterministic star system: one sun, planets, moons, simple orbits, sun lighting, drawn cheaply for the LOW-END target laptop (docs/VISION.md "Target hardware": OpenGL 2.1 fixed function, shared-memory Intel GPU, generic x86-64).

RULES: read docs/WORKFLOW.md, docs/MODULES.md, docs/WORLD.md. Edit ONLY the files under OWNERSHIP. DO NOT COMMIT (a hook blocks it); leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. Report per item ("N: done | needs decision (what)") plus a 3-sentence summary (built / verified how / could NOT verify) and a manual test checklist. Your worktree was fast-forwarded to main; run package/tools/link_assets.sh. Old game (read-only reference): /home/contolis/Documents/Space-Game-V2/space-game/space-game - look for its star system / planet generation for behaviour (counts, orbit radii, colours), port behaviour not structure.

TARGET: package/modules/world/star_system/ (name "world/star_system"), pure logic in star_system_rules.h (no GL/SDL/engine types), a service interface in star_system_api.h: world::IStarSystem { sunPosition(), bodies() (id, name, kind sun|planet|moon, position (double), radius, parent index, orbit radius, period, colour), positionAt(id) }.

CHANGE:
1. Generation from tunable world.seed (default 1234): 1 sun, planets.count (default 6, range 2-10), 0-3 moons per planet. Orbit radii grow outward (tens of thousands of units at the outer edge, like the old game's 80,000+), radii/colours/periods drawn from seeded mt19937. Same seed => identical system (unit-test this).
2. Orbits: circular, in a shared plane with small per-planet inclination; body positions computed ANALYTICALLY from simulation time (no integration drift): pos = f(t). Tunable world.time_scale (default 1.0; sim time advances in onFixedUpdate and is paused while the engine is paused). Saveable via ISaveable (id "world/star_system": seed + sim time) so load restores positions.
3. PRECISION: store body positions as double. Render camera-relative: subtract the camera position in double, then convert to float. Bodies far away are drawn as scaled-down points/discs at a clamped distance (so a planet 60,000 units away is still visible as a dot and keeps correct angular size) - unit test the projection helper. Do NOT introduce world re-origin of other modules; document the approach in docs/WORLD.md.
4. RENDER (placeholder meshes, real LOD meshes come in 3.3): sun = emissive coloured sphere (low-poly UV sphere built once, vertex arrays, no per-frame allocation) plus a simple billboard glow; planets/moons = low-poly lit spheres (16x12 segments max) tinted by body colour. One GL_LIGHT0 directional-from-sun light while drawing bodies; restore all GL state. Render pass order after starfield/skybox, with the normal world geometry. Tunables: world.sphere_detail (default 1 = 16x12), star_system.enabled.
5. Do NOT wire collisions (that is 3.4) and do not touch physics_world. Radar wiring is out of scope, but expose IStarSystem so it can be wired later. Register nothing that changes ship behaviour. The demo rocks stay.
6. Spawn safety: the ship's default start position must not be inside/at a body; if it is, tunable world.spawn_offset... - simply verify the default spawn is well clear and report the distance to the nearest body.
7. Docs: docs/WORLD.md section (system generation, tunables, camera-relative rendering, how to add a body kind). Update docs/MODULES.md module table row if the table lists modules.

CONSTRAINTS: no globals, no printf (LOG_*), OpenGL 2.1 fixed function, generic build (no -march), no per-frame heap allocation in render, unit tests use tolerances not exact float equality. Add tests to package/tests/test_world.cpp (or test_star_system.cpp plus one Makefile TEST_SRCS entry, say so).

OWNERSHIP: package/modules/world/star_system/**, package/tests/test_world.cpp or test_star_system.cpp, Makefile TEST_SRCS line only if a new test file is added, docs/WORLD.md, docs/MODULES.md.

ACCEPTANCE: (a) smoke.sh + make test-san pass. (b) Real-game screenshots (SDL_AUDIODRIVER=dummy ./space_game_v2 --frames=60 --settings=<{} file> --screenshot=... --screenshot-frame=50): default spawn, and from a saved-game position near a planet; describe them. (c) `./space_game_v2 --benchmark=5 --no-vsync` with star_system on vs off (--disable=world/star_system): report both fps. (d) Report the seed-1234 system: body list with orbit radii and the ship spawn distance to the nearest body. (e) Delete scratch files.
