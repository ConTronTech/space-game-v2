TASK 1.9b - in-game controller setup screen (joysticks / wheels / pedals / shifters). REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome; do not wait to finish everything before reporting.

Background: the joystick input method (docs/CONTROLLERS.md, package/modules/core/input_methods/joystick/) reads devices through per-device JSON profiles in config/input/devices/. To make the first PXN V10 wheel profile we used a standalone tool `package/tools/joytest/joytest.cpp` (`./joytest --guided`): the user found its UI messy and asked to REUSE THE GAME'S OWN UI. Build the proper feature INSIDE the game: a Controllers screen using the game's UI (core/ui_handler glass panels, bars, buttons, tabs(), listRow(); look at ui/game_menu and ui/pause_menu for the style and how a panel is registered). Read joytest.cpp for the guided flow (steps, confirm/retry/back/skip, detection thresholds, the first-value problem: some wheels report a wrong first axis value, so detect a control by CHANGE from a baseline taken at the start of the step, never by absolute value) and docs/CONTROLLERS.md for the profile format (the shipped wheel profile config/input/devices/11ff_3245_pxn_v10.json is the reference for what a good generated profile looks like: pedals "rest":"min"|"max", steering with deadzone/curve/invert, button actions).

RULES: read docs/WORKFLOW.md, docs/CONTROLLERS.md, docs/UI.md, docs/GAME_MENU.md, docs/PERFORMANCE.md (laptop: GPU fill-rate bound; the screen draws only while open, a few glass quads, no per-frame allocation), docs/INPUT.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh, do not run the game fullscreen or --quality=low on this desktop except with a small --frames; use --ui-click=X,Y,FRAME[;...], saved games and the fake device flags (--fake-joystick=..., --fake-axis=..., --fake-button=...: docs/CONTROLLERS.md) since this dev PC may have no joystick. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

CHANGE:
1. New module ui/controller_setup (name "ui/controller_setup"; optional deps core/input_methods/joystick via core::IControllers, core/ui_handler required, ui/pause_menu optional). A full-panel screen (centred glass, ~80% of the screen, scales with the UI layout scale; must fit 1280x720 and 5:4/4:3 down to 800x600 and 640x480: see docs/DISPLAYS.md) with three views: (a) DEVICES: one row per connected device (name, vid:pid, axes/buttons/hats, matched profile name or "(generic: maps nothing)"), click to select; hot-plug updates live. (b) LIVE: the selected device: one bar per axis (value, a flash when it moves, the axis number), a box per button (lit while down, count of presses), the hat as a small cross; nothing else on screen (clean and spacious: reuse ui.bar()/glass()/button() so it matches the game). (c) SETUP WIZARD: the guided flow from joytest, asking one control at a time with a big prompt: steering (turn fully right), throttle, brake, clutch (optional), then the buttons the player wants for fire, dock, warp, orbit lock, menu, camera, next weapon (a wheel has ONE weapon switch: action weapon_next), previous weapon (optional), lock target (optional), lock clear (optional). Detected control shown as "GOT AXIS 6 (rests low, pressed +)": ENTER/click OK confirms, BACKSPACE/click RETRY re-asks, BACK goes to the previous step, SKIP skips optional steps. Use only ACTION names that exist in config/input/default.json (read it) and note which are missing. At the end show a summary and a SAVE button.
2. PROFILE GENERATOR (pure logic in controller_setup_rules.h, no SDL/GL/engine types, unit tests with tolerances): from the wizard results (control -> axis/button index, the baseline (first reading) and the pressed direction, min/max seen) build the profile JSON in the same format as the shipped wheel profile: pedals get "rest":"min" or "max" from the detected resting end (baseline near an end and the press moves the other way), steering/stick axes get role steering/stick with a default deadzone 0.03 and curve 1.5, invert chosen so that turning the wheel right yields yaw right (the game's yaw + = turn LEFT: check the shipped profiles for the sign convention and write it down), clutch -> lift down like the shipped profile, buttons -> actions. Write the file to config/input/devices/<vid>_<pid>_<slug>.json (creating the directory if needed, an existing profile is renamed to .bak first, never overwrite silently), with a header comment stating it was generated by the in-game setup and the date. Add a `IControllers::reloadProfiles()` (non-pure default no-op in joystick_api.h; implement it in the joystick module: re-read config/input/devices and re-match every open device without restarting) so the new profile takes effect immediately (this is the ONE small edit allowed in the joystick module: say exactly which lines).
3. SETTINGS: a "Controllers" row in the pause menu Settings page opening the screen (edit ONLY the row + its handler in pause_menu.cpp: keep it tiny, another worker may edit that file: tell me the exact lines), `--controller-setup` dev flag (opens the screen at startup, for screenshots), and on the LIVE view sliders for the user multipliers the joystick module already has (settings input.joystick_deadzone_scale 0.5..2 and input.joystick_sensitivity) with live effect. Show a TEST strip on the LIVE view: the current values of the main actions the profile produces (yaw, thrust, brake, lift, roll, pitch) as small bars, read through core::IInput (value("yaw") etc.), so the player can see what the game will actually get: this is how a wrong sign or a stuck axis becomes obvious.
4. Input capture: while the screen is open the game must not react to the controls being tested (the ship would fly): reuse the game menu's approach (mouse freed, engine paused? choose the game menu's behaviour, and note that joystick contributions to ship actions should not fly the ship while the wizard listens: simplest is to pause the game while the screen is open, like menu.pause_game true).
5. Tests: the profile generator (pedal rest detection min/max, steering invert, missing optional controls, slug/filename, JSON validity through the joystick module's parseProfile so a generated profile must parse and produce sane action values for a simulated wheel with the PXN numbers from docs/DEVICES.md: steering axis 0, throttle 2, brake 5, clutch 6, buttons), the wizard state machine (confirm/retry/back/skip/finish, detection by change from the baseline ignoring a wrong first value, ignores an already-used axis/button), layout math. Docs: docs/CONTROLLERS.md (the in-game setup replaces the joytest wizard for players; joytest stays as a standalone dev tool), docs/UI.md, docs/QUICKSTART.md.

OWNERSHIP: package/modules/ui/controller_setup/** (new), package/modules/core/input_methods/joystick/joystick_api.h + joystick.cpp (ONLY reloadProfiles), package/modules/ui/pause_menu/pause_menu.cpp (the Controllers row ONLY), tests (test_controller_setup.cpp new, globbed), docs above. Do not edit other modules or the shipped profiles.

ACCEPTANCE: smoke + make test-san pass; screenshots via --controller-setup and --fake-joystick=pxn --fake-axis=... --fake-button=... (small --frames) of the DEVICES view, the LIVE view (axes lit, test strip), the wizard with a detected candidate, and the finished summary, at 1280x720 and at 640x480; a generated profile from a scripted wizard run (fake events) written to a scratch dir and parsed back; the exact lines edited in pause_menu.cpp and joystick.cpp.
