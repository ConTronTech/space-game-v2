TASK 3.7e - BUG (user playtest): "the large pole is still on the stations when the cube is supposed to be FLAT on the surface". Planetary stations (world/stations, docs/STATIONS.md) are drawn as a cube on a cylinder PEDESTAL/pillar standing on the planet. The user wants the cube sitting FLAT ON THE SURFACE with the flattened cylinder landing pad on top of the cube (as for orbital stations), no pillar. REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome. RULES: read docs/WORKFLOW.md and the docs named. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh, do not run the game fullscreen or --quality=low on this desktop except with a small --frames; use saved games (docs/WARP.md method), --auto-* dev flags and --ui-click. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Performance rules (docs/PERFORMANCE.md): the target laptop is GPU fill-rate bound; tiny draw calls only, no per-frame allocation. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Docs: docs/STATIONS.md, docs/WORLD.md (planet meshes: package/modules/world/star_system/planet_mesh.h has the seeded terrain relief world.terrain_height ~3% of the radius; planets have no rotation).
CHANGE: 1. MODEL: remove the pedestal from the planetary station: the cube base rests directly on the surface, pad on top of the cube (same pad model as orbital). 2. GROUNDING: planet terrain is now displaced by noise (up to ~3% of the radius, ~34 units on the biggest planet) while the station used the base-radius sphere: compute the LOCAL surface radius at the station's lat/lon from the SAME pure noise/height function the planet mesh uses (planet_mesh.h: expose a small pure function if needed: local height at a unit direction for a given body id/seed/terrain_height; keep planet_mesh behaviour identical), and place the cube so its bottom face sits on that local surface (slightly embedded, e.g. 10 percent of its height, so no gap is visible on slopes), oriented to the surface normal (use the radial direction or the noise gradient; keep the pad flat: the pad is what the ship rests on so keep 'up' = the station's up used by docking; if you use a slope-aware normal say so). Update StationInfo so info().position = the cube centre at its NEW height and padTop/up stay consistent for docking (ship/docking sits the ship on padTop): verify the docked ship still rests flat on the pad (the docking scenario in docs/STATIONS.md). 3. ORBITAL stations are unchanged. The physics sphere follows the new cube centre. Radar diamond uses info().position (fine). 4. Tests (tolerances): the local height function (deterministic, within the terrain amplitude, same as the mesh vertices at a vertex direction), the grounded position, the pad top height. Docs: docs/STATIONS.md (model + grounding).
OWNERSHIP: package/modules/world/stations/**, package/modules/world/star_system/planet_mesh.h (only to expose the pure local-height function; behaviour unchanged) and its tests, package/tests/test_stations.cpp / test_planet_mesh.cpp, docs/STATIONS.md, docs/WORLD.md. Do not edit docking, ship_core, HUD or cockpit.
ACCEPTANCE: smoke + make test-san pass; saved-game screenshots of a PLANETARY station from outside at two angles (default seed has two on Planet 2, docs/STATIONS.md) showing the cube flat on the ground with the pad on top and NO pole, and the ship docked on its pad (chase view); orbital station unchanged (screenshot); state the height mismatch you saw before.
