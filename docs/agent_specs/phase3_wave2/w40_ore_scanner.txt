TASK 5.5b - the ORE SCANNER effect. REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome; do not wait to finish everything before reporting.

Background: the Ore Scanner item (data/items.json "ore_scanner", effect hud_ore_labels, permanent) can already be crafted and USED (docs/CRAFTING.md): it sets the saved perk "ore_scanner" (gameplay::IInventory::hasPerk("ore_scanner"), docs/INVENTORY.md). Nothing reads it yet. Asteroids know their ore id (world::IAsteroids::ore(i), tints from data/ores.json colours and cargo caps, docs/WORLD.md, docs/MINING.md). Design intent (user): the game is hard and strategic; WITHOUT the scanner the player only sees "ASTEROID" (size), WITH it the ore type and the expected yield are visible, so mining decisions need the scanner.

RULES: read docs/WORKFLOW.md, docs/COCKPIT.md, docs/HUD.md, docs/COMBAT.md, docs/MINING.md, docs/PERFORMANCE.md (laptop: GPU fill-rate bound; a few small quads/lines only, cached text, no per-frame allocation). Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh, do not run the game fullscreen or --quality=low on this desktop except with a small --frames; use saved games (docs/WARP.md method), --give / --auto-* dev flags, --ui-click. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

CHANGE:
1. RADAR (cockpit PROXIMITY_RADAR, package/modules/ship/cockpit/hud_screens.cpp + radar_map.h): the nearby asteroid dots (12 nearest within cockpit.radar_asteroid_range) are dim tan today. WITH the perk, colour each dot by its ore (colour from data/ores.json via core::IData, cached per ore id, fallback tan) and make a valuable/rare ore stand out slightly (size tier +1 for ores with rarity <= 8: gold, uranium, platinum, crystal); WITHOUT the perk nothing changes. Also with the perk the nearest-body label area may show "NEAREST ROCK: IRON 340" as one more small line ONLY if there is room without shrinking existing text (skip otherwise). Pure helpers with unit tests (ore -> colour/size tier, no perk -> unchanged).
2. LOCK TARGET INFO (combat/weapons + ship_hud): today the lock name is "asteroid 12 (iron)" (package/modules/combat/weapons/weapons.cpp lockName_ and the ICombat lock accessors) which leaks the ore even without a scanner. Without the perk the lock info must say only "ASTEROID  r 9.6  850 m" (radius and distance are always visible); WITH the perk "ASTEROID  IRON  r 9.6  ~41 ore  850 m" (expected yield = mining.yield_scale * radius^2 as mining does: read the tunable/function through the mining module's public header if it exposes it, else recompute with the same formula and note the coupling). Show this as one small status line under the crosshair in ship_hud ONLY while a target is acquiring/locked (a few small quads, cached string rebuilt when the target or distance bucket changes, not per frame). Keep ICombat backwards compatible (add a method like lockDescription(bool scanner) or return the raw ore id separately and let the HUD decide; your choice, documented).
3. WORLD MARKER (combat/weapons lock bracket): with the perk, tint the world-space lock bracket by the ore colour; without it keep the current colour. Tiny change in the existing batch.
4. Docs: docs/CRAFTING.md (the item now does something), docs/MINING.md (scanner section), docs/HUD.md, docs/COCKPIT.md, docs/COMBAT.md (lock info), docs/QUESTIONS.md is NOT to be edited by you.
5. Tests (tolerances): the description text with/without the perk, expected yield rounding, colour/tier mapping, the radar helper. Use the existing test files (test_cockpit.cpp, test_hud_logic.cpp, test_missiles.cpp/test_weapons.cpp) or a new test_scanner.cpp (globbed).

OWNERSHIP: package/modules/ship/cockpit/** (radar only), package/modules/combat/weapons/** (lock description + bracket tint only), package/modules/ui/ship_hud/** (the lock info line only), tests, docs above. Do not edit inventory, crafting, mining, asteroids or other modules (report "needs decision").

ACCEPTANCE: smoke + make test-san pass; saved-game screenshots (docs/WARP.md method; chase or cockpit) near an asteroid cluster with --give-missiles=3 --auto-weapon=3 (auto-lock picks the nearest rock) WITHOUT the scanner (lock line "ASTEROID r 9.6 850 m", tan radar dots) and WITH it (start with the perk: find the saved-inventory key for perks in docs/INVENTORY.md or craft/use the item via the menu flags --give=iron:5,copper:3,titanium:2 then --open-menu --ui-click; the CARGO/CRAFTING flow is in docs/CRAFTING.md), showing ore-coloured radar dots, the ore + yield in the lock line and a tinted bracket. Describe them.
