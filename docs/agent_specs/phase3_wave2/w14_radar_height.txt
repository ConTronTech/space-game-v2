TASK 3.4c - radar height stems (user request after playtest). The old game radar drew, for every contact, a vertical LINE from the contact's flat position on the scope plane up or down to the contact's dot, so you can see at a glance that the body is above or below your plane and by how much ("you just need to go up or down"). Read the old game's radar (read-only reference: /home/contolis/Documents/Space-Game-V2/space-game/space-game, grep for radar) and port the BEHAVIOUR onto the cockpit PROXIMITY_RADAR you wrote (radar_map.h, hud_screens.cpp).
RULES: read docs/WORKFLOW.md and the docs named below. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST): use --ui-click=X,Y,FRAME, saved games and dev flags. Never rm -rf with a variable path; delete scratch files by literal path. Report per item ("N: done | needs decision") + 3-sentence summary (built / verified how / could NOT verify) + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Target hardware: docs/VISION.md (i5 M 560, Intel Ironlake, OpenGL 2.1 fixed function, 60 fps at 1280x720). Docs: docs/COCKPIT.md, docs/HUD.md.
CHANGE: 1) Each contact keeps its flat (right/forward) plot position on the scope; add the height above/below the ship's plane (dot(rel, ship up)) mapped with the same logarithmic scale to a vertical offset on the scope (clamped to a max, sign = up above / down below); draw a thin stem line from the flat position to the offset dot, and a small flat marker at the base so you can read it. Contacts level with the ship (|height| tiny) show no stem. 2) Colour or style the stem so above vs below is distinguishable (e.g. brighter above, dimmer below) and add a tiny UP/DOWN arrow or +/- by the nearest-body label ("PLANET 1 1.5K  UP 800"). 3) Pure functions in radar_map.h with unit tests (tolerances): height offset mapping, sign, clamp, zero height. 4) Docs. 
OWNERSHIP: package/modules/ship/cockpit/**, cockpit/hud test file, docs/COCKPIT.md, docs/HUD.md.
ACCEPTANCE: smoke + make test-san; saved-game screenshots (docs/WARP.md method) with a body clearly above and one below the ship plane; describe them. Delete scratch files.
