TASK 3.7b - HUD dock prompt. You wrote ui/ship_hud (hud_logic.h pure logic + ship_hud.cpp; banner stack, ORBIT LOCKED line, hints). Stations and docking are merged: read docs/STATIONS.md and package/modules/ship/docking/docking_api.h (ship::IDocking with nearestDockable(), events ship::Docked / ship::Undocked) and world/stations/stations_api.h.

RULES: read docs/WORKFLOW.md, docs/HUD.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST); use --ui-click, saved games and the dev flags (--auto-dock=FRAME, --auto-undock=FRAME, stations.seed_offset 2 gives two orbiting stations near Planet 1: see docs/STATIONS.md). Never rm -rf with a variable path. Report per item + 3-sentence summary + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Target laptop: keep per-frame work tiny (no per-frame heap allocations in the draw path beyond what the HUD already does).

CHANGE:
1. Optional services (per-use get, no crash when absent; the HUD must work without ship/docking or world/stations). While a station is within a prompt range (docking.prompt_range default = 4x its dock radius; use the distance IDocking gives) show under the banners a calm status line: "STATION 1  420 m" (name + distance, use the same distance text style as the rest) and, when nearestDockable says ok, a highlighted "DOCK [G]" (the key label: read the real binding from core::IInput if it exposes it, else "G"); when not ok show the reason in a dim colour ("too fast", "too far" - shorten the reason text: map the known reason strings to short forms, unknown = as is). While docked: a persistent "DOCKED: STATION 1 - [G] UNDOCK" line and a short "DOCKED" / "UNDOCKED" banner on the events (like ORBIT RELEASED).
2. Pure logic in hud_logic.h (text builders, reason shortening, prompt state machine, range test) with unit tests (no exact float equality). The cockpit view: these lines live in the banner group which already shows above the ship screens in minimal mode (as ORBIT LOCKED does) - verify with a screenshot in the default cockpit view and in chase view.
3. Docs: docs/HUD.md, docs/STATIONS.md (HUD section, minimal edit).

OWNERSHIP: package/modules/ui/ship_hud/**, the HUD test file under package/tests/, docs/HUD.md, docs/STATIONS.md. Do not touch cockpit, docking or stations.

ACCEPTANCE: smoke + make test-san; saved-game screenshots: approaching a station (distance line + reason), within the zone slow (DOCK [G]), docked (line + banner). Delete scratch files by literal path.
