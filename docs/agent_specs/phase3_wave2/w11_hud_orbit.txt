TASK 3.5b - HUD shows the orbit lock. You wrote ui/ship_hud (hud_logic.h pure logic + ship_hud.cpp). The new module ship/orbit_lock emits ship::OrbitLockChanged{locked, bodyName} (package/modules/ship/orbit_lock/orbit_lock_api.h; docs/ORBIT_LOCK.md).

RULES: read docs/WORKFLOW.md, docs/HUD.md, docs/ORBIT_LOCK.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST): use --ui-click=X,Y,FRAME, saved games and dev flags (`--auto-orbit-lock=FRAME`, see docs/ORBIT_LOCK.md and docs/WARP.md for the save method). Never `rm -rf` with a variable path; delete scratch files by literal path. Report per item ("N: done | needs decision") + 3-sentence summary (built / verified how / could NOT verify) + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

CHANGE:
1. Subscribe to ship::OrbitLockChanged (optional: the HUD must work when ship/orbit_lock is not loaded; unsubscribe/no dangling capture on shutdown, like your other subscriptions). While locked the flat overlay (chase view / full overlay mode) shows a persistent status line "ORBIT LOCKED: PLANET 1" in a calm colour (like the warp "DRIVE ENGAGED" style but not a banner that expires); on release show a short banner "ORBIT RELEASED" (same banner system as your other events). Also show a short "ORBIT LOCK: NO BODY IN RANGE"? NO - the module only logs that; do not add it.
2. Pure logic in hud_logic.h (text builder, empty body name fallback "ORBIT LOCKED", state transitions) with unit tests (tolerances not needed but no exact float equality) in the HUD test file.
3. Cockpit view: the flat overlay steps back there, so if you can do it WITHOUT editing package/modules/ship/cockpit, expose the state; otherwise write in the report what the cockpit worker would need (do not edit cockpit).
4. Docs: docs/HUD.md.

OWNERSHIP: package/modules/ui/ship_hud/**, the HUD test file under package/tests/, docs/HUD.md.

ACCEPTANCE: smoke + make test-san pass; screenshot in chase view (V toggles; use a saved game/flags, e.g. settings/config that select chase, or your existing dev method) mid-orbit-lock showing the status line, and one just after release showing the banner; describe them.
