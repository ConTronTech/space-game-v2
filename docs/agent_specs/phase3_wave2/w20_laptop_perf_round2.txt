TASK 3.8 round 2 - GPU / fill-rate work for the low-end laptop. Round 1 (merged, tag good-20260921-13) was CPU/driver-side. I benchmarked it ON THE LAPTOP with alternating runs of the old and the new binary (same settings): old 33.8 / 35.6 / 35.6 fps, new 36.0 / 35.7 / 35.5 fps = NO measurable change. So the laptop (Intel HD Graphics ILK, Mesa 25.2, 1366x768, GL 2.1, shared memory) is GPU/fill-rate bound, as your llvmpipe experiment suggested. (The laptop also ran slower today than at first: 42.7 fps then, ~35.7 now; it throttles and is noisy: compare only alternating A/B runs.)

LAPTOP DATA (Low preset, windowed 1280x706, --profile=gpu, glFinish per pass; passes avg ms): skybox 11.7 (worst 59), ship/cockpit 10.2 (solid 3.1 + glass 2.9 + screens 2.9), starfield 6.2 (!), star_system 1.7, stations 1.0, everything else < 0.3; frame 38 ms (26 fps under glFinish). Stalls of a consistent ~55-57 ms show up inside whichever pass happens to block (skybox 56.7, starfield 54, cockpit glass 55): a fixed-length GPU/driver stall, not a module. Without glFinish: all on ~35.7 fps; ALL of skybox/star_system/asteroids/starfield/cockpit off: 57-60 fps (~17 ms) - suspiciously near 60: the compositor (Cinnamon) may be capping presentation even with --no-vsync, hiding the GPU cost. Fullscreen (1366x768, no compositor redirect?) gave 38.5 fps and a better 1% low (19.3 vs 12.5), all-off 62 fps. No MSAA is requested (window.cpp sets only GL 2.1, depth 24, double buffer).

RULES: read docs/WORKFLOW.md, docs/PERFORMANCE.md (yours), docs/VISION.md, docs/QUALITY.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop; do not ssh anywhere. SCRATCH FILES: create them ONLY under logs/ (git-ignored) with fixed literal names and do NOT delete them with commands that use shell variables (the tool blocks `rm` with a variable path and stalls); just list them in the report. Report per item + 3-sentence summary + what to benchmark on the laptop. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Constraints: OpenGL 2.1 (extensions that Mesa/Intel Ironlake definitely has, e.g. EXT_framebuffer_object, are OK if you check the extension string and fall back cleanly), generic x86-64, no visual regression on High/Ultra, no gameplay change.

CHANGE (think in FILL RATE: 1366x768 = 1.05 M pixels per full-screen layer; every full-screen layer costs several ms on this GPU; overdraw is the enemy):
1. RENDER SCALE: new tunable render.scale (0.5..1.0, default from the quality preset: low 0.7, medium 0.85, high/ultra 1.0): render the 3D world (skybox, stars, bodies, cockpit view) into an offscreen texture/FBO at scale x the window size, then draw it to the window with one textured quad (GL_LINEAR), UI/HUD/text drawn at native resolution afterwards. If FBO is unavailable or scale >= 1, skip it entirely (zero cost). Handle window resize / fullscreen toggle. Report the fps gain on llvmpipe (2 pinned cores: `taskset -c 0,1 ... LIBGL_ALWAYS_SOFTWARE=1`) at scale 1.0 vs 0.7 vs 0.5. Careful with the depth buffer attachment (renderbuffer DEPTH_COMPONENT24 or 16).
2. STARFIELD (6 ms on the laptop for ~1200 points is absurd): find why: point size 1.5 (non-1 point sizes can fall back to slow paths on old Intel), glEnable(GL_POINT_SMOOTH)/blend, per-frame streak lines, vertex-array setup, many state changes. Try size 1.0 or 2.0 (integer), no smoothing, no blending, GL_POINTS in one glDrawArrays, and when nothing streaks skip the line pass; alternatively bake the stars into the skybox at Low. Keep the look acceptable; show screenshots.
3. COCKPIT GLASS (2.9 ms) and other full-screen blends: look for full-screen alpha quads (canopy tint, vignette, HUD glass): each is a full-screen blend; reduce to at most one, use a cheaper method (scissor to the actual glass area, skip the tint at Low via a cockpit.glass_tint tunable set by the preset, draw the glass BEFORE with less coverage). Also check cockpit solid (3.1 ms): if the display list is big, does the interior need 100% of its triangles at Low? (report triangle count; add a simple LOD if it is high).
4. SKYBOX (11.7 ms): it is 1 to 3 visible full-screen textured faces. Try: skip the frame glClear COLOR when the skybox is on (the sky overwrites every pixel; keep DEPTH clear; core/window or render_engine, report the location), no mipmaps and GL_LINEAR is right, draw the sky at render.scale automatically via item 1, use GL_RGB with 512 px at Low (already) and test 256. Report the effect via llvmpipe.
5. PRESENT/COMPOSITOR: the game shows suspiciously ~60 fps when everything is off with --no-vsync: check whether SDL_GL_SetSwapInterval(0) actually takes effect (log the result of SDL_GL_GetSwapInterval after setting), and think about what else could cap it. Also add a `--fullscreen` dev flag (or make the existing setting easy: video.fullscreen exists) so the coordinator can A/B on the laptop; a borderless-desktop fullscreen (SDL_WINDOW_FULLSCREEN_DESKTOP) may let the compositor unredirect: verify which mode window.cpp uses and report.
6. Update the profiler to also print the render.scale in use. Tests for pure logic (scale/size computation, preset rows) with tolerances. Docs: docs/PERFORMANCE.md (round 2: findings and each fix), docs/QUALITY.md, docs/BENCHMARK.md, docs/CONFIG.md if needed.

OWNERSHIP: package/modules/core/render_engine/**, package/modules/core/window/**, package/modules/world/starfield/**, package/modules/world/skybox/**, package/modules/ship/cockpit/**, package/modules/core/quality/quality_rules.h (rows only) and its tests, package/modules/core/ui_handler/** only if the render-scale composite needs a hook (say so), tests (test_perf.cpp + small edits), docs above. Anything else: needs decision.

ACCEPTANCE: smoke + make test-san pass; screenshots at render.scale 1.0 vs 0.7 (cockpit view), starfield before/after; llvmpipe fps numbers per item; a list of exactly what to run on the laptop (the coordinator will use package/tools/laptop_bench.sh with extra flags such as --settings=file for render.scale via game.json keys: tell me the exact keys/flags to A/B each fix independently, e.g. individual tunables to switch each change off).
