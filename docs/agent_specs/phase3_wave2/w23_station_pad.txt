TASK 3.7d - station model + landing pad docking (user playtest feedback). Two problems in the merged stations/docking:
(1) MODEL: the placeholder uses the CYLINDER as the base and the cube on/through it. It must be the reverse: the CUBE is the base (the station body/habitat) and the CYLINDER, FLATTENED to a disc, is the landing PLATFORM (pad) sitting on top of the cube's top face (wider than the cube so it reads as a pad; thin, e.g. thickness ~ 0.08 x cube size; give the pad a distinct colour ring/marking so it is obvious, e.g. an inner lighter disc). Orbital stations: cube base + pad on the top face (top = the spin axis direction `up`), the whole station spins about `up` (as now). Planetary stations: cube standing on the surface, pad on top (up = surface normal).
(2) DOCKED POSE: today the ship "holds the offset it had at the moment of docking" so it hangs in space wherever it was. In the OLD game (read-only reference: /home/contolis/Documents/Space-Game-V2/space-game/space-game, include/world/stations.h getStationOrientation and how the ship copied the station's orientation when docked; grep the ship/docking code in src/ and include/ship/ship.h for the docked logic) the ship SAT ON THE PLATFORM, always flat on it, and carried the station's motion and rotation. Reproduce that: when docked the ship sits ON the pad (position = pad centre + up * (pad top height + a ship half-height offset so the hull rests on it), ship `up` = station `up` (flat on the pad, belly to the pad), ship forward = a fixed direction in the station frame (the ship's heading at the moment of docking projected onto the pad plane, kept in the station's frame), and BOTH follow the station every fixed step: orbital stations move around their planet AND spin about `up`, planetary stations move with their planet. The user struggled to get moving+rotating bodies to carry the ship so it stays flat: do it analytically (no integration drift): pose(t) = stationPose(t) x fixedLocalOffset, where stationPose comes from IStations at the current sim time (position double, velocity, up, spin angle/rate: extend world::StationInfo/IStations minimally: e.g. `Vec3d forward` (the spin reference axis at the current spin angle) and `double spinRate` (rad/s) so orientation is known), and set the ship's linear velocity every step to the pad point's true velocity = station velocity + spinRate * up x r (so speed readouts, undocking momentum and everything else are consistent), and its orientation from the pose.
   SHIP API: IShip has no orientation setter. Add the MINIMAL contract addition in package/modules/ship/ship_core/ship_api.h: non-pure `virtual void setPose(const engine::Vec3& pos, const engine::Vec3& fwd, const engine::Vec3& up) {}` (or a `holdPose` variant; your call, keep it non-pure with a no-op default so fake_ship etc. still compile) and implement it in ship_core.cpp: set position, fwd_/up_/right_ (orthonormalised), zero angular velocity, keep the prev* interpolation state consistent so the camera does not jitter (the ITransformSource pose interpolates between fixed steps: set prev = current for the first step of a hold, then let the normal prev/current shift give smooth interpolation while the pad moves), sync physics (physics_->teleport or setBody with the pad velocity), and make ship_core IGNORE player rotation/thrust input while a hold is active (the docking module tells it: a `setHeld(bool)` non-pure default, or the same setPose call with a flag; keep it simple and documented). Thrust input (or G) still UNDOCKS as now; on undock the ship keeps the pad point velocity + the small push, control returns.
   TRANSITION: docking should not snap: on G (all existing conditions unchanged) ease the ship from where it is to the pad pose over docking.approach_seconds (default 1.5 s, smoothstep on position and a slerp/nlerp of the orientation, target recomputed every step from the moving station) and only then mark it docked (Docked event at the END of the approach, or at the start: your call, document it; the HUD shows DOCKED on the event). Undock: instant release as before.
   COLLISION: while approaching or docked the ship sits inside/on the station's physics sphere; make sure this does NOT produce collision damage/bounce (e.g. ignore Collided with kind "station" while docking/docked in ship_core's collision handler via the same hold flag, or skip it in the physics registration for the docked ship: pick the smallest correct change). Also verify a ship that is docked at a planetary station is not hit by the planet body.
   ALSO: the docking "zone" and `docking.max_speed` rules stay. Keep everything in the modules world/stations, ship/docking (+ the small ship_core / ship_api change above); do NOT touch star_system, HUD, cockpit (the HUD keeps working via the same events/services), radar (its station diamond uses info().position: keep that meaning = the model centre; if the pad changes the model centre, say so).

RULES: read docs/WORKFLOW.md, docs/STATIONS.md, docs/SHIP.md, docs/PHYSICS.md, docs/ORBIT_LOCK.md (same steering idea), docs/PERFORMANCE.md (laptop is GPU fill-rate bound: no extra full-screen layers, keep the model tiny: a cube + a flattened cylinder ~ 100 triangles, display list or vertex arrays). Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh, do not run the game fullscreen or --quality=low on this desktop except with a small --frames. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Report per item + 3-sentence summary + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

PURE LOGIC + TESTS (tolerances, no exact float equality): the pad pose function (station pose x local offset -> position/forward/up), the pad point velocity (v + spinRate * up x r), the local-offset capture at dock start (heading projected onto the pad plane), the approach easing (smoothstep, orientation nlerp stays orthonormal), for both an orbital (moving + spinning) and a planetary station; a test that after N simulated steps of a moving+spinning station the ship's up stays within 1e-4 of the station up and its distance to the pad point stays ~0 (no drift), and that speed relative to the station is ~0.

OWNERSHIP: package/modules/world/stations/**, package/modules/ship/docking/**, package/modules/ship/ship_core/ship_api.h and ship_core.cpp (ONLY the pose/hold addition and the docked collision skip), package/tests/test_stations.cpp (+ a ship_rules/ship test if needed), docs/STATIONS.md, docs/SHIP.md.

ACCEPTANCE: smoke + make test-san pass; saved-game screenshots (docs/WARP.md method; stations.seed_offset 2 puts two orbital stations near Planet 1, the default seed has planetary ones on Planet 2) of (a) the new model from outside: cube base + flat pad on top, both station kinds; (b) docked: the ship resting flat on the pad in chase view and cockpit view; (c) log lines over several seconds while docked at a moving+spinning orbital station: distance to the pad point, ship up vs station up angle (should stay ~0), relative speed (~0), and the same at a planetary station; (d) undock momentum: the speed after undocking equals the pad point velocity + push. Describe them.
