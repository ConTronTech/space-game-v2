TASK 1.8 - monitor awareness. The user runs the game on a mini retro rig (see docs/DEVICES.md, read it first): a laptop with TWO displays: LVDS-1 1366x768 (16:9-ish laptop panel, primary) and VGA-1 = a CRT at 1280x1024 (5:4, 310x230 mm), as an extended desktop of 2646x1024; later also lower-res 4:3 CRT modes (640x480 ... 1024x768) and normal PCs with one or more modern monitors (main rig: 1920x1080 @ 60). Today the game only knows "the window": it opens on whichever display SDL picks (SDL_WINDOWPOS_CENTERED = display 0), core/quality reads display 0's desktop mode only, fullscreen uses SDL_WINDOW_FULLSCREEN_DESKTOP, the UI/HUD/FOV assume 16:9. Make the game display-aware.

RULES: read docs/WORKFLOW.md, docs/DEVICES.md, docs/QUALITY.md, docs/PERFORMANCE.md (fullscreen matters: on the laptop borderless fullscreen was 56 -> 80 fps and 1% low 16 -> 29 fps vs windowed because it skips the compositor), docs/UI.md, docs/HUD.md, docs/COCKPIT.md, docs/INPUT.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh, do not run the game fullscreen on this desktop except with a tiny --frames (this machine has ONE 1920x1080 monitor: multi-monitor behaviour must be proven by unit tests with fake display lists, and the coordinator will test the real two-display laptop). SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Report per item + 3-sentence summary + manual checklist + what to test on the laptop. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

CHANGE:
1. DISPLAY MODEL (pure logic in display_rules.h, no SDL types; unit tests with fake data): a `DisplayInfo { index, name, x, y, w, h, refreshHz, dpi/size?, modes[] }` list built by core/window from SDL (SDL_GetNumVideoDisplays, SDL_GetDisplayName, SDL_GetDisplayBounds, SDL_GetDesktopDisplayMode, SDL_GetNumDisplayModes/SDL_GetDisplayMode, SDL_GetDisplayDPI where available), and pure functions: aspect classification (4:3, 5:4, 16:10, 16:9, ultrawide, other), choose display (settings `video.display` = auto|0|1..., flag `--display=N`; auto = the display the mouse/primary is on: SDL_GetGlobalMouseState if available else the primary/display 0; if the chosen index does not exist fall back to auto and log), choose a fullscreen mode (`video.mode`: borderless (default = FULLSCREEN_DESKTOP) | exclusive with `video.resolution` "1024x768@85" picking the closest available mode from the display's mode list | windowed with `video.window_size` "1280x720"), window placement (center the window ON the chosen display, not display 0), list of selectable resolutions filtered/sorted (dedupe, min 640x480), and a "CRT/low-res hint" (e.g. 4:3/5:4 aspect and refresh >= 60 with width <= 1280: report it as `retro`, purely informational for logs and presets: do NOT guess more than that).
2. WINDOW: apply the chosen display, mode and placement at startup (before the GL context if possible, SDL_WINDOWPOS_CENTERED_DISPLAY(n) / SDL_SetWindowFullscreen with SDL_SetWindowDisplayMode for exclusive), keep working when the display is unplugged or the window moves to another display (SDL_WINDOWEVENT_DISPLAY_CHANGED: re-read info, log it, refresh dependent values: viewport, UI scale, quality's displayW/H), keep `--fullscreen`, `--no-vsync`, settings `video.fullscreen` working exactly as before (the settings toggle is Borderless fullscreen on/off), log ONE clear block at startup: every display (name, bounds, mode, refresh, DPI/size, aspect class), which one was chosen and why, mode chosen, actual drawable size (SDL_GL_GetDrawableSize), swap interval. Provide a service `core::IDisplays { list(); current(); chosen(); drawableSize(); refreshHz(); aspect(); }` in window_api.h (or the existing window header) for other modules.
3. ASPECT-AWARE RENDERING/UI: (a) FOV: the camera's fovDeg is treated as VERTICAL FOV today?: check; for 4:3 / 5:4 the horizontal FOV gets narrow and for ultrawide too wide: apply "Hor+"-style correction so the horizontal FOV stays comfortable: tunable `camera.fov_mode` = vertical | horplus (default horplus with a reference of the settings FOV at 16:9; 5:4 must not look zoomed-in/cropped; document the formula, pure function + tests at 16:9, 4:3, 5:4, 21:9); (b) UI/HUD/pause menu/cockpit screens must fit 5:4 and 4:3 (e.g. 1280x1024, 800x600, 640x480) without clipping or overlapping: audit ui_handler layout scale (does it scale by height or width?) and the HUD Layout/banner widths, fix layouts that assume 16:9 within YOUR ownership, and REPORT (needs decision, not edit) problems in files you do not own; (c) the render.scale FBO path (core/render_engine) and the vignette/skybox must follow the drawable size; check the viewport after mode changes and a display change.
4. QUALITY: core/quality should read the CHOSEN display's size/refresh (not display 0's) and add the `retro` hint + display count to its hardware log line; if it auto-selects a preset it must not change for this laptop (Ironlake still Low): add a small documented rule: displays with a very low pixel count (<= 1024x768 native) prefer Low/Medium presets' UI cheapness only if you can justify it in tests; otherwise change nothing.
5. SETTINGS UI (pause menu -> Settings): rows for Display (cycles Auto / 1 / 2 ... "Display: 2 (VGA-1 1280x1024)"), Mode (Borderless / Exclusive / Windowed), Resolution (cycles the list for the chosen display when Exclusive or Windowed), each showing "(restart)" only if it really needs a restart; apply changes live where SDL allows it (fullscreen toggle and window resize already work live; display change: move the window to the target display live if simple, else restart). Keep the existing rows working, keep the settings page inside the screen at 5:4/4:3 (scroll or two columns if needed: the page now has ~8 rows).
6. DEV FLAGS: `--list-displays` (prints the display block described in item 2 and EXITS before creating the GL window: use SDL_INIT_VIDEO only, must work over SSH with DISPLAY=:0; and print a fake-friendly machine-readable JSON to stdout with `--list-displays=json`), `--display=N`, `--video-mode=borderless|exclusive|windowed`, `--resolution=WxH[@Hz]`, `--fake-displays=1366x768;1280x1024` (test hook: feeds the pure logic a fake list so aspect/placement paths can be screenshot-tested on this single-monitor machine without changing the real display).
7. Tests (pure logic, tolerances): aspect classes, display choice rules (auto/index/out-of-range/mouse-on-display), closest-mode picking incl. no exact match, placement centering on a display with a non-zero origin (e.g. VGA at 0,0 and LVDS at 1280,256), resolution list dedupe/sort/min, Hor+ FOV at several aspects, retro hint. Docs: docs/DISPLAYS.md (new: model, settings keys, flags, aspect handling, how to test on the CRT), docs/QUALITY.md, docs/UI.md (aspect notes), docs/DEVICES.md (append what you learn), docs/CONFIG.md if keys.

OWNERSHIP: package/modules/core/window/**, package/modules/core/quality/** (hardware/display read only), package/modules/core/camera/** (FOV mode only), package/modules/core/render_engine/** (viewport/size handling only), package/modules/core/ui_handler/** (aspect/scale layout only), package/modules/ui/pause_menu/** (settings rows/layout only), package/modules/core/settings/** if a key needs registering, package/modules/ui/ship_hud/** ONLY for layout fixes needed at 5:4/4:3 (another worker edits the HUD right now: keep changes minimal and tell me exactly which lines; if it conflicts, report instead), tests (test_display.cpp new, globbed), docs above. Do NOT edit the cockpit, input, ship, world, combat, gameplay modules.

ACCEPTANCE: smoke + make test-san pass; `--list-displays` output pasted (this machine) and `--fake-displays=1366x768;1280x1024 --list-displays` output; screenshots (small --frames, windowed) at 1280x1024 (5:4), 800x600 (4:3), 640x480, 1366x768 and 1920x1080 via --resolution/--video-mode=windowed of: the cockpit view with HUD, the pause menu Settings page, the radar screen: describe clipping/overlaps and what you fixed; a Hor+ FOV before/after at 5:4; the startup log block; that `--fullscreen`, `--no-vsync`, `--benchmark` behave as before (numbers on this machine).
