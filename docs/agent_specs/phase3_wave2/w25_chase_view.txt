TASK 2.6b - chase view (V key) fixes, user playtest feedback: "when pressing V for the camera the skybox does not match and we are not displaying the ship model because I have made it a full model with internal cockpit which we are using in first-person view".
So two problems in chase view (CameraMode::Chase, core/camera):
(1) SKYBOX MISMATCH: pressing V changes the sky relative to the first-person view (it does not line up with the world/stars/planets or jumps). Reproduce it with saved-game screenshots of the SAME ship pose in cockpit view and chase view (e.g. ship rolled/pitched so a bug in the up vector or view construction shows: check cam::chasePose / cam::viewMatrix in package/modules/core/camera: does the chase camera build its orientation from WORLD up instead of the ship's up, does it look at the ship (pitching down) instead of along the ship's forward, is the position interpolation (alpha) mismatched, is the skybox using a stale/other view matrix, are the starfield/star_system/asteroid passes camera-relative with a different camera than the skybox?). Find the actual cause and fix it so the chase camera is a rigid offset camera fixed to the ship frame: same orientation as the ship (roll follows the ship), positioned chaseDistance behind and chaseHeight above IN THE SHIP FRAME, looking parallel to the ship forward (a small fixed downward pitch toward the ship is fine only if the sky/stars stay consistent with the world: everything is drawn with that same view, so what must hold is that the skybox rotation = the view rotation exactly, and toggling V must not change the ship's world heading of the sky beyond the fixed camera offset). Say precisely what was wrong.
(2) SHIP MODEL IN CHASE VIEW: today the chase view draws the OLD placeholder wireframe fighter (ship_core.cpp drawShip pass "flight/ship") instead of the real ship. The real ship model is the ShipV2 OBJ the cockpit module loads (assets/models/ship/ShipV2, see docs/COCKPIT.md: it is a FULL model with the interior; the cockpit module draws it in VIEW space glued to the camera in cockpit view, with '@' tagged live screens, glass, display lists, cached screens). In chase view draw that SAME model from outside in WORLD space at the ship's interpolated pose (core::ITransformSource pose at eng.alpha(): position, forward, up; the model's local frame = the frame it is drawn in for cockpit view, where the pilot's eye is at the view-space origin: so the transform is view * shipPose (translate to ship position, rotate into the ship frame) and the eye-origin convention must be respected: check the model's coordinates/scale in package/modules/ship/cockpit and get the orientation right: forward/up axes, handedness, units). Include the interior/screens as they appear from outside (opaque hull, glass see-through, screens live: reuse the cached screen quads; if they are not visible from outside they may be skipped: measure and choose the cheapest correct behaviour), lit by the REAL sun now (the star system exists: direction from the ship to world::IStarSystem sun via the optional service, transformed into the right space; cockpit.light_dir stays the fallback when there is no star system; in cockpit view the same sun direction should be used so the cockpit lighting matches the world: keep it simple and correct). Remove/disable the wireframe fighter when the real model is available (keep it as the fallback when no model loaded): coordinate through a tiny read-only service or flag from the cockpit module (e.g. cockpit::ICockpitScreens or a new ship::IShipModel { drawnInChase() }) and gate ship_core's drawShip on it: the ship_core edit must be limited to that gate.
   Also: in chase view the ship must not be clipped by the near plane or the camera (chase distance 12 m default); depth-sorting with the see-through glass must still work; the ship should sit at the right depth relative to asteroids/planets; the engine exhaust particles (fx/particles) emit from the ship rear: they should look right now that the real model is drawn (the exhaust position offset comes from the ship frame; check the model's rear and, if a small constant offset in fx/particles is off, REPORT it as needs decision, do not edit fx).
   ALSO check: the cockpit view is unchanged pixel-for-pixel apart from the sun lighting change (screenshot before/after), the V toggle works both ways with no flash, the HUD flat overlay in chase view is unchanged, the docked-on-pad ship (stations) looks right in chase view (the model rests flat on the pad: report if the pad offset `docking.rest_height` needs adjusting for the real model's belly height and tell me the value; do not edit docking).

RULES: read docs/WORKFLOW.md, docs/COCKPIT.md, docs/SHIP.md, docs/PERFORMANCE.md (laptop: GPU fill-rate bound; do not add full-screen layers; the model is already display-listed: draw it once per frame), docs/WORLD.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh, do not run the game fullscreen or --quality=low on this desktop except with a small --frames. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Report per item + 3-sentence summary + manual checklist. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

PURE LOGIC + TESTS (tolerances): the chase pose function (orientation equals the ship's, position = ship + frame offset, roll follows, no NaN at pitch +-90 / with a zero forward), the view rotation equals ship rotation (skybox consistency), the model transform (world matrix from a pose) and the sun-direction-to-view-space conversion.

OWNERSHIP: package/modules/core/camera/** (chase pose/view matrix fix), package/modules/ship/cockpit/** (world-space model draw in chase view, lighting), package/modules/ship/ship_core/ship_core.cpp (ONLY the gate on the wireframe fighter pass), package/modules/world/skybox/** ONLY if the cause is in the skybox, tests (test_camera*.cpp / test_cockpit.cpp), docs/COCKPIT.md, docs/SHIP.md.

ACCEPTANCE: smoke + make test-san pass; saved-game screenshot pairs (docs/WARP.md method; camera.mode 0 vs 1 via a settings file {"camera.mode":1}) at the SAME pose including a rolled/pitched pose: the sky/stars/planet line up (same features at the same bearing modulo the fixed camera offset), the real ShipV2 model visible from behind in chase view (describe it: hull, glass, orientation right way up, forward pointing the right way), the docked-on-pad chase view, the sun lighting on the model; the cause of the sky mismatch stated.
