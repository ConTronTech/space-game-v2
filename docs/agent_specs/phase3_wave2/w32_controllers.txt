TASK 1.9 - controller awareness: joysticks, steering wheels, pedals, shifters. The user's test rig (docs/DEVICES.md, read it first) has a PXN PXN-V10 racing wheel (USB 11ff:3245, /dev/input/js0) and a Microsoft SideWinder joystick (045e:003c, js1) connected NOW; PEDALS AND A STICK SHIFTER WILL PLUG INTO THE WHEEL BASE LATER, so the wheel is ONE HID device exporting steering + pedal axes (throttle/brake/clutch, possibly combined into one axis) + shifter buttons + wheel buttons + a hat. The game only has keyboard + mouse input methods today (core/input_methods/{keyboard,mouse}, JSON profiles in config/input/*.json, action values read through core::IInput; read docs/INPUT.md and the existing methods first: a new device type must plug in the SAME way, as another input method module).

RULES: read docs/WORKFLOW.md, docs/DEVICES.md, docs/INPUT.md, docs/MODULES.md, docs/UI.md, docs/DISPLAYS.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST), do not ssh; this dev PC may have NO joystick attached: ALL logic must be provable with fake devices in unit tests (feed the pure mapper recorded/simulated axis and button values), and a `--fake-joystick=...` test hook (see below) so scenarios run without hardware; the coordinator will run the real devices on the laptop over SSH (js nodes are world-readable, SDL joystick needs no input group) and the USER will test the real feel. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Report per item + 3-sentence summary + manual checklist + exactly what to run on the laptop. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

CHANGE:
1. New input method module package/modules/core/input_methods/joystick/ (name "core/input_methods/joystick"; depends on core/input_handler, registers like keyboard/mouse do). Uses SDL2's joystick API (SDL_INIT_JOYSTICK; NOT SDL_GameController: wheels/flight sticks are usually not in the game controller DB, and we want raw axes/buttons/hats), enumerates devices at startup and on hot-plug (SDL_JOYDEVICEADDED/REMOVED), reads them per frame with SDL_JoystickUpdate (works headless with no window/display: the joystick subsystem must not require focus; make sure background input is enabled with SDL_HINT_JOYSTICK_ALLOW_BACKGROUND_EVENTS so it works when the window is unfocused). One physical device may expose MANY axes/buttons: everything is addressed as device + axis index / button index / hat.
2. PURE LOGIC in joystick_rules.h (no SDL types; unit tests with tolerances, simulated inputs): axis normalisation to -1..1 from SDL's int16 (-32768..32767) with per-axis calibration {min, max, centre}, dead zone (with rescaling so the output starts at 0 after the dead zone), response curve (exponent), invert, per-axis scale; a "combined pedals" mode (one axis where throttle = above centre, brake = below centre, as some wheels report both pedals on one axis) and "split pedals" (separate axes per pedal, each with its own min/max because pedal axes often rest at -1 or +1 and go to the opposite end: an `rest` = min|max option so an unpressed pedal reads 0, fully pressed 1); button -> action press/hold/toggle; hat -> 4 buttons; shifter gears as buttons mapped to discrete values (e.g. gear buttons -> action `throttle_limit` steps, or reverse gear button -> a flag): keep this generic: "button group -> discrete value" mapping; auto-centre/auto-range learning is NOT required, but a `--joystick-calibrate` dev mode that prints the min/max/centre seen per axis over 10 s of the user moving everything and writes a suggested profile snippet to logs/joystick_calibration.json IS (the user will use it with the wheel and pedals: this is how axes get identified).
3. PROFILES: per-DEVICE JSON profiles in config/input/devices/<vid>_<pid>_<slug>.json (or matched by name substring) selected automatically by the device's USB ids/name/GUID (SDL_JoystickGetVendor/Product), with a generic fallback profile when a device is unknown (first two axes = yaw/pitch style stick? choose safe defaults that do NOT make the ship spin uncontrollably: unknown devices map nothing except a log line telling the user to run --list-joysticks and --joystick-calibrate). Profile format: axes (index -> action + scale + invert + dead zone + curve + calibration + role: stick|steering|throttle|brake|clutch|combined_pedals), buttons (index -> action), hats, button groups, and `merge` with the active keyboard/mouse profile (the actions are the same names the keyboard profile uses: thrust, strafe, lift, roll, yaw, pitch, brake, fire, weapon_1, weapon_2, dock, toggle_warp, toggle_orbit_lock, toggle_menu, camera_next, pause...: read config/input/default.json for the list) so axis values ADD to key values like the mouse does via IInput::contribute. SHIP THE FOLLOWING PROFILES with sensible defaults based on general knowledge of these devices, clearly marked "UNVERIFIED: run --joystick-calibrate": (a) Microsoft SideWinder (classic 3-axis: X = yaw? pitch/roll conventions: joystick X -> roll, Y -> pitch (invert so pulling back pitches up), twist Rz -> yaw, throttle slider -> thrust (centre-detent optional), trigger -> fire, hat -> look/none, other buttons: weapon_1/2, dock, warp...), (b) PXN V10 wheel (steering axis -> yaw with a wheel-like curve and a bigger dead zone option, pedals: throttle -> thrust, brake -> brake; clutch -> lift down?; paddles/buttons -> weapons/dock/warp; shifter buttons -> a documented mapping table, initially just the actions warp/orbit lock/menu). The point is a working first guess the user can tune; describe in docs how to adjust.
4. SERVICE + UI: a service `core::IControllers { devices(); deviceCount(); rawAxis(dev, i); rawButton(dev, i); lastEvent(); }` in joystick_api.h for the future controls settings screen and the profiler/debug overlays; a settings entry `input.joystick_enabled` (default true), `input.joystick_deadzone_scale` (user multiplier 0.5..2), `input.joystick_sensitivity` (multiplier); the pause menu Settings rows are OUT OF SCOPE (another worker just reworked that page; report what rows you would add). A short log block at startup: each device (name, vid:pid, axes, buttons, hats, GUID, matched profile or fallback).
5. DEV FLAGS (work with NO window: SDL_INIT_JOYSTICK only, so they work over SSH; they must exit before creating the GL window like --list-displays does in core/window: find the smallest correct place, e.g. the module's init after SDL_Init(JOYSTICK) calling std::exit(0)): `--list-joysticks[=json]` (table of devices with axes/buttons/hats/vid:pid/GUID + which profile would match), `--joystick-monitor` (prints live axis/button changes for ~30 s, throttled, so the user/coordinator can see which axis is which while moving the wheel/pedals/shifter: this is the "evtest" equivalent for the game's own mapping), `--joystick-calibrate` (item 2), and `--fake-joystick=<profile-name-or-spec>` + `--fake-axis=IDX:VALUE[,..]` `--fake-button=IDX[,..]` test hooks that create a virtual device feeding the same pipeline so scenarios (e.g. hold roll via the fake stick) run with a saved game without hardware.
6. Tests (tolerances): dead zone rescale continuity, calibration mapping incl. centre offset, curve, invert, combined vs split pedals (rest at min/max), button groups, hat to buttons, profile matching (vid/pid, name substring, fallback), profile JSON parsing + errors (bad file: log and use the fallback, never crash), hot-plug add/remove state machine, and an end-to-end test: fake device + profile -> IInput action values (pitch/yaw/thrust) after a step. Docs: docs/CONTROLLERS.md (new: how it works, device model incl. one-device-many-controls, profile format with an annotated example, the flags, how to find which axis is which with --joystick-monitor and --joystick-calibrate, tuning), docs/INPUT.md (link + the new device type), docs/DEVICES.md (append the shipped profiles and the UNVERIFIED status), docs/MODULES.md, docs/CONFIG.md.

OWNERSHIP: package/modules/core/input_methods/joystick/** (new), config/input/devices/*.json (new), package/modules/core/input_handler/** ONLY if the method registration needs a small additive hook (say exactly what), tests (test_joystick.cpp new, globbed), docs above. Do NOT edit window, pause_menu, ship, HUD, cockpit or other modules: report "needs decision".

ACCEPTANCE: smoke + make test-san pass; `--list-joysticks` on this machine (probably 0 devices: paste it) and `--fake-joystick=... --fake-axis=... --joystick-monitor` output showing the pipeline; a saved-game scenario (docs/WARP.md method) where a fake stick input produces roll/pitch/yaw/thrust (ship speed/heading log); the annotated example profile; benchmark unchanged with the module loaded and no device (numbers). Describe what the coordinator should run on the laptop: `./space_game_v2 --list-joysticks`, `--joystick-monitor`, `--joystick-calibrate`.
