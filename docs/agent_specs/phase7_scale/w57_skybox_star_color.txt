TASK 7.2 - match the skybox to the seeded star's colour (user: "randomly seeded stars aligned to the skybox - blue star blue skybox"). REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome; do not wait to finish everything before reporting.

CONTEXT: `world/star_system` picks the sun's colour randomly from a 5-entry palette (warm white, pale blue, orange, red-orange, near-white - see `star_system_rules.h`'s `sunColors`) every seed. `world/skybox` separately picks a pre-rendered skybox image set from `assets/skybox/bkg/<color>/<set>/` (currently 8 colour-named folders exist: blue, dark, green, lightblue, orange, purple, red, teal - these are the USER's assets, read-only, do not add/edit/remove any image files), controlled by a `skybox.set` config value that today defaults to a FIXED `"dark/set1"` with no connection at all to the generated star. The two are completely decoupled, so a blue-white star can end up under an orange sky. Make `world/skybox` pick whichever available set's colour is the closest match to the seeded star's actual colour, so the sky always reads as consistent with the star lighting the scene.

CHANGE:
1. Give `world/skybox` an OPTIONAL dependency on `world/star_system` (it must not crash or behave differently if star_system is absent/disabled - falls back to exactly today's fixed-default behaviour). Read the sun's colour (`IStarSystem::bodies()[0]`, the sun `Body`'s `.color` field - confirm the exact field/accessor by reading `star_system_api.h`) once at init, before choosing which skybox set to use.
2. Decide HOW to match a skybox set's "colour" to the star's RGB. The folder NAME (blue/dark/green/lightblue/orange/purple/red/teal) is a hint but not a reliable numeric colour - prefer actually sampling each found set's real pixel colour: decode one representative face image per set (once, at startup, not per frame - this is a small one-time cost, justify it against docs/PERFORMANCE.md if it turns out to matter) and compute its average RGB (or use existing decode/downscale machinery already in skybox.cpp if that's easier than a second image load - your call, avoid loading full-resolution images twice if the existing pipeline already has a cheap thumbnail/preview step). Pick whichever set's average colour has the smallest distance (plain RGB Euclidean distance is fine, document if you choose otherwise) to the star's colour. Implement the actual matching/distance logic as a pure function in `skybox_rules.h` (no GL/SDL/file I/O in the pure function itself - it should just take a target colour and a list of candidate (name, colour) pairs and return the best match), unit-tested with a few synthetic cases (exact match wins outright; nearest of several candidates is chosen correctly; empty candidate list handled without crashing).
3. **An explicit user/config choice must still win.** If `skybox.set` is explicitly configured to a real value (not just left at whatever placeholder/default the engine's config system uses to mean "not set" - read how `eng.config.get` and `config/game.json`'s "DEFAULT" placeholder convention work, docs/WORKFLOW.md or the config module's own docs, to do this correctly), use that set exactly as today, unaffected by star-colour matching. Auto-matching should only kick in when the user has NOT explicitly chosen a set - this becomes the new default behaviour (so a fresh `config/game.json` with `skybox.set` left as its auto-generated placeholder gets colour-matched; a deliberately edited value is never overridden). New tunable `skybox.match_star_color` (default true) as an explicit off-switch alongside that.
4. Docs: `docs/WORLD.md` (the matching behaviour, the new tunable, how the "explicit override always wins" rule works), a one-line note in whichever doc lists `skybox.*` tunables today.
5. Tests: the pure matching/distance function (item 2) with synthetic colour lists; a check that the "explicit override wins" rule (item 3) is honoured (some sensible way to unit-test this against the config layer's actual "was this explicitly set" mechanism, once you've confirmed how it works); a check that star_system being absent/disabled leaves skybox behaving exactly as it does today (regression).

RULES: read docs/WORKFLOW.md, docs/WORLD.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST). For any scripted scenario / screenshot run of the real game, use --display=1 --input-profile=testing (docs/WORKFLOW.md "Testing display + mouse capture"). Use several different `world.seed` values via saved games or a fresh start (docs/WARP.md method / `--seed=N` if that flag exists, check) to show different star colours picking different, sensible skybox sets. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Do NOT touch any file under assets/ (the user's own art) - read-only.

OWNERSHIP: package/modules/world/skybox/** only (skybox.cpp, skybox_rules.h and any skybox_api.h if it needs a new accessor - unlikely). tests (test_skybox.cpp additions, or wherever skybox is tested today). docs above. Do not edit world/star_system (read its Body/colour field only, through the existing IStarSystem interface - do not add anything to that interface; if you find you need something IStarSystem doesn't expose, report "needs decision" rather than editing it yourself).

ACCEPTANCE: smoke + make test-san pass; at least 3 different seeds shown to produce visibly different, colour-appropriate skybox choices (screenshot or logged "star colour (r,g,b) -> chosen set 'X/Y' (distance D)" for each, docs/WARP.md method); confirm an explicit `skybox.set` override in game.json is still honoured exactly (not overridden by matching); confirm `--disable=world/star_system` (or star_system genuinely absent) leaves skybox picking its old fixed default, unchanged from today.
