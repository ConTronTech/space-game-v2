TASK 7.1 RESUME - continue the double-precision ship/physics position work after a machine crash killed the previous worker's terminal (the file changes survived intact and still build clean - do NOT restart from scratch). REPORT EARLY: send a heartbeat after your first working step and after each item.

STATE ALREADY DONE (verify, do not redo): items 1-4 of the original task (docs/agent_specs/phase7_scale/w56_double_precision_position.txt - read it for full context) are complete and uncommitted in this worktree: engine::Vec3d added to package/engine/math.h, world::Vec3d aliased to it; ship_core's pos_/prevPos_/spawnPos_ are now engine::Vec3d with double integration; IShip gained positionD()/setPoseD() (float position()/setPose() unchanged, still compiles for every existing caller); core/physics_world stores double internally with a real double swept test, IPhysics gained additive double addBody/setBody/teleport/query overloads (float overloads still work, forward to the double ones); Collided gained posAd/posBd; ship_core/asteroids/stations updated to the double overloads; gravity/orbit_lock/docking now call positionD()/setPoseD(); core::Pose gained posD. Confirmed just now (after the crash): the worktree still builds clean with `make -j` and `make test` passes (545 tests, 0 failed) - re-verify this yourself as your first step, then re-read the diff (`git diff`) to refresh your own context on exactly what's already there before continuing.

REMAINING WORK (items 5-6 of the original spec, not yet done):
1. **Regression-first testing at today's scale.** Confirm every existing test still passes with NO tolerance loosened to hide a real behaviour change (the 545 passing now is a good sign but re-verify under `make test-san`, not just `make test`). Add a regression test flying the ship through a normal scenario (thrust, orbit, dock) entirely within today's small-scale distances, confirming tolerance-for-tolerance identical results to before this change.
2. **Large-scale proof (the actual point of this task).** A scripted scenario teleporting the ship to at least 10,000,000 units from the origin (push further toward 5e9-1e10 if time allows) and confirming: position integration under thrust stays smooth (log position deltas per frame, show they track velocity*dt with no quantization/snapping); a `ship/gravity` orbit near a body out there stays stable over many simulated seconds (reuse the `--gravity-scenario=orbit`-style verification); `ship/orbit_lock` still settles correctly that far out; `ship/docking` still holds within its normal tolerance that far out. Report real before/after numbers proving this was broken (with the old float position) and is now fixed.
3. **Camera-jitter measurement (diagnostic only, not a fix - the camera/render chain is explicitly OUT OF SCOPE for this task, deferred to a dedicated follow-up).** Measure and report how the current camera path (still float end-to-end past ship_core's new double position, per the earlier decision) behaves at 1e7 and, if cheap, higher - quantify the visible world/ship jitter in metres so the follow-up task can be scoped accurately from real numbers rather than a guess.
4. Run `package/tools/smoke.sh` and `make test-san` one final time, confirm both green, and report full before/after numbers for items 1-3 in your final report.

RULES: same as the original spec (docs/agent_specs/phase7_scale/w56_double_precision_position.txt) - read it again for the full rules/ownership/acceptance text, they still apply unchanged. DO NOT COMMIT. **IMPORTANT (new, after the crash): never run more than one `smoke.sh` / `make test-san` / real-GPU game-window process at a time on this machine - the crash that killed your previous terminal was very likely caused by concurrent heavy compiles/GL windows across worktrees (docs/WORKFLOW.md "One build/smoke run at a time"). You are the only worker running right now, so this should not be an issue, but do not launch multiple background smoke/test runs of your own concurrently either.** SCRATCH FILES only under logs/ with literal names.

OWNERSHIP: unchanged from the original spec.
