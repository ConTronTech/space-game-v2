TASK 3.5g - gravity gradient debugger (user request: "add a gravity debugger so we can see the gradient of gravity around the planet"). REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome; do not wait to finish everything before reporting.

CONTEXT: ship/gravity (docs/GRAVITY.md) already has a debug draw hook (`dbg_->drawHook("gravity.draw", ...)` in gravity.cpp) registered with core/debugger (docs/DEBUGGER.md, F6/F7/F8) that currently draws the dominant body's SOI circle, the ship's acceleration vector, and a forecast arc. The user wants to actually SEE the shape of the gravity field around a body - how strong the pull is at different distances/altitudes - not just the ship's own current acceleration.

DESIGN: extend the EXISTING gravity.draw hook (do not add a new keybind or a new debugger surface - F6/F7 already toggle this correctly) to optionally also draw a gradient visualization of the dominant body's field: a set of concentric rings (or radial tick marks) around the body at several distances, each labelled or coloured by the local gravity magnitude at that distance (`gravity::acceleration()`'s magnitude, which the module already computes via the pure `gravity_rules.h` functions - reuse them, do not duplicate the physics). A simple, cheap approach: pick N distances (e.g. body radius x [1.1, 1.5, 2, 3, 5, 8] or similar, clamped to the SOI), draw a ring at each (reuse the same circle-drawing approach as the existing SOI ring, fixed buffer, no allocation), colour each ring by its local g magnitude on a gradient (strong = red/hot, weak = blue/cool, or amber-to-blue - your call, pick something readable and consistent with the existing HUD gradient conventions if there's a natural fit) using a shared `core::IDebug` text label or the debugger's watch panel to show the actual numbers (e.g. "1.1x: 42.3 m/s^2, 2x: 12.8, 5x: 2.1..."). This only needs to be legible in the F6/F7 debug view, not polished HUD art - keep the drawing budget small (a handful of rings, not a dense field of arrows).

CHANGE:
1. Extend `ship/gravity`'s existing `draw()` method (or add a small sibling function called from the same drawHook) to optionally render the gradient rings described above, gated by a new tunable `gravity.debug_gradient_rings` (default true - it only draws anything when core/debugger's draw hooks are switched on via F7 anyway, so this is a cheap always-available extra, not a new toggle the user has to discover) and a ring count/distance-multiplier tunable if useful (your call on how much to expose vs hardcode reasonable defaults - keep it simple). Add a new debugger watch (e.g. `gravity.gradient`) showing the actual magnitude-at-distance numbers as a compact string, so the numbers are readable via F6's Watches tab even without screenshotting the 3D rings.
2. Make sure this works for whichever body is dominant (not just planets - moons and the sun should work too, reusing whatever `gravity::acceleration()` already needs). If no dominant body (deep space), the gradient hook should simply draw/report nothing (matches the existing pattern for the SOI circle already skipping in deep space - read gravity.cpp's `draw()` to match that convention).
3. Pure logic (the distances-to-sample and colour-gradient mapping) in gravity_rules.h if there is real logic worth extracting (e.g. a magnitude-to-colour lerp function, unit-tested at a few known inputs: strongest sampled distance maps to the "hot" end, weakest to the "cool" end, monotonic in between) - don't force it if it's trivial enough to inline, your call, but the actual gravity MAGNITUDE computation must reuse the existing `gravity::acceleration()` function, never reimplemented.
4. Tests: the colour/distance mapping function if extracted (tolerances, no exact float equality), a check that the sampled distances stay within the body's SOI (or are clamped), a regression check that this whole feature costs nothing when core/debugger's draw hooks are off (same "closed = zero cost" discipline docs/DEBUGGER.md already documents for every hook). Docs: docs/GRAVITY.md (a short "Gradient debugger" section: what it shows, the new tunable, an example of the watch panel string), docs/DEBUGGER.md (one line noting gravity's hook now includes the gradient rings).

RULES: read docs/WORKFLOW.md, docs/GRAVITY.md, docs/DEBUGGER.md, docs/PERFORMANCE.md (laptop is GPU fill-rate bound: keep this cheap, fixed small buffers, no allocation per frame). Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST). For any scripted scenario / screenshot run of the real game, use --display=1 --input-profile=testing (docs/WORKFLOW.md "Testing display + mouse capture"). Use saved games (docs/WARP.md method), --auto-gravity, --gravity-scenario=orbit|drop|deep (all already exist), F6/F7 via --ui-click or a scripted key send if the harness supports it (read docs/DEBUGGER.md for how prior debugger screenshots were captured), --ui-click. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

OWNERSHIP: package/modules/ship/gravity/** only (the existing module - this is an additive extension to its own draw hook, not a new module). tests (test_gravity.cpp additions). docs above. Do not edit core/debugger itself, ship/orbit_lock, ship/docking, or ship_core.

ACCEPTANCE: smoke + make test-san pass; a saved-game screenshot (docs/WARP.md method, F7 debug draws on, near a planet) showing the gradient rings around the dominant body alongside the existing SOI circle/acceleration vector/forecast arc (all should coexist cleanly, nothing overlapping unreadably); the F6 Watches tab showing the `gravity.gradient` numbers string (screenshot or logged text); confirm deep space (no dominant body) draws/shows nothing extra; confirm cost is unchanged with F7 off (compare the existing "closed = 0.24ms/frame" baseline from docs/DEBUGGER.md, or re-measure if that number has drifted).
