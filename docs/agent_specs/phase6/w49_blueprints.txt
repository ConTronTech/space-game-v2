TASK 6.2 - gameplay/blueprints: unlocks that gate crafting (docs/GAME_LOOPS.md item 2, docs/ROADMAP.md Phase 6). REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome; do not wait to finish everything before reporting.

DESIGN (read docs/ANOMALIES.md and docs/CRAFTING.md first - this reuses both): some recipes require a blueprint the player does not start with. Blueprints are unlocked by EXPLORATION (an anomaly investigation can grant one), NOT bought or sold - the user has explicitly withheld a station-economy design (docs/ROADMAP.md "Parked ideas": "do not design/build station trading, prices, contracts"), so blueprints must stay entirely outside any buy/sell mechanic. This also gives world/anomalies (just merged) a second reward type beyond ore, and reuses the same "optional service, no crash if absent" pattern used everywhere in this codebase.

RULES: read docs/WORKFLOW.md, docs/ANOMALIES.md, docs/CRAFTING.md, docs/INVENTORY.md, docs/TOAST.md, docs/DATA.md, docs/SAVES.md, docs/GAME_MENU.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST). For any scripted scenario / screenshot run of the real game, use --display=1 --input-profile=testing (docs/WORKFLOW.md "Testing display + mouse capture"). Use saved games (docs/WARP.md method), --give=..., --give-anomaly-scanner, --anomaly-dump, --ui-click. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

CHANGE:
1. New module package/modules/gameplay/blueprints/ (name "gameplay/blueprints"; optional core/save_system). Provides gameplay::IBlueprints in blueprints_api.h: `bool unlocked(const std::string& id) const`, `bool unlock(const std::string& id)` (returns false if already unlocked), `void list(std::vector<std::string>& out) const`. A simple persisted set (ISaveable id "gameplay/blueprints", saving the unlocked ids). Pure logic in blueprints_rules.h if there is any real logic beyond a set (there may not be much - a thin module is fine, do not force complexity in). Event `gameplay::BlueprintUnlocked{ id, name }`. Dev flag --give-blueprint=ID (test flag).
2. CRAFTING GATE: extend the recipe format with an OPTIONAL field "requires_blueprint": "<blueprint id>" in data/recipes.json. Extend gameplay::Recipe (crafting_rules.h/crafting_data.h parsing) to carry it, and gameplay::decideCraft to check it: if set and gameplay::IBlueprints is present and NOT unlocked(id), refuse with reason "blueprint required: <name>" (pick where the blueprint's display name comes from - a small data/blueprints.json with id->name, or just title-case the id if that is simpler; your call, document it). If IBlueprints is absent, treat the gate as if it does not exist (do not block crafting just because the module is not loaded - matches the "optional absent service = no restriction added" convention already used elsewhere in this codebase, e.g. crafting.require_dock's own docking check). This mirrors the SAME pattern the anomaly_scanner crafting extension just used (crafting_rules.h/crafting_data.h/crafting.cpp) - read that diff's shape in git log if useful, but re-derive it cleanly for this feature rather than copy-pasting names. GATE 1-2 existing "advanced" recipes only (e.g. Emergency Beacon and/or Missile Pack are reasonable candidates - your call) - do NOT gate basic survival recipes (Warp Fuel Cell, Fuel Canister, Repair Kit, Hull Plating) since gating those risks a genuine softlock for a new player with no blueprint yet.
3. ANOMALY REWARD: extend world/anomalies (docs/ANOMALIES.md) so an anomaly KIND in data/anomalies.json can OPTIONALLY grant a blueprint unlock instead of (or alongside) ore - add a new anomaly kind (e.g. "ancient_blueprint_cache") whose reward is a specific blueprint id matching what you gated in item 2, so investigating that kind of site is how a player actually gets access to the gated recipe. world/anomalies takes an OPTIONAL dependency on gameplay/blueprints (does not crash if absent; if absent, that anomaly kind's blueprint reward is simply not granted, same "optional service" discipline). When a blueprint is granted, show a toast (ui::IToast, optional) distinct from the ore-pickup toast - "Blueprint unlocked: <name>" - and log it. This is a NARROW, additive extension to a module you do not otherwise own: report the exact lines touched, and if the reward-type plumbing turns out to be more invasive than a small addition, stop and report "needs decision" instead of restructuring anomalies.
4. UI: the existing CRAFTING tab (ui/game_menu, unowned by you) ALREADY shows each recipe's craftable state + refusal reason per-recipe (docs/CRAFTING.md; do not edit game_menu or crafting's tab-drawing code beyond what item 2 needs) - confirm in your testing that the new "blueprint required: X" reason shows there for free, with no UI module changes needed. If it does NOT show correctly, report exactly what is missing as "needs decision" rather than editing files outside your ownership.
5. Tests (tolerances where relevant): the blueprints set (unlock/unlocked/list, unlocking twice returns false the second time, save/load round trip), the crafting gate (locked recipe refused with the right reason, unlocked recipe crafts normally, the gate has no effect when IBlueprints is absent - test this explicitly since it is an important "does not softlock" guarantee), data parsing (a recipe with/without requires_blueprint, sanitising a malformed value), the anomaly reward extension (a blueprint-reward kind actually calls unlock when the service exists, does nothing without it, still investigates/removes the site either way). Docs: docs/BLUEPRINTS.md (new: the whole design, how a recipe gets gated, how an anomaly grants one, save format, how to test), docs/CRAFTING.md (the new refusal reason + field), docs/ANOMALIES.md (the new reward type), docs/MODULES.md, docs/SAVES.md, docs/DATA.md, docs/GAME_LOOPS.md (mark item 2 built once verified - a one-line status edit).

OWNERSHIP: package/modules/gameplay/blueprints/** (new), package/modules/gameplay/crafting/{crafting.cpp,crafting_data.h,crafting_rules.h} (the requires_blueprint gate only), package/modules/world/anomalies/** (the blueprint reward extension only - narrow, additive), data/recipes.json (add requires_blueprint to 1-2 existing entries only), data/anomalies.json (add the one new kind only), data/blueprints.json (new, only if you chose that approach for display names), tests (test_blueprints.cpp new, globbed; minimal additions to test_crafting.cpp / test_anomalies.cpp), docs above (docs/GAME_LOOPS.md: only the one-line status edit). Do not edit ui/game_menu, ui/toast, ui/system_map, gameplay/inventory, ship/cockpit, or ship_core.

ACCEPTANCE: smoke + make test-san pass; saved-game scenarios (docs/WARP.md method; --give=... for ingredients, --give-anomaly-scanner + --anomaly-dump to find the blueprint-reward site, or --give-blueprint=ID to skip straight to testing the crafting gate): WITHOUT the blueprint, the gated recipe shows "blueprint required: X" in the CRAFTING tab and craft() refuses with that reason (screenshot); investigate the blueprint-reward anomaly (log: BlueprintUnlocked, a toast screenshot distinct from the ore-pickup one); WITH the blueprint unlocked, the same recipe now crafts normally (screenshot/log); save mid-game after unlocking, reload, confirm it stays unlocked; confirm an ungated recipe (e.g. Repair Kit) is completely unaffected throughout; confirm nothing crashes with gameplay/blueprints disabled (--disable=gameplay/blueprints) - crafting must NOT be blocked in that case.
