TASK 3.3c - raise planet mesh detail ceiling (user request: "the quality of the planits are low for ultra we need more detail"). REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome; do not wait to finish everything before reporting.

FOUND ROOT CAUSE (already confirmed, not a hypothesis): package/modules/world/star_system/planet_mesh.h line ~16 has `constexpr int kMaxMeshLevel = 4;` - a HARD ceiling on icosphere subdivision that clamps `level`/`maxLevel` regardless of quality preset. core/quality/quality_rules.h's preset table (Low/Medium/High/Ultra columns) already has `world.planet_max_lod` = {2, 3, 3, 4} and `world.planet_triangle_budget` = {8000, 20000, 40000, 80000}. Level 4 = 20*4^4 = 5120 triangles per planet - Ultra's triangle_budget of 80000 has huge unused headroom above that, so the BUDGET was never the real bottleneck; the hard `kMaxMeshLevel=4` code ceiling is. `world.planet_lod_edge_px` = {20, 12, 8, 6} (smaller = demands more detail per screen pixel, drives when the LOD system reaches for a higher level).

DESIGN: raise the hard ceiling and Ultra's own preset values so Ultra can actually render meaningfully more detailed planets, WITHOUT touching Low/Medium/High (the laptop-facing presets must render identically to today - same triangle counts, same LOD behaviour, verify this explicitly). This is purely a "let the top preset reach higher" change, not a global increase.

CHANGE:
1. In package/modules/world/star_system/planet_mesh.h: raise `kMaxMeshLevel` from 4 to a higher value you settle on by MEASURING actual mesh-build cost per level on this machine first (build time to generate one planet's mesh at levels 5 and 6; level 5 = 20480 triangles, level 6 = 81920 triangles) - the existing lazy one-level-per-frame(ish) build discipline (read planet_mesh.h/star_system.cpp's build scheduling - do not break it) must still keep the game responsive when a planet's LOD needs to climb, so pick the new ceiling based on real numbers, not a guess. Report the measured build times for levels 4/5/6 and justify your final choice (5 or 6) in the report.
2. In package/modules/core/quality/quality_rules.h: raise ONLY the Ultra column of `world.planet_max_lod` to match your new ceiling, raise Ultra's `world.planet_triangle_budget` enough that it is not an artificial second bottleneck (Ultra's column only - do not touch Low/Medium/High numbers in any of these three rows), and if needed lower Ultra's `world.planet_lod_edge_px` so the LOD system actually requests the new higher level when the camera is reasonably close (rather than the budget/edge settings preventing it from ever being reached in practice - verify empirically that flying close to a planet at --quality=ultra actually reaches the new max level, not just that the config permits it).
3. Verify explicitly (and report) that Low/Medium/High preset planet rendering is BYTE-FOR-BYTE unchanged in triangle count/LOD selection behaviour - run the existing test suite plus a manual close-approach check at each of the three lower presets and confirm the same level is reached as before your change (this is the single most important regression to avoid: do not accidentally make Low/Medium/High more expensive, the laptop depends on them staying exactly as they are - docs/PERFORMANCE.md).
4. Optional, lower priority, only if cheap: consider whether `world.terrain_height` (the relief amplitude tunable applied on top of the fbm noise in planet_mesh.h's height function) is worth a modest bump for Ultra specifically now that there are enough triangles to actually resolve more relief - your call, document reasoning either way; do NOT change it for other presets, and do not over-do it (should look like sensible geological relief, not spiky/unrealistic terrain - if you're not confident it reads well, skip this item and just report why).
5. Tests: any existing planet_mesh/quality tests that assert the old kMaxMeshLevel=4 / old preset numbers need updating to match; add a regression test asserting Low/Medium/High preset rows are unchanged, and a new one confirming Ultra's row now permits (and, via a build/selection check if feasible in a unit test, actually produces) more detail than before. Docs: docs/QUALITY.md (the new Ultra numbers, why raised, measured cost), docs/WORLD.md (cross-reference), docs/PERFORMANCE.md (note the laptop/low-mid presets are explicitly unaffected).

RULES: read docs/WORKFLOW.md, docs/QUALITY.md, docs/WORLD.md, docs/PERFORMANCE.md. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST). For any scripted scenario / screenshot run of the real game, use --display=1 --input-profile=testing (docs/WORKFLOW.md "Testing display + mouse capture"). Use saved games (docs/WARP.md method), --quality=ultra/--quality=low etc, --ui-click. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

OWNERSHIP: package/modules/world/star_system/planet_mesh.h (the LOD ceiling constant + optional terrain_height for Ultra only), package/modules/core/quality/quality_rules.h (Ultra column of the three named rows only), tests (test_planet_mesh.cpp / test_quality.cpp additions), docs above. Do not edit world/atmosphere (a separate task owns it), ship/gravity, ship_core, or anything under ui/.

ACCEPTANCE: smoke + make test-san pass; measured build-time numbers for levels 4/5/6 justifying the chosen new ceiling; a saved-game screenshot comparison at --quality=ultra close to a planet before vs after (visibly more geometric detail - describe the triangle count reached) and confirmation the same close-approach at --quality=low/medium/high reaches the SAME level/triangle count as before your change (explicit before/after numbers for all four presets, not just Ultra). No regression in package/tools/smoke.sh performance checks.
