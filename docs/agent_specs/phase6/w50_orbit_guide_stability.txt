TASK 3.5e - BUG (user, with screenshot): the orbit guide ring "tweaks out" - it is not a smooth, stable curve; any slight movement makes lines "recast" (visibly reorient/flicker), and the colour is a hard snap between amber (misaligned) and green (aligned) instead of a smooth gradient as alignment improves. REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome; do not wait to finish everything before reporting.

LIKELY ROOT CAUSE (verify first, report your actual finding in the first heartbeat - this is a hypothesis, not confirmed): package/modules/ship/orbit_lock/orbit_lock.cpp's `probe()` calls `orbit::makeOrbit(rel, relVel, forward, mu, fallback)` EVERY FRAME from the ship's raw, instantaneous current velocity (`ship->velocity()`), which derives the guide's orbit PLANE (its normal vector) from that velocity direction (`guidePlaneNormal` in orbit_lock_rules.h). Since `ship/gravity` is now ON BY DEFAULT (docs/GRAVITY.md, merged this session), the ship's velocity is being continuously perturbed by gravitational acceleration EVERY FIXED STEP even while coasting with no player input at all - so the plane normal, and therefore the ring's orientation, is being freshly recomputed from a constantly-changing velocity vector every render frame. This would produce exactly the "any slight movement recasts the lines" symptom the user describes, and would have gotten worse/become visible only after gravity shipped (it may have looked stable before gravity existed, when velocity only changed from deliberate thrust). Confirm by logging the guide plane normal (or ring vertex 0's position) frame-to-frame while coasting near a body with `ship.gravity_enabled` on vs `--no-gravity`, with ZERO player input, and see whether it visibly moves in the `on` case.

RULES: read docs/WORKFLOW.md, docs/ORBIT_LOCK.md, docs/GRAVITY.md, docs/HUD.md, docs/PERFORMANCE.md (laptop is GPU fill-rate bound; the ring is already a single line-strip draw, keep it that way). Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST). For any scripted scenario / screenshot run of the real game, use --display=1 --input-profile=testing (docs/WORKFLOW.md "Testing display + mouse capture"). Use saved games (docs/WARP.md method), --auto-orbit-lock, --no-gravity, --gravity-scenario (all already exist), --ui-click. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

CHANGE:
1. STABILITY FIX: the guide ring must be visually smooth and stable under normal flight/gravity, updating its shape/orientation only as fast as the ship's TRUE orbital state is meaningfully changing, not flickering from noise in the raw instantaneous velocity every frame. Do this WITHOUT breaking the actual alignment CHECK's correctness (the gating logic that decides whether O can lock must still react promptly and accurately to real player intent - do not make the LOCK decision laggy or wrong, only stabilise the DISPLAYED ring). Reasonable approaches (pick the one that fits the code cleanly, explain your choice): (a) low-pass filter / exponential smoothing of the relative velocity direction used ONLY for `guidePlaneNormal`'s input when computing what to DRAW (a separate smoothed state kept in the module, decayed each frame, snapping to the raw value quickly when it changes a lot so it does not lag behind real manoeuvres - only damps small/fast noise); (b) recompute the plane less often (e.g. only when it has drifted more than a small angular threshold from the currently-drawn plane) with a short SLERP-style blend to the new orientation over a few frames instead of an instant cut; (c) something better you find while investigating - your call, but explain the trade-off and prove it in a screenshot/log (report the raw vs smoothed plane normal's frame-to-frame variance, before and after, while coasting under gravity). Whatever you do, add pure, unit-tested logic in orbit_lock_rules.h (no GL/SDL/engine types): the smoothing/threshold function itself, tested for: converges toward a step change over a bounded number of frames, dampens small per-frame noise close to zero net drift over many frames of small random jitter, and does not introduce NaN/instability.
2. COLOUR GRADIENT: replace the current hard snap (`const bool ok = align_.aligned(); const float cr = ok ? 0.30f : 1.00f, cg = ok ? 1.00f : 0.75f, cb = ok ? 0.45f : 0.20f;` in drawGuide()) with a smooth amber-to-green gradient driven by a continuous 0..1 "closeness to aligned" score. `orbit_lock_rules.h`'s `settleSecondsFor` ALREADY computes exactly this kind of normalized error (`eh`, `es`, clamped to `e` in 0..1) from heading/speed error against tolerance - pull that pattern out into its own small reusable pure function (e.g. `alignmentCloseness(const Alignment&, const Tolerances&) -> double` returning 0 = perfectly aligned, 1 = at or beyond the tolerance edge) and have BOTH `settleSecondsFor` and the new gradient colour use it (do not duplicate the maths twice). Lerp amber (1.00, 0.75, 0.20) at closeness 1.0 to green (0.30, 1.00, 0.45) at closeness 0.0, smoothstep the blend (reuse the existing `smoothstep01` helper already in the file) so it feels like a natural gradient rather than a linear ramp. The ring, the arc-ahead brighter segment, and any other guide-coloured element should all use the SAME closeness value for one consistent gradient (check drawGuide() for anywhere else `ok` currently branches colour, e.g. around line ~262 and the second `bool ok = align_.aligned();` near line ~338 - keep them consistent, do not diverge). Keep the "latched aligned" HUD text/lock-acceptance logic (align_.aligned(), the boolean gate) UNCHANGED - only the ring's/arc's VISUAL colour becomes a gradient, the actual pass/fail decision for pressing O stays the existing binary+hysteresis logic (docs/ORBIT_LOCK.md), which already works correctly and should not be touched.
3. Tests (tolerances, no exact float equality): `alignmentCloseness` at several error levels (0 at perfect alignment, 1 at the tolerance edge, in between scales sensibly, clamps beyond 1), the smoothing/threshold function from item 1 (as described above), a regression check that `settleSecondsFor`'s behaviour is unchanged after refactoring to share the new helper (same output for the same inputs it had before). Docs: docs/ORBIT_LOCK.md (the gradient, the stability fix and why it was needed - mention the gravity interaction plainly since that is likely the real cause), docs/GRAVITY.md (one line cross-reference if relevant).

OWNERSHIP: package/modules/ship/orbit_lock/** only. tests (test_orbit_guide.cpp additions). docs above. Do not edit ship/gravity, ship_core, ship_hud, or cockpit.

ACCEPTANCE: smoke + make test-san pass; state your CONFIRMED root cause (not just the hypothesis above - say what you actually found) with evidence (a log of frame-to-frame plane-normal variance while coasting under gravity, before vs after your fix); saved-game screenshots (docs/WARP.md method) of: the ring visibly stable over several seconds of coasting near a body with gravity on and zero player input (a short sequence of screenshots or a described log showing near-zero ring movement, vs the old behaviour if you can capture it for comparison), the colour gradient at a few different alignment states (fully misaligned/amber, partially aligned/blended, fully aligned/green) - describe or screenshot each; confirm locking (pressing O) still works exactly as before (same tolerances, same refusal reasons, same settle behaviour) - this must not regress.
