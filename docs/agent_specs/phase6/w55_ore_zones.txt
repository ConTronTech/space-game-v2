TASK 6.3 - ore tiers by zone (docs/GAME_LOOPS.md item 3, "mostly data"). REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome; do not wait to finish everything before reporting.

DESIGN: today every asteroid (belt or cluster, anywhere in the system) picks its ore from the SAME single global weight table (`data/ores.json`'s `rarity` field, via `world::OreTable`/`pickOre` in asteroid_rules.h). Make ore distribution vary by distance from the sun, so exploring farther out (or a specific belt/cluster) is meaningfully different from mining right next to the start - rarer/valuable ores (platinum, uranium, crystal) should skew more common farther from the sun, common ones (iron, copper) should skew more common close in. This is deliberately DATA-DRIVEN (a new small JSON file with per-zone weight multipliers), not a new mechanic or UI - the existing mining/cargo/crafting loop is completely unaffected, only WHICH ore an asteroid is likely to contain changes.

CHANGE:
1. New data file `data/ore_zones.json`: an ordered list of distance bands from the sun (e.g. "inner"/"mid"/"outer"/"deep" or similar - your naming), each with `min_distance`/`max_distance` (world units, matching the same distance metric `generateField` already uses: belts use their orbit-gap midpoint, clusters use the target body's distance from the sun) and a `weight_multiplier` map of `{ore_id: multiplier}` (missing ore id = multiplier 1.0, i.e. unaffected). Pick sensible tier boundaries relative to this seed's actual planet orbits (read docs/WORLD.md / docs/GRAVITY.md's seed-1234 numbers, or query the running game, to ground real distances rather than guessing) and multipliers that make a real, noticeable difference without making any ore literally unobtainable in any zone (every ore should still have SOME non-zero chance everywhere, just a different likelihood - a hard 0 multiplier risks a real "I can't find X" bug report). Document your reasoning in docs/WORLD.md.
2. Extend `world::OreTable` (asteroid_rules.h) or add a sibling structure to carry the zone table, and extend the ore SELECTION logic so each BELT (by its orbit-gap midpoint distance from the sun) and each CLUSTER (by its target body's distance from the sun) resolves to a zone and uses that zone's ADJUSTED weights (base `rarity` from ores.json x that zone's multiplier per ore, renormalized) instead of the single global table - reuse the existing `pickOre` function, just feed it zone-adjusted weights instead of the raw global ones. No zone data present (file absent/empty): fall back EXACTLY to today's single global table behaviour (regression-tested). A distance outside every listed zone's range: fall back to multiplier 1.0 for every ore (unaffected), not a crash or an empty table.
3. Wire it in `asteroids.cpp`: load `data/ore_zones.json` via `core::IData` (same optional-service pattern already used for `data/ores.json` - read the existing loading code around line 43-52 and follow it), pass the parsed zone table into `generateField` (or compute per-belt/per-cluster adjusted `OreTable`s before calling `pushAsteroid`, whichever fits the existing structure more cleanly - your call, but do not change `pushAsteroid`'s signature/behaviour itself if you can avoid it, just feed it the right per-zone weights).
4. Pure logic (no GL/SDL/engine types) in asteroid_rules.h: the zone-lookup-by-distance function, the weight-adjustment (base x multiplier, renormalized, clamped non-negative) function, unit-tested: a distance inside a zone gets that zone's multipliers applied correctly; a distance in no zone falls back to 1.0 for everything; an ore with a very low but non-zero multiplier still has SOME chance (never exactly 0 unless the data explicitly sets 0, which you should avoid using in the shipped data per item 1); the existing global-table behaviour is exactly unchanged when the zone file is absent/empty (regression test against the current `pickOre`/`OreTable` behaviour). Docs: docs/WORLD.md (the zone tiers, boundaries, multipliers, reasoning - e.g. "platinum is 3x more likely past the belt than near the sun"), docs/DATA.md (the new file format).
5. Verify in-game (saved-game scenario, docs/WARP.md method, `--give=...`/mining/`--anomaly-dump`-style dev flags as needed, or a dev flag you add if useful like `--ore-zone-dump` printing the resolved zone + adjusted weights for every belt/cluster at startup) that the distribution actually shifts: sample a reasonable number of asteroids in an inner belt/cluster vs an outer one and report the resulting ore-id histograms (not just "the code looks right" - show it produces a real difference, e.g. "inner cluster: 61% iron, 2% platinum; outer belt: 24% iron, 11% platinum").

RULES: read docs/WORKFLOW.md, docs/WORLD.md, docs/DATA.md, docs/GRAVITY.md (for this seed's real distances). Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST). For any scripted scenario / screenshot run of the real game, use --display=1 --input-profile=testing (docs/WORKFLOW.md "Testing display + mouse capture"). Use saved games (docs/WARP.md method), --ui-click. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

OWNERSHIP: data/ore_zones.json (new), package/modules/world/asteroids/{asteroid_rules.h,asteroids.cpp} (the ore-zone lookup/application only - do not touch belt/cluster placement, LOD, physics pool, or drawing code), tests (test_asteroids.cpp additions), docs above. Do not edit gameplay/mining, gameplay/inventory, gameplay/crafting, or data/ores.json's existing fields (adding new files is fine; do not change existing ore rarity/color/cargo_cap values - that would be a separate, unrelated balance change).

ACCEPTANCE: smoke + make test-san pass; the zone table and reasoning documented in docs/WORLD.md; the in-game histogram comparison from item 5 showing a real, meaningful shift in ore distribution between at least two zones; confirm every ore still has a non-zero chance in every zone (no accidental hard lockout); confirm the exact old global-table behaviour when `data/ore_zones.json` is missing/disabled (regression test, and `--disable`-style check if there's a clean way to simulate "file absent" without deleting the shipped file - your call, could just be a unit test against an empty zone table).
