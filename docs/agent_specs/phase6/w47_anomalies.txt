TASK 6.1 - gameplay/anomalies + scanner: the "what's over there?" hook (docs/GAME_LOOPS.md item 1, docs/ROADMAP.md Phase 6). REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome; do not wait to finish everything before reporting.

DESIGN (read docs/GAME_LOOPS.md first for the framing): seeded points of interest ("anomalies") scattered through the star system, invisible until the player crafts and uses an Anomaly Scanner item (a PERMANENT perk, exactly the same pattern as the existing Ore Scanner: docs/CRAFTING.md, docs/MINING.md "Ore Scanner", gameplay::IInventory::hasPerk/addPerk). Once the perk is set, anomalies within a detection range show as a marker on the cockpit radar (and, if time allows, the system map). Flying within a small radius of one INVESTIGATES it: one-time, consumes/removes it, gives the player ore and a short flavour-text discovery message via the newly-built ui/toast module (docs/TOAST.md) - the FIRST thing wired to toast; nothing else uses it yet, so this is a clean pilot with no risk to existing banners. Investigated anomalies are remembered across save/load (do not respawn).

RULES: read docs/WORKFLOW.md, docs/WORLD.md, docs/MINING.md, docs/CRAFTING.md, docs/INVENTORY.md, docs/TOAST.md, docs/COCKPIT.md (radar), docs/SAVES.md, docs/DATA.md, docs/PERFORMANCE.md (laptop is GPU fill-rate bound: tiny draw calls, no per-frame allocation). Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST). For any scripted scenario / screenshot run of the real game, use --display=1 --input-profile=testing (docs/WORKFLOW.md "Testing display + mouse capture"). Use saved games (docs/WARP.md method), --give=..., --ui-click, dev flags. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

CHANGE:
1. New module package/modules/world/anomalies/ (name "world/anomalies"; requires world/star_system for seeded positions; optional gameplay/inventory, ui/toast, ship/ship_core, core/save_system - must work with any of them absent, never crash, just do less). Seeded generation from world.seed (reuse the SAME seed the star system uses, so a given seed always gives the same anomaly layout): a handful of sites (tunable anomalies.count, default 6-10) placed at varied, interesting spots - some in deep space between planets, some near a planet/moon (but not overlapping a body's own radius or a station), some near/in the asteroid belt if one exists (world::IAsteroids, optional). Each site has: a position (double, world frame), a kind (an id into data/anomalies.json - see item 3), and a stable id (index is fine, since the seed makes generation deterministic).
2. SCANNER ITEM: a new item "anomaly_scanner" in data/items.json (permanent: true, effect: { "anomaly_scan": true } or similar - pick a clear effect key name and document it) and a matching recipe in data/recipes.json (pick sensible ore ingredients, similar cost tier to Ore Scanner). Extend crafting's effect interpretation MINIMALLY to call gameplay::IInventory::addPerk("anomaly_scanner") for this new effect key, mirroring exactly how hud_ore_labels/ore_scanner already works (find that exact code path in gameplay/crafting and add one matching case - this is the one narrow edit you are allowed in a file you do not otherwise own; report the exact lines). If you cannot find or safely extend that one code path, STOP and report it as "needs decision" instead of guessing.
3. DATA: data/anomalies.json (new) - a small table of anomaly KINDS (2-4 to start: e.g. "derelict_wreckage", "energy_signature", "ancient_artifact", "unstable_phenomenon"), each with a name, a short flavour-text discovery message (or 2-3 variants to pick from with the seeded RNG, for a little variety), an ore reward table (which ore id(s) and an amount range, using existing ids from data/ores.json), and a detection-range multiplier (some kinds are "louder"/easier to detect than others - keep this simple, a single float per kind is enough). Sites generated in item 1 pick a kind (weighted or uniform, your call, document it) from this table.
4. DETECTION + INVESTIGATION (pure logic in anomaly_rules.h, no GL/SDL/engine types, unit-tested): with the anomaly_scanner perk set, a site is "detected" (shown) when the ship is within anomalies.detect_range (default a few thousand units, scaled by the kind's detection multiplier) of it; "investigated" (triggers the reward, once) when within anomalies.investigate_range (default ~50-100 units, much smaller than detect range - the player has to actually fly there, not just scan from afar: keep this a real "hard" traversal, not a freebie). Without the perk, nothing is detected or investigated (sites are inert). Reward: pick an ore + amount from the site's kind (seeded per-site so it is deterministic, not re-rolled every frame), add it to gameplay::IInventory (optional; if absent, still mark investigated but log a warning that nothing was given), emit an event world::AnomalyInvestigated{ siteId, kind, oreGiven, amount } and, if ui::IToast is present, show a toast with the flavour text + "+N <ORE>" (Info level; a slightly longer duration than the default is fine for a flavour-text moment, your call). Investigated sites are removed from detection/investigation (one-time) and persist as investigated across save/load (a small saved bitset/id-list under a new ISaveable id "world/anomalies"; on load, re-generate the deterministic site list from the seed then mark the saved ids as already investigated - do NOT need to save positions, only which ids are done).
5. RENDERING/RADAR: draw detected-but-not-investigated anomalies on the cockpit PROXIMITY_RADAR as a small distinct marker (a different shape/colour from the existing body/station/asteroid markers - e.g. a pulsing star or diamond outline in a distinctive colour such as violet/magenta, so it reads as "unusual" at a glance), with the SAME log-range/height-stem conventions the radar already uses for bodies (read radar_map.h for the pattern, but DO NOT duplicate its whole formula set blindly - reuse what makes sense, keep it simple, a flat dot with no stem is acceptable for a first pass if time-constrained, say so). No anomalies visible without the perk (the radar shows nothing extra, exactly like today). Keep this cheap: a handful of sites at most, one small batched draw, no per-frame allocation.
6. Tunables: anomalies.enabled (default true), anomalies.count, anomalies.detect_range, anomalies.investigate_range. Dev flags: --give-anomaly-scanner (test flag: sets the perk directly without crafting, for scripted scenarios), --anomaly-dump (logs every generated site's position/kind at startup, for finding one to fly to in a test).
7. Tests (tolerances, no exact float equality): the seeded site generation (deterministic for a given seed, sites do not overlap known bodies/stations within some margin - a best-effort check, not perfect collision avoidance), detect-range and investigate-range math (with the kind multiplier), the reward selection (deterministic per site, within the kind's declared range), the investigated-state save/restore round trip (a pure function operating on ids, not the whole save system), data parsing of anomalies.json (defaults/sanitising if a field is missing, matching the style of test_weapons.cpp's "weapons_data_parsing_defaults_and_sanitising" test). Docs: docs/ANOMALIES.md (new: the whole design, data format, tunables, save format, how to test), docs/CRAFTING.md (the new item), docs/MINING.md or docs/WORLD.md (cross-reference), docs/COCKPIT.md (the new radar marker), docs/TOAST.md (note: this is the first real caller), docs/MODULES.md, docs/SAVES.md, docs/DATA.md (data/anomalies.json format), docs/GAME_LOOPS.md (mark item 1 as built once verified - a one-line status edit).

OWNERSHIP: package/modules/world/anomalies/** (new), data/anomalies.json (new), data/items.json (add the ONE new item only), data/recipes.json (add the ONE new recipe only), package/modules/gameplay/crafting/crafting.cpp (ONLY the one-line effect-interpretation addition described in item 2 - report the exact lines touched), package/modules/ship/cockpit/** (radar marker addition only - if it risks conflicting with other radar code, report instead of guessing), tests (test_anomalies.cpp new, globbed), docs above (docs/GAME_LOOPS.md: only the one-line status edit). Do not edit ui/toast, gameplay/inventory (beyond calling its existing hasPerk/addPerk/add), ship_hud, star_system, or asteroids beyond read-only queries.

ACCEPTANCE: smoke + make test-san pass; saved-game scenarios (docs/WARP.md method; --give-anomaly-scanner to skip crafting for the test, or craft+use it via the menu flow like the ore scanner precedent; --anomaly-dump to find a site) showing: WITHOUT the scanner perk, flying near a known site shows nothing on radar and does not investigate; WITH the perk, the site appears on radar within detect range (screenshot), and flying within investigate range triggers the reward (log: AnomalyInvestigated, ore added, a toast screenshot with the flavour text), the site then gone from radar and not re-triggerable; save mid-game after investigating one, reload, confirm it stays investigated and is not re-offered. CPU/draw cost of the radar markers (cheap, report the number).
