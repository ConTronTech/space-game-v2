TASK 3.5f - BUG/UX (user, after playtesting gravity + planetary stations): landing on a planetary station is "nearly impossible" because gravity fights the player during the final approach - the user LIKES gravity existing generally, just wants it easier specifically when trying to land. REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome; do not wait to finish everything before reporting.

CONTEXT/DESIGN: ship/gravity (docs/GRAVITY.md) already skips itself entirely while ship::IDocking::busy() is true (approaching the pad or docked) - that part already works. The gap is BEFORE that: the player has to manually fly up to and line up with a station's landing pad while STILL under full gravity, and a planetary station sits right at a body's surface where local gravity is strongest (an orbital station is farther out where gravity is weak, so this mostly matters for planetary ones, but the fix below should be general - it naturally has little effect where gravity is already weak). Fix: fade gravity's magnitude toward zero as the ship gets close to a station it is actually approaching (the SAME station `ship::IDocking::nearestDockable()` already reports, so this only affects the station you are actually near/heading for, not some other station in a different direction), so that by the time the ship is close enough to press G and start the docking approach, gravity has already backed off smoothly and the player is fighting less of it exactly when fine maneuvering matters most. Do NOT remove gravity generally, do NOT change gravity anywhere else in the game (flying past/near a station without intent to dock, or near a planet with no station, is unaffected).

RULES: read docs/WORKFLOW.md, docs/GRAVITY.md, docs/STATIONS.md, docs/ORBIT_LOCK.md (for the smoothing pattern precedent just added in Leap 49 - reuse the same kind of smoothstep/easing discipline where relevant). Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST). For any scripted scenario / screenshot run of the real game, use --display=1 --input-profile=testing (docs/WORKFLOW.md "Testing display + mouse capture"). Use saved games (docs/WARP.md method), --gravity-test, --auto-gravity, --no-gravity, --auto-dock (all already exist), --ui-click. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

CHANGE:
1. In ship/gravity (ship::IDocking is already an optional dependency there): each fixed step, before applying the gravity acceleration, check ship::IDocking::nearestDockable(name, distance, ok, reason) (optional; a null/absent IDocking = no change to gravity, exactly like today). If a candidate station is reported, compute a fade factor in 0..1: 1.0 (full gravity, no change) at distance >= gravity.dock_assist_range (a NEW tunable, default a few multiples of a typical dock zone - pick a sensible value and justify it, e.g. related to docking.prompt_range if that is readable/known, or just a standalone tunable in the same spirit), smoothly (reuse smoothstep01-style easing, matching the discipline docs/ORBIT_LOCK.md's guide code just used) fading DOWN to a MINIMUM fraction gravity.dock_assist_min (a NEW tunable, default something like 0.1-0.2, NOT necessarily all the way to 0.0 before docking.busy() takes over fully - your call on the exact curve/floor, document the reasoning; it must reach a low enough value that landing actually becomes easy, verified by the acceptance scenario below) as distance shrinks toward the station's own dock-zone radius (nearestDockable's `distance` is measured how docking.cpp/docs/STATIONS.md defines it - read that to use it correctly and consistently, do not reinvent the distance metric). Multiply the gravity acceleration by this factor (direction unchanged, only magnitude reduced) BEFORE applying it. This must compose smoothly with the EXISTING full skip once docking.busy() becomes true (the fade should already be near its floor by the time busy() kicks in, so there is no visible jump - if there would be a jump, adjust the fade curve/range so the transition is smooth, and say so).
2. Pure logic in gravity_rules.h (no GL/SDL/engine types), unit-tested with tolerances: the fade factor as a function of (distance, dockAssistRange, dockAssistMin) - full at/beyond range, at the floor at/inside the station's zone, smooth in between, clamped/NaN-safe for degenerate inputs (distance <= 0, range <= 0, etc.), and a regression check that with NO nearby station (ok=false or IDocking absent) the factor is exactly 1.0 (no behaviour change when not near a dockable station - this is important, do not accidentally soften gravity everywhere).
3. Tunables: gravity.dock_assist_range, gravity.dock_assist_min (both documented with your reasoning). Dev flag if useful for testing (e.g. --gravity-dock-assist-off to force the factor to 1.0 always, for an A/B comparison screenshot - optional, only if cheap to add). Docs: docs/GRAVITY.md (the new fade, why, the tunables, how it interacts with the existing full-skip-while-docking-busy behaviour), docs/STATIONS.md (one cross-reference line if useful).

OWNERSHIP: package/modules/ship/gravity/** only. tests (test_gravity.cpp additions). docs above. Do not edit ship/docking, ship/orbit_lock, world/stations, or ship_core.

ACCEPTANCE: smoke + make test-san pass; the CORE proof needed: a saved-game scenario near a PLANETARY station (docs/STATIONS.md has the seed_offset/default-seed pointers used by earlier station tasks) flying a slow, deliberate manual approach toward the pad (log the ship's speed/position and the gravity acceleration magnitude actually being applied over time as distance to the station shrinks, showing it visibly dropping well before docking.busy() would take over) - compare against the SAME approach with gravity.dock_assist_min effectively 1.0 (full gravity, i.e. the old behaviour) to show the difference is real and meaningful, not a rounding error; confirm flying near the SAME planet but NOT toward any station (or with no station nearby) shows gravity completely unaffected (factor stays 1.0); confirm the transition into docking.busy()'s existing full-skip is smooth (no sudden jump in applied acceleration at the moment G is pressed and the approach begins). Report the actual numbers (gravity magnitude at a few distances, before/after) so the improvement is verifiable, not just asserted.
