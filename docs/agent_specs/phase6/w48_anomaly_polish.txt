TASK 6.1b - anomaly radar polish (docs/DEVLOG.md Leap 46 "Not done" items). REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome; do not wait to finish everything before reporting.

CONTEXT: world/anomalies (docs/ANOMALIES.md) just merged. Its radar marker is flat (no height stem, unlike body/station markers which show up/down offset from the ship's plane - docs/COCKPIT.md radar section, radar_map.h), and there is no marker on the new system map (docs/SYSTEM_MAP.md, ui/system_map). Both were explicitly left as follow-ups, not bugs.

RULES: read docs/WORKFLOW.md, docs/ANOMALIES.md, docs/COCKPIT.md, docs/SYSTEM_MAP.md, docs/GAME_MENU.md, docs/PERFORMANCE.md (laptop is GPU fill-rate bound: tiny draw calls, no per-frame allocation). Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST). For any scripted scenario / screenshot run of the real game, use --display=1 --input-profile=testing (docs/WORKFLOW.md "Testing display + mouse capture"). Use saved games (docs/WARP.md method), --give-anomaly-scanner and --anomaly-dump (already exist), --ui-click. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Worktree is fast-forwarded to main; run package/tools/link_assets.sh.

CHANGE:
1. RADAR HEIGHT STEM: give the anomaly marker in package/modules/ship/cockpit/hud_screens.cpp (+ radar_map.h) the SAME height-stem treatment the body/station markers already have (a vertical line from the flat position to the true 3D position when it is meaningfully above/below the ship's orbital plane; read how bodies/stations already do it and reuse the SAME pure helper functions in radar_map.h rather than duplicating the math - this one, unlike the anomaly module itself, SHOULD reuse radar_map.h since it already lives in the file you own). Keep the violet pulsing-star look, just add the stem the same way other markers have one.
2. SYSTEM MAP MARKER: add a small marker for detected-but-not-investigated anomalies to ui/system_map (world::IAnomalies is optional - the map already handles other optional services being absent; follow that exact pattern). Reuse the map's existing log-radial projection and marker-drawing approach (system_map_rules.h) - do not need a stem/height on the flat 2D map, just a distinct small marker (matching the radar's violet star colour for consistency) at the anomaly's projected position. No anomalies shown without the scanner perk, exactly like the radar.
3. Tests (tolerances): the radar stem calculation reused/verified for an anomaly position (a regression-style test using the same helper as bodies/stations, not new logic), the system map anomaly marker projection (reuse the map's existing projection tests' pattern). Docs: docs/ANOMALIES.md (mark both follow-ups done), docs/COCKPIT.md, docs/SYSTEM_MAP.md.

OWNERSHIP: package/modules/ship/cockpit/** (radar stem for anomalies only), package/modules/ui/system_map/** (the anomaly marker only), tests (existing test_cockpit.cpp / test_system_map.cpp, minimal additions), docs above. Do not edit world/anomalies, gameplay/crafting, or ui/toast.

ACCEPTANCE: smoke + make test-san pass; saved-game screenshots (docs/WARP.md method, --give-anomaly-scanner, --anomaly-dump to find a site placed above/below the ship's plane) showing: the radar marker with a visible height stem, and the system map showing the same anomaly as a marker; confirm both still show nothing without the scanner perk and nothing crashes with world/anomalies disabled (--disable=world/anomalies).
