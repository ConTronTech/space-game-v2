TASK 5.6 - ui/system_map: a map tab in the game menu (docs/ROADMAP.md Phase 5.6). REPORT EARLY: send a short progress message (orca orchestration send, type heartbeat) after your first working step and after each item; partial results welcome. RULES: read docs/WORKFLOW.md and the docs named. Edit ONLY files under OWNERSHIP. DO NOT COMMIT; leave everything uncommitted. Run package/tools/smoke.sh and make test-san at the end. DO NOT inject keys/mouse into the desktop (no xdotool/XTEST). For any scripted scenario / screenshot run of the real game, use `--display=1 --input-profile=testing` (docs/WORKFLOW.md "Testing display + mouse capture"): this opens on a secondary monitor with mouse capture off, never the user's primary display/controller. --frames=N headless-only checks (no window content that matters) do not need it. Use saved games (docs/WARP.md method), --ui-click, dev flags. SCRATCH FILES only under logs/ with literal names; never rm with a variable path (it stalls the tool): list them in the report. Performance rules (docs/PERFORMANCE.md): the target laptop is GPU fill-rate bound; tiny draw calls only, no per-frame allocation. Worktree is fast-forwarded to main; run package/tools/link_assets.sh. Docs: docs/GAME_MENU.md, docs/WORLD.md, docs/STATIONS.md, docs/COCKPIT.md (the existing radar, for a consistent visual language), docs/GRAVITY.md.

CONTEXT: ui/game_menu (docs/GAME_MENU.md) already supports pluggable tabs via ui::IGameMenu::addTab(name, order, drawFn) - CARGO and CRAFTING are the two built-in tabs today. This task ADDS a MAP tab the same way, entirely additively (no edits to game_menu itself needed - if you find you truly need one, report it as "needs decision" instead of editing it).

DESIGN: new module package/modules/ui/system_map/ (name "ui/system_map"; requires ui/game_menu; optional world/star_system, world/stations, world/asteroids, ship/ship_core, ship/gravity - the map must work (show "no data") when any of these is absent, never crash). On load, call  (an order after CARGO/CRAFTING; read game_menu.cpp's existing tab order constants if any to pick a sensible number, do not need to edit that file).

CONTENT: a 2D top-down schematic of the star system (NOT a 3D render - this is a flat map, like the radar's logarithmic-range idea but zoomable/pannable and showing the WHOLE system, not just nearby contacts): the sun at the centre, planets as dots on their orbit circles (the orbit's actual radius, drawn as a thin ring - use world::IStarSystem::bodies() orbitRadius/period, and world::updatePositions-equivalent read-only data already exposed, do NOT recompute orbital mechanics yourselves, just read the existing positions), moons as smaller dots near their planet, stations as small diamond markers (matching the radar's diamond convention) near their parent body, asteroid belts as a faint shaded ring band (from world::IAsteroids if it exposes belt geometry, else approximate from the body list - check what is actually queryable and report what you used), the ship's own position as a bright marker, and (if ship/gravity is present) the ship's current SOI/dominant body highlighted. A simple pan (drag or arrow keys reusing ui_up/down/left/right) and zoom (mouse wheel or +/- keys) since the system spans a huge range of scales (a moon a few hundred units from its planet vs a planet 300,000+ units from the sun) - a logarithmic radial scale (reuse the SAME log-scale idea the cockpit radar already uses, but as a NEW pure function in this module's own header, do not include radar_map.h - keep modules decoupled, duplication of a small formula is fine and expected in this codebase's style) is likely necessary for the whole system to be visible and useful at once; document the scale choice. Clicking/selecting a body (optional, nice-to-have) shows its name/kind/distance in a small info line - skip if time-constrained and say so.

Pure logic in system_map_rules.h (no GL/SDL/engine types): the log-radial projection (a worked example test at a few real radii from docs/GRAVITY.md's SOI table), pan/zoom state clamping (never zoom/pan to something degenerate), body-to-marker-size/colour mapping (reuse simple, documented rules, not a copy of radar_map.h). Unit tests (tolerances).
Tunables: system_map.zoom_min/max, system_map.default_zoom. Docs: docs/SYSTEM_MAP.md (new), docs/GAME_MENU.md (the MAP tab), docs/MODULES.md.

OWNERSHIP: package/modules/ui/system_map/** (new), tests (test_system_map.cpp new, globbed), docs above. Do not edit game_menu, cockpit/radar_map.h, star_system, stations, or asteroids.

ACCEPTANCE: smoke + make test-san pass; screenshots (docs/WARP.md method, --open-menu=FRAME,MAP if game_menu's existing dev flag supports a tab argument, else --ui-click to the MAP tab) at the default zoom showing the whole seed-1234 system (sun, all 6 planets on their orbit rings, moons, at least one station diamond) and a zoomed-in view near a planet showing its moons clearly; describe what is visible and confirm nothing crashes when world/stations or ship/gravity is disabled (--disable=...).
