TASK 2.1 - ship/ship_core (Phase 2, Wave 1). You own the ship simulation.

READ FIRST: docs/WORKFLOW.md (rules, definition of done, report format, FIX LIST protocol), docs/MODULES.md, docs/ROADMAP.md (your step), and the docs of the services you use. Run package/tools/link_assets.sh once in your worktree. The old game (read-only reference) is at /home/contolis/Documents/Space-Game-V2/space-game/space-game. Do NOT edit docs/MODULES.md or docs/ROADMAP.md (the coordinator does); put your documentation in your own docs file. DO NOT COMMIT, amend, merge, rebase or push: a git hook refuses commits in agent worktrees. Leave every change uncommitted in your worktree; the coordinator reviews the diff and commits it after approval. Finish with package/tools/smoke.sh passing on the uncommitted tree, then send worker_done with: 3-sentence summary (built / verified and how / could NOT verify), files added/changed, any edits to shared files or the ship contract, and a "manual test checklist" for the human player.

TARGET: turn the demo module package/modules/gameplay/flight/flight.cpp into the real ship module package/modules/ship/ship_core/ (module name "ship/ship_core"). Use git mv so history follows. The contract you implement is package/modules/ship/ship_core/ship_api.h (ship::IShip, ShipStatus, events) - already committed; read it carefully. Provide it with eng.services.provide<ship::IShip>(this).

CHANGE:
1. Keep ALL current behavior: 6-axis Newtonian flight with eased controls, drift/max_speed/brake tunables, interpolation, core::ITransformSource pose, physics body + rocks + bounce, audio engine hum + impact thud, save/load of position/velocity/orientation, chase-view wireframe ship, starfield/rocks passes. Keep the existing config keys "flight.*" so current game.json overrides keep working.
2. Add ship stats per the old game (read include/ship/ship.h; do NOT port its structure, port the behavior): max HP 100, passive regen 0.2 HP/s (only while alive; frozen while paused since it runs in onFixedUpdate), disposable shield (max 200, absorbs damage first, no regen, gone when broken -> emits ShieldBroken, shieldInstalled=false), warp fuel storage (max 100; addWarpFuel/consumeWarpFuel; emit FuelEmpty when it reaches 0 from >0), maxHp bonus (addMaxHp, capped at 200 like the old game). Do NOT implement the warp drive itself or the respawn timer - those are separate Wave 2 modules; you only provide storage + respawn() (instant full reset at the spawn point/orientation, emits Respawned).
3. Collision damage: on core::Collided involving the ship, compute damage from the closing speed exactly like the old game's collision damage rules (read the function in include/ship/ship.h around "min 1 HP scratch"), call applyDamage(amount, sourceKind) with the other body's kind ("asteroid", ...). A hit below the scratch threshold still does 1 HP. Bounce behavior stays.
4. Death: when hp reaches 0 set alive=false, emit Died{cause}; while dead the ship ignores flight input (still coasts/does not crash), engine hum silent. kill(cause) = instant death (sun later).
5. REMOVE the HUD code from this module (the speed panel, control hints line and IMPACT flash): a parallel worker builds ui/ship_hud from ship::IShip + events. Keep publishing the events.
6. Make the stats save/load (extend the ISaveable payload; missing keys keep current values so old saves still load).
7. Put pure rules in a header with no SDL/GL dependency (e.g. ship_rules.h: damage-through-shield, regen, fuel clamps, collision-damage formula) so they are unit-testable; add package/tests/test_ship_rules.cpp (add the .cpp to TEST_SRCS in the Makefile only if it is not header-only). Numbers that a player may tweak are eng.config.get("ship.<key>", default, "doc") tunables (e.g. ship.max_hp, ship.hp_regen).
8. Docs: docs/SHIP.md describing the module, IShip usage for other modules, tunables, saved fields.

CONSTRAINTS: No globals, no printf (LOG_*), modules talk via interfaces/events. Do not change other modules' APIs. Do not touch ui/, core/ except tiny additive fixes you must report. ship_api.h: additive changes only, and report them.

OWNERSHIP: package/modules/ship/ship_core/**, package/modules/gameplay/flight (removed/moved), package/tests/test_ship_rules.cpp, Makefile TEST_SRCS line if needed, docs/SHIP.md, config/input/default.json only if you add an action.

OBSERVABLE ACCEPTANCE: (a) smoke.sh passes; new unit tests cover shield-then-hull damage, shield break event/flags, regen clamp, fuel consume/add clamps, collision damage formula, death; (b) in the running game a save file placing the ship 60 m from the first rock flying at it (see how I did this: config/game.json engine.log_level=debug prints rock positions) shows HP reduced by the expected amount and Collided still bounces; (c) save/load round-trips hp/shield/fuel; (d) game still starts with flight controls unchanged.
