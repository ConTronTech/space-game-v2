TASK 2.3 - ui/ship_hud (Phase 2, Wave 1). You own the flight HUD.

READ FIRST: docs/WORKFLOW.md (rules, definition of done, report format, FIX LIST protocol), docs/MODULES.md, docs/ROADMAP.md (your step), and the docs of the services you use. Run package/tools/link_assets.sh once in your worktree. The old game (read-only reference) is at /home/contolis/Documents/Space-Game-V2/space-game/space-game. Do NOT edit docs/MODULES.md or docs/ROADMAP.md (the coordinator does); put your documentation in your own docs file. DO NOT COMMIT, amend, merge, rebase or push: a git hook refuses commits in agent worktrees. Leave every change uncommitted in your worktree; the coordinator reviews the diff and commits it after approval. Finish with package/tools/smoke.sh passing on the uncommitted tree, then send worker_done with: 3-sentence summary (built / verified and how / could NOT verify), files added/changed, any edits to shared files or the ship contract, and a "manual test checklist" for the human player.

TARGET: new module package/modules/ui/ship_hud/ (module name "ui/ship_hud"), plus small ADDITIVE glass widgets in package/modules/core/ui_handler (e.g. a bar). Another worker is concurrently converting gameplay/flight into ship/ship_core and will REMOVE the old demo HUD from it; you build the replacement. Code against the contract package/modules/ship/ship_core/ship_api.h (ship::IShip, ShipStatus, DamageTaken, Died, ShieldBroken, FuelEmpty, Respawned) - it is committed on main. The real IShip implementation will only exist after the coordinator merges the other branch, so develop and verify against a fake.

CHANGE:
1. HUD panels in the existing glass style (read docs/UI.md, look at how the demo HUD in package/modules/gameplay/flight/flight.cpp draws its panels; match the look): speed readout; HP bar; shield bar (only when shieldInstalled); warp fuel bar; centre crosshair; controls hint line (move the text from the demo HUD; keep the current key list, add "V camera"); IMPACT flash driven by DamageTaken (amount + source), fading over ~0.7 s.
2. Warnings: low HP (<=25%), low fuel (<=15%), FuelEmpty, ShieldBroken - short-lived flashing glass banners near the top; a dead ship (status().alive == false) shows a red vignette and "SHIP DESTROYED" (a later respawn module adds the countdown). No audio in this task.
3. Bars get colour by level (e.g. HP green->amber->red); all sizes scale sanely from 1280x720 up to 4K and down to 1024x600 without overlapping the hint line.
4. Pure layout/threshold logic (percent -> colour, warning conditions, banner timers) goes in a header with no SDL/GL (hud_logic.h) with unit tests in package/tests/test_hud_logic.cpp.
5. Fake ship for development: add a module package/modules/ship/fake_ship/ that is INERT unless launched with --fake-ship=hp:35,shield:120,fuel:10[,dead] (parse those keys; only then provide ship::IShip via services); it must never provide IShip when the flag is absent, so it can coexist with the real ship_core. Use it plus --screenshot to verify your HUD visually in states: full, low HP, shield broken, no fuel, dead. Include screenshots you inspected in your report (paths).
6. Docs: docs/HUD.md.

CONSTRAINTS: HUD reads state only through ship::IShip and events; if IShip is missing (ship module off) the HUD draws nothing and logs one warning. UIHandler additions must not change existing signatures. Do not edit package/modules/gameplay/flight or ship_core (owned by the other worker); on your branch both HUDs will draw until the merge - that is expected.

OWNERSHIP: package/modules/ui/ship_hud/**, package/modules/ship/fake_ship/**, additive edits in package/modules/core/ui_handler/**, package/tests/test_hud_logic.cpp, docs/HUD.md.

OBSERVABLE ACCEPTANCE: smoke.sh passes; unit tests for the logic; screenshots of each fake-ship state inspected and described in the report; module inert with a missing/absent IShip (verify --disable=ship/fake_ship run does not crash and logs one warning).
