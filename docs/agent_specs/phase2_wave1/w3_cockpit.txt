TASK 2.5 - ship/cockpit (Phase 2, Wave 1). You own the cockpit model.

READ FIRST: docs/WORKFLOW.md (rules, definition of done, report format, FIX LIST protocol), docs/MODULES.md, docs/ROADMAP.md (your step), and the docs of the services you use. Run package/tools/link_assets.sh once in your worktree. The old game (read-only reference) is at /home/contolis/Documents/Space-Game-V2/space-game/space-game. Do NOT edit docs/MODULES.md or docs/ROADMAP.md (the coordinator does); put your documentation in your own docs file. DO NOT COMMIT, amend, merge, rebase or push: a git hook refuses commits in agent worktrees. Leave every change uncommitted in your worktree; the coordinator reviews the diff and commits it after approval. Finish with package/tools/smoke.sh passing on the uncommitted tree, then send worker_done with: 3-sentence summary (built / verified and how / could NOT verify), files added/changed, any edits to shared files or the ship contract, and a "manual test checklist" for the human player.

TARGET: new module package/modules/ship/cockpit/ (module name "ship/cockpit"), and the OBJ/MTL loading in package/modules/core/import_handler. Assets are in assets/models/ (run link_assets.sh): look at assets/models/ship/ and assets/models/README.md. Read the old code for reference: include/ship/cockpit.h, include/ship/ship_loader.h, include/core/obj_loader.h (MTL material colors, auto normals, bounds).

CHANGE:
1. Split OBJ+MTL parsing out of import_handler.cpp into a pure parser (obj_parser.h/.cpp: no SDL, no GL) that returns positions, normals and per-triangle material colors (parse Kd from the .mtl named by mtllib; missing MTL/material -> a neutral grey); extend core::Mesh ADDITIVELY (per-vertex colors) without breaking existing users; import_handler uses the parser. Add package/tests/test_obj_parser.cpp using temp files: faces with v//vn, v/vt/vn, quads and n-gons (fan-triangulated), negative indices, missing normals (computed), MTL colors, bad indices, empty file. Add the parser .cpp to TEST_SRCS in the Makefile.
2. The cockpit module loads the model(s) in assets/models/ship through core::ImportHandler (the ship model the old game used as its cockpit; see ship_loader.h for which file and how it was oriented/scaled/offset) and draws it as a core::RenderEngine pass in VIEW space (attached to the camera, so it does not move with the world), lit by a fixed directional light plus ambient (tunables cockpit.ambient, cockpit.light_dir; there is no star system yet, the world module will later supply the sun direction through a service, so isolate the light source in one function). Clear/reset the depth buffer for this pass so the cockpit never clips into world geometry, and restore GL state afterwards (matrices, lighting, depth) so other passes are unaffected.
3. Only draw when core::ICamera says the camera is in Cockpit mode (ICamera::showsShip() == false); no cockpit in chase view. Tunable cockpit.enabled (default true). If the model fails to load, log one warning and draw nothing; never crash.
4. Verify visually with --screenshot in cockpit mode and check the model is oriented right (forward through the canopy), correctly scaled, not upside-down, and not covering the whole view. Include the screenshot paths you inspected in your report. The window is 1280x720 by default.
5. SHIP SELECTION (port the idea of include/ship/ship_loader.h, but as a module): scan assets/models/ship/*/ship.json with engine/json.h (fields: name, model, showDefaultUI, screens[{tag, content}]). Use the ship named by tunable cockpit.ship (default "ShipV2"); if it is missing fall back to the first ship found; log which one was chosen. Both assets/models/ship/shipv1 (plain, no screens) and shipv2 (has @HUD screens) already exist. Remember showDefaultUI from ship.json and expose it as ICockpitScreens::showsDefaultUI() (see item 7) so a HUD module can later hide its flat 2D overlay when the model draws its own screens; do NOT edit ui/ship_hud (another worker owns it).
6. CUSTOM '@' MATERIALS (a system from the old game; the user's rule: "anything labelled with @ has custom texturing"). Read include/core/obj_loader.h (struct HudScreen and the '@HUD-' handling around lines 140-230), include/ui/hud/cockpit_screens.h and assets/models/ship/shipv2/ShipV2.mtl. Convention: ANY material whose name starts with '@' is NOT ordinary shaded geometry. Its faces are extracted as a tagged quad and handed to custom rendering. Tag format @GROUP-NAME: GROUP is the text before the first '-' (e.g. HUD), NAME the rest (INFO, RADAR, SYSTEMS). Implement it GENERICALLY in the pure parser (not just HUD): the parse result gets a vector of TaggedQuad {group, name, corners[4], normal, centre, width, height} and those faces are EXCLUDED from the ordinary triangle list (do not draw them as coloured mesh). Non-quad tagged faces: use the first four vertices or fan-triangulate and report a warning; never crash. Unit tests (tempfile OBJ+MTL modelled on ShipV2): one @HUD-INFO quad plus ordinary faces gives 1 tagged quad with correct centre, normal and size and the ordinary triangles unaffected; several tags; a tag with no dash; '@' in the middle of a name is NOT a tag; missing MTL; a tagged material with a triangle.
7. SCREEN CONTENT SERVICE + BUILT-IN HUD SCREENS. In the cockpit module add a small interface + implementation, e.g. package/modules/ship/cockpit/cockpit_screens_api.h: ICockpitScreens { registerRenderer(group, fn(const ScreenContext&)), removeRenderer(group), showsDefaultUI() } provided with eng.services.provide<ICockpitScreens>(this), where ScreenContext carries the TaggedQuad (its GL space is view space, drawn in the same pass right after the model). This lets OTHER modules supply content for a group later without editing the cockpit module. Ship the built-in 'HUD' group renderer in this module for the three screens named in ship.json: content FLIGHT_DATA (speed, ship stats bars: HP/shield/fuel from ship::IShip), SHIP_SYSTEMS (status lines: hull, shield installed/enabled, warp fuel, alive/warping), PROXIMITY_RADAR (radar frame, ship-centred rings and a sweep; there is no world data yet, so show the frame with the text NO CONTACTS; a later phase feeds it). Draw a dark screen backing first (look at the colours in cockpit_screens.h). Content comes ONLY through ship::IShip (package/modules/ship/ship_core/ship_api.h); if IShip is absent draw NO DATA on the screens and log one warning. TEXT on the quads: do not edit core/ui_handler (another worker owns additive edits there). A small self-contained vector stroke font in the cockpit module (the old game drew text as quad strips) is the expected route; unit-test any pure glyph/layout logic. Tunables for anything a player might tweak (e.g. cockpit.screen_brightness).
8. Because ship_core is being written in parallel and will not exist in your worktree, verify with a tiny provider INSIDE YOUR OWN module folder (e.g. package/modules/ship/cockpit/dev_ship.cpp, module name "ship/cockpit_dev_ship"), inert unless the flag --cockpit-dev-ship=hp:35,shield:120,fuel:10 is passed (only then provide ship::IShip). Do NOT create package/modules/ship/fake_ship (the HUD worker owns that name). Include screenshots of the three screens in normal and low-HP states in your report.
9. Docs: docs/COCKPIT.md (module, the '@' convention with the tag table, ICockpitScreens usage for other modules, ship.json fields, tunables).

CONSTRAINTS: Do not edit package/modules/gameplay/flight or ship_core (another worker owns them), package/modules/ui/** or core/ui_handler (the HUD worker owns those), or package/modules/ship/fake_ship. Keep ImportHandler's public API source compatible (registerLoader<T>, load<T>). Do not add third-party libraries. Keep drawing fixed-function OpenGL 2.1 like the rest of the project.

OWNERSHIP: package/modules/ship/cockpit/** (including dev_ship.cpp, the stroke font and cockpit_screens_api.h), package/modules/core/import_handler/**, package/tests/test_obj_parser.cpp, Makefile TEST_SRCS line, docs/COCKPIT.md.

OBSERVABLE ACCEPTANCE: smoke.sh passes; parser unit tests pass under make test-san; screenshot(s) of the cockpit in cockpit view and confirmation of absence in chase view (V key or settings camera.mode=1 via --settings=<file>); module survives a missing model file; @HUD tagged quads are NOT drawn as ordinary mesh and show live content from IShip (dev provider) on all three ShipV2 screens; the tagged-quad parser tests pass; a ship folder without screens (shipv1) works.
