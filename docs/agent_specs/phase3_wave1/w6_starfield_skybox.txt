TASK 3.1 - world/starfield + world/skybox (Phase 3, first wave). You are the ship_core/warp worker: you know ship_core.cpp (it currently draws the demo starfield) and the IShip contract. Now move the stars out into the world and add a real skybox, built for the user's LOW-END target laptop.

RULES (same as always): read docs/WORKFLOW.md and docs/VISION.md (section "Target hardware": i5 M 560 2c/4t, Intel Ironlake integrated GPU with shared memory, 7.6 GB RAM, OpenGL 2.1 only, 1366x768 and 1280x1024, generic x86-64 builds); edit ONLY the files named under OWNERSHIP; DO NOT COMMIT (a hook blocks it), leave everything uncommitted; run package/tools/smoke.sh at the end; report per item ("N: done | needs decision (what)") plus a 3-sentence summary (built / verified how / could NOT verify) and a manual test checklist. Your worktree was just fast-forwarded to main. The old game (read-only reference) is at /home/contolis/Documents/Space-Game-V2/space-game/space-game: read include/world/skybox.h, include/world/starfield.h, and the per-set JSON files next to the face images (assets/skybox/bkg/<color>/<set>/). Port the BEHAVIOUR, not the structure. The assets folder is git-ignored; run package/tools/link_assets.sh in your worktree.

TARGET: two new modules, package/modules/world/starfield/ (name "world/starfield") and package/modules/world/skybox/ (name "world/skybox"), both drawing through core::RenderEngine passes.

CHANGE:
1. STARFIELD: move the star generation and drawing out of ship_core.cpp (remove ONLY the stars: keep the demo rocks, their physics bodies and everything else). Behaviour to keep: points on a far sphere that follow the camera (no parallax), deterministic. Tunables (eng.config.get, documented): starfield.count (default 2500), starfield.seed (1234), starfield.point_size (1.5), starfield.brightness (1.0), starfield.enabled (true). Pass order 0 (after the skybox, before world geometry).
2. WARP STREAKS (the old game stretched stars while warping): when ship::IShip::status().warping is true, draw each star as a short line streaking AWAY from the direction of travel (opposite ship::IShip::velocity() normalised), length proportional to speed / 2000 (tunable starfield.warp_streak_length, default 300 units at full warp speed; 0 disables). Look the ship up with eng.services.get<ship::IShip>() each frame; no ship = plain points. Keep it cheap (GL_LINES or points, no per-frame allocations).
3. SKYBOX: scan assets/skybox/bkg/*/*/ for sets of six face images (front, back, left, right, top, bot .png; read how the old skybox.h names them) plus the optional per-set JSON with UV orientation flags (flipU / flipV / rotate 0|90|180|270: read the real files and parse them with engine/json.h). Choose the set from tunable skybox.set ("color/setname", default "dark/set1"); if it is missing fall back to the first set found; log which set. Tunable skybox.enabled (default true). Draw as six textured quads around the camera (view space translation removed, depth writes off, depth test off, lighting off, texturing state restored afterwards), pass order -100 so it is behind everything.
4. LOW-END TEXTURE BUDGET (important): the face PNGs are 4-8 MB each (probably 4096-8192 pixels wide) and graphics memory on the target is shared. Load faces ONE AT A TIME with SDL_image, DOWNSCALE each to at most skybox.max_size pixels on its longest side (tunable, default 1024; use SDL_BlitScaled or an area-average) BEFORE uploading, free the big surface immediately, upload with GL_LINEAR filtering and clamp-to-edge. Never upload a texture larger than GL_MAX_TEXTURE_SIZE. Log one line: set, face size after capping, total texture bytes, and how many ms loading took.
5. DOWNSCALE CACHE (startup time on a slow CPU): after downscaling, save each face to cache/skybox/<color>_<set>_<maxsize>/<face>.png (IMG_SavePNG; create folders; cache/ is git-ignored, the coordinator adds that line); on the next start load the small cached file instead of decoding the huge original, if the cache file exists and is newer than the source. A missing/corrupt cache file falls back to the original. The log line from item 4 must say whether the cache was used.
6. PURE LOGIC in headers with NO SDL/GL/engine types, with unit tests (tolerances, never exact float equality): capped size keeping aspect ratio (e.g. 8192x4096 with max 1024 -> 1024x512; smaller than max stays unchanged; max 0/negative is treated as 1), face-name mapping, the per-face UV transform (all flip/rotate combinations map the four corners as in the old skybox.h), set choice (exact name, fallback to first, empty list), the warp streak length (0 when not warping, proportional to speed, capped), cache path building and 'is cache fresh'. Put them in package/tests/test_world.cpp. Add any new .cpp that the tests need to the Makefile TEST_SRCS line (header-only preferred so no Makefile change is needed).
7. Verify with the benchmark and screenshots (see ACCEPTANCE). Docs: docs/WORLD.md (starfield, skybox, tunables, the texture budget and cache, how to add a skybox set).

CONSTRAINTS: no globals, no printf (LOG_*), OpenGL 2.1 fixed function only, generic build (no -march). All GL state you change must be restored (glPushAttrib/glPopAttrib or explicit). If a skybox image fails to load, log ONE warning and draw no skybox; never crash. Do not edit other modules except: ship_core.cpp (remove the star code only), and you may ADD (not change) small things to core/import_handler if you need them (say so in the report).

OWNERSHIP: package/modules/world/starfield/**, package/modules/world/skybox/**, package/modules/ship/ship_core/ship_core.cpp (remove star code only), package/tests/test_world.cpp, Makefile TEST_SRCS line only if unavoidable, docs/WORLD.md.

OBSERVABLE ACCEPTANCE: (a) smoke.sh passes; new tests pass under make test-san. (b) Real-game screenshots (SDL_AUDIODRIVER=dummy ./space_game_v2 --frames=60 --settings=logs/s_settings.json --screenshot=logs/s1.bmp --screenshot-frame=50, settings file {}): default (skybox on, stars on), skybox.enabled=false, and skybox.max_size=64 (visibly blurry, proving the downscale). Inspect and describe each. (c) Warp streaks: use the save + --auto-warp method from docs/WARP.md and take a screenshot mid-warp; describe the streaks. (d) Report the log lines for cold load (no cache) and warm load (cache) with their ms, and run `./space_game_v2 --benchmark=5 --no-vsync` with the skybox on and off and report the fps for both (this dev machine is very fast, so what matters is the difference). (e) Delete logs/s_* and any scratch files; do not leave cache/ files that are not gitignored.
