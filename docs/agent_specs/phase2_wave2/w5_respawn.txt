TASK 2.4 - ship/respawn + HUD countdown (Phase 2, Wave 2). You are the ui/ship_hud worker: you know hud_logic.h, ship_hud.cpp and fake_ship. Now build the respawn module and show its countdown on the death screen.

RULES (same as always): read docs/WORKFLOW.md; edit ONLY the files named under OWNERSHIP; DO NOT COMMIT (a hook blocks it), leave everything uncommitted; run package/tools/smoke.sh at the end; report per item ("N: done | needs decision (what)") plus a 3-sentence summary (built / verified how / could NOT verify) and a manual test checklist. Your worktree was just fast-forwarded to main. The old game (read-only reference) is at /home/contolis/Documents/Space-Game-V2/space-game/space-game: see include/ship/ship.h respawn() (~line 292) and how main handled the 3 s RESPAWN_TIME + red overlay. Port the BEHAVIOUR.

TARGET: new module package/modules/ship/respawn/ (module name "ship/respawn") plus a small addition to ui/ship_hud. The contract header package/modules/ship/respawn/respawn_api.h (ship::IRespawn: counting(), secondsLeft()) is ALREADY committed on main by the coordinator: implement it, provide it with eng.services.provide<ship::IRespawn>(this). The ship itself is ship::IShip (ship_api.h): respawn() resets it at the spawn point with full hull and fuel (the shield is not restored) and emits Respawned.

CHANGE:
1. ship/respawn watches ship::IShip::status().alive every frame in onUpdate (poll, so a SAVE that was loaded while dead also counts down). When the ship is dead and no countdown is running, start one of respawn.seconds (tunable, default 3.0, minimum 0.5). Advance it by the frame dt only while eng.paused() is false. At 0 call ship->respawn() once and go idle. Tunable respawn.enabled (default true): false = never auto-respawn (hardcore mode; the destroyed screen stays). optionalDependencies on ship/ship_core and ship/fake_ship so the service exists at init; if there is no IShip do nothing and log one warning.
2. Put the state machine in a pure header (respawn_rules.h, no SDL/GL/engine types): idle -> counting on death, tick(dt, paused), finished flag, reset when alive again, enabled=false never counts. Tests in package/tests/test_respawn.cpp: dead starts the countdown; not dead stays idle; paused time does not advance; exactly one respawn trigger; a ship that comes back alive during the countdown cancels it; minimum seconds clamp; enabled=false; a countdown restarting for a second death. Tolerances for floats.
3. ui/ship_hud: under SHIP DESTROYED show "RESPAWNING IN N" (N = ceil of secondsLeft, counting 3,2,1) when ship::IRespawn is present and counting; nothing extra if the service is absent or disabled. Put the text formatting in hud_logic.h as a pure function with tests in test_hud_logic.cpp (3.0 -> 3, 2.01 -> 3, 2.0 -> 2, 0.2 -> 1, 0 -> no text). Add ship/respawn to the HUD's optionalDependencies.
4. Docs: docs/RESPAWN.md (behaviour, tunables, service) and a short addition to docs/HUD.md. Do not edit docs/MODULES.md or docs/ROADMAP.md.

CONSTRAINTS: no globals, no printf (LOG_*), interfaces + events only. Do not edit ship_core or ship_api.h (tell me if you need something). Death by the sun etc. also goes through Died so it works the same.

OWNERSHIP: package/modules/ship/respawn/** (respawn_api.h may only get additive doc comments), package/modules/ui/ship_hud/**, package/tests/test_respawn.cpp, package/tests/test_hud_logic.cpp, docs/RESPAWN.md, docs/HUD.md.

OBSERVABLE ACCEPTANCE: (a) smoke.sh passes; new tests pass under make test-san. (b) In the REAL game (no fake ship), create logs/r_saves/save_a.json = {"format":1,"time":1789000000,"modules":{"gameplay/flight":{"pos":[-23.2,-161.3,-81.6],"vel":[0,0,-100],"fwd":[0,0,-1],"up":[0,1,0],"hp":8,"maxHp":100,"shield":0,"shieldInstalled":false,"shieldEnabled":false,"warpFuel":12,"alive":true}}} (a lethal hit about 0.8 s after loading) and run SDL_AUDIODRIVER=dummy ./space_game_v2 --frames=400 --paused --ui-click="640,388,10;640,357,20" --saves=logs/r_saves --settings=logs/r_settings.json --screenshot=logs/r1.bmp --screenshot-frame=NNN with a settings file {}: choose NNN (about 100) so the screenshot shows SHIP DESTROYED with RESPAWNING IN 2 or 3; a second run with --screenshot-frame about 280 must show the alive HUD with the cockpit screens at HP 100 and STATUS ALIVE at the spawn point. Inspect and describe both screenshots. Also run with config/game.json {"respawn":{"enabled":false}} (delete it afterwards) and confirm the destroyed screen stays with no countdown. Delete logs/r_* and scratch files afterwards.
