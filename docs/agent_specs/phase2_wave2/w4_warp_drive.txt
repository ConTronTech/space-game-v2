TASK 2.2 - ship/warp_drive (Phase 2, Wave 2). You are the ship_core worker: you know ship_core.cpp and the IShip contract. Now build the warp drive as its own module.

RULES (same as always): read docs/WORKFLOW.md; edit ONLY the files named under OWNERSHIP; DO NOT COMMIT (a hook blocks it), leave everything uncommitted; run package/tools/smoke.sh at the end; report per item ("N: done | needs decision (what)") plus a 3-sentence summary (built / verified how / could NOT verify) and a manual test checklist. Your worktree was just fast-forwarded to main. The old game (read-only reference) is at /home/contolis/Documents/Space-Game-V2/space-game/space-game: read include/ship/ship.h around toggleWarp (~line 168-200) and the warp part of update (~line 468-482) for the behaviour to port. Port the BEHAVIOUR, not the structure.

TARGET: new module package/modules/ship/warp_drive/ (module name "ship/warp_drive"), which drives the ship only through ship::IShip (package/modules/ship/ship_core/ship_api.h). The coordinator already added: IShip::setWarping(bool) (non-pure, default ignored) and the input action "toggle_warp" (key Z) in config/input/default.json. ship_core must OVERRIDE setWarping so ShipStatus::warping follows (the cockpit's SYSTEMS screen already prints DRIVE ENGAGED / DRIVE STANDBY from it).

CHANGE:
1. Toggle: on input action "toggle_warp" pressed (IInput::pressed, called from onUpdate, NEVER onFixedUpdate) engage if the ship is alive, not already engaged and has at least warp.min_fuel fuel; otherwise disengage. Log one LOG_I line per change.
2. While engaged (onFixedUpdate, so it freezes while paused): consume fuel warp.fuel_drain per second through IShip::consumeWarpFuel(dt * drain) - when it returns false (tank cannot cover it) disengage. Accelerate along IShip::forward(): v += forward * (warp.speed * warp.accel_factor * dt), then clamp the speed to warp.speed; apply with IShip::setVelocity. No passive fuel regeneration (like the old game).
3. Auto-disengage when: fuel runs out, the ship dies (status().alive false), or the module shuts down. Always call setWarping(false) on disengage.
4. On disengage, if warp.exit_speed > 0 and the speed is above it, scale the velocity down to warp.exit_speed keeping its direction (the old game clamped to 200 m/s when leaving warp); warp.exit_speed = 0 keeps the momentum (pure Newtonian). Default 200. Say in docs that this is a design choice the user may want to change.
5. Tunables (eng.config.get, documented): warp.speed 2000, warp.accel_factor 2.0, warp.fuel_drain 3.33 (fuel per second, about 30 s on a full tank), warp.min_fuel 1.0, warp.exit_speed 200.
6. Pure logic in a header with NO SDL/GL/engine types (warp_rules.h): the state machine and per-step maths (canEngage, step(state, dt, params) returning new velocity, fuel to burn, and whether to disengage). Unit tests in package/tests/test_warp.cpp: refused when dead / no fuel / already engaged; acceleration reaches but never exceeds the cap; fuel drain matches drain*time; auto-disengage exactly when fuel cannot cover a step; exit-speed clamp keeps direction; exit_speed 0 keeps momentum; dt=0 changes nothing. Compare floats with tolerances (1e-4), never exact equality.
7. Dev aid so the real game can be verified without a keyboard: flag --auto-warp=FRAME makes the module behave as if toggle_warp was pressed on that engine frame (and --auto-warp-off=FRAME likewise disengages). Only active when the flag is given.
8. Docs: docs/WARP.md (behaviour, tunables, the exit-speed decision, dev flags, how other modules use it). Do not edit docs/MODULES.md or docs/ROADMAP.md.

CONSTRAINTS: no globals, no printf (LOG_*), interfaces + events only. In ship_core.cpp you may ONLY add the setWarping override (and keep status_.warping in the mirror); do not change flight behaviour. Do not edit ship_api.h (tell me if you need a change). Collisions at warp speed are handled by the existing physics (swept spheres) and damage is speed-scaled like the old game (a crash at 2000 m/s is fatal); do not special-case that.

OWNERSHIP: package/modules/ship/warp_drive/**, package/modules/ship/ship_core/ship_core.cpp (setWarping only), package/tests/test_warp.cpp, docs/WARP.md.

OBSERVABLE ACCEPTANCE: (a) smoke.sh passes; the new tests pass under make test-san. (b) In the REAL game, no fake ship: create logs/w_saves/save_a.json = {"format":1,"time":1789000000,"modules":{"gameplay/flight":{"pos":[5000,5000,5000],"vel":[0,0,0],"fwd":[0,0,-1],"up":[0,1,0],"hp":100,"maxHp":100,"shield":0,"shieldInstalled":false,"shieldEnabled":false,"warpFuel":100,"alive":true}}} (far from every rock) and run SDL_AUDIODRIVER=dummy ./space_game_v2 --frames=240 --paused --ui-click="640,388,10;640,357,20" --saves=logs/w_saves --settings=logs/w_settings.json --auto-warp=40 --screenshot=logs/w1.bmp --screenshot-frame=100 (settings file {}). The screenshot must show the cockpit SYSTEMS screen with DRIVE ENGAGED, a high SPD and WARP FUEL below 100. A second screenshot at frame 230 (the tank runs dry around 30 s = 1800 frames, so use a small fuel value like "warpFuel":3 in a second save to make it happen in about 1 s of warp) must show the drive disengaged, speed cut to the exit speed, fuel 0 and the LOW/EMPTY fuel banner. Inspect and describe each screenshot. Delete logs/w_* and any scratch files afterwards.
