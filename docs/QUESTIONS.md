# Open questions for the user (saved while working; answer when back)

Newest first. Each has a default that is being used until answered.

1. **Warp key and exit speed** - warp is `Z`; leaving warp cuts speed to 200 m/s (`warp.exit_speed`, 0 = keep momentum = pure Newtonian). Keep the cut? (default: keep)
2. **Respawn** - 3 s countdown at the spawn point after death (`respawn.seconds`). OK? (default: 3 s)
3. **Orbit lock** - `O`, releases on thrust/brake/damage/warp. Should it also hold while the ship rotates freely? (default: yes, rotation allowed) Should thrust really release it, or should it need a second `O`? (default: thrust releases)
4. **Demo rocks** are off by default now (real asteroids replace them). Want the old 150 rocks near the spawn back as a training field? (default: off)
5. **Default belt density** is sparse (about 9 rocks within 1,500 units at the densest spot). Denser for gameplay? (default: `asteroids.belt_asteroids` 1500) **Revisit now that belt count is 1-5 per system (2026-09-22): more belts existing doesn't by itself fix a thin one - worth a fresh look once this is playtested.**
6. **Stations** - the default seed puts both stations on Planet 2 as surface stations. Want an orbital one guaranteed? (default: seed decides; `stations.seed_offset` re-rolls)
7. **Dock key label** - ANSWERED + IMPLEMENTED (2026-09-22): `IInput`/`InputMethod` now expose `primaryBindingLabel`/`bindingLabel`; the HUD dock prompt reads the real binding, "G" is only the fallback. Follow-up questions from the build: (a) joystick buttons are labelled counting from 1 (button index + 1) to match controller markings, while config files and `--joystick-monitor` count from 0 - the numbers won't match if you cross-reference them; (b) weapon-select labels ("1"/"2") in `ship_hud` are still hardcoded and could use the same lookup; (c) stale "DOCK [G]" comments remain in `docking_api.h` and `hud_screens.cpp` (comments only, not shown to the player); (d) if a profile binds both a key AND a joystick button to the same action, the keyboard label always wins (device priority order: keyboard, mouse, joystick, then others alphabetically) - fine unless you want it to prefer whichever device was used most recently.
8. **Laptop target** - is 60 fps average / 30 fps floor at 1280x720 on Low still the goal, or is 30-40 fps acceptable for the laptop? (default: keep chasing 60 via render scale)
9. **Old game radar** - the reference tree's radar had no height stems; the stems were built from the user's description. Anything else you remember about it?
10. **Laptop folder** - `~/Documents/Space-Game-V2/space-game-v2` on the laptop holds the built game (no game.json/settings copied). Remove it when we are done?
11. **Crafting location** - ANSWERED (user, 2026-09-21): crafting requires a docked station (a workbench), as a SETTING either way. `crafting.require_dock` default changed to `true` (source edit made while testing was paused; needs a smoke run on resume before it counts as verified). The CRAFTING tab shows "Dock at a station to craft" and refuses everywhere else when true.
12. **Game menu and the ship** - the I menu does not pause the game (the ship keeps flying and can be hit; `menu.pause_game true` pauses it). Keep it live? (default: yes, live) **Tab-key part ANSWERED + IMPLEMENTED (2026-09-22): the menu now uses dedicated `[`/`]` (`menu_tab_prev`/`menu_tab_next`) instead of `ui_left`/`ui_right`, so switching tabs no longer also strafes the ship.**
13. **Ore scanner and missiles** - crafting them works but USING them is refused ("not available yet") because nothing implements ore labels or missiles yet. Should the ore scanner be a permanent flag saved with the cargo (the inventory module would need a small addition)? (default: refused until the feature exists)
14. **Real gravity** - should planets/moons/the sun exert real gravity on the ship (Newtonian orbits you must actually fly, the lock becoming an assist)? This would change flight a lot (Space-Engineers-like planetary gravity vs pure space). Today the ship is not pulled by bodies: the orbit guide only shows the orbit the lock would give. (default: no gravity) **Research note (2026-09-21, docs/PHYSICS_RESEARCH.md): recommend single-dominant-body (sphere-of-influence) gravity on the ship, like Kerbal Space Program's patched-conic approach - planets/moons/sun keep their existing zero-cost analytic orbits, orbit_lock becomes a real assist. Full N-body is not recommended (too expensive for the laptop, not needed for this game). Still waiting on your decision before any implementation.** **ANSWERED + IMPLEMENTED (3.5e, 2026-09-21): single-body SOI gravity, ship/gravity, docs/GRAVITY.md. Default ON (`ship.gravity_enabled` true, user decision); false restores the old no-gravity flight.**
15. **Save system, from the 2026-09-22 overnight autosave/active-slot rework:** (a) after loading the autosave slot, "Save" creates a NEW slot rather than writing back into `autosave` - intended, so a manual save never gets silently overwritten by the next timed autosave, but flagging it since it might feel surprising; (b) `lastSaveTime()` counts autosaves toward "when did we last save" - should an "unsaved progress" warning (if one gets built later) count them too, or only manual saves; (c) the pause menu's Load list still only shows the newest 6 slots, and the autosave now takes one of those slots - fine, or should autosave be shown separately from the manual-save list?
16. **Asteroid rings/belts, from the 2026-09-22 overnight rework:** (a) tuning numbers (ring chance 40%, 300 asteroids per ring, 2 moon clusters, belt count 1-5) are first-guess defaults, worth adjusting after actually flying around a few seeds; (b) total asteroid count per system can now reach roughly 2,000-9,000 (up from ~1,740 before) - draw/physics cost is already distance-limited, but system-generation TIME on the laptop is worth checking; (c) removing the old `asteroids.cluster_count` config key means any saved reference to a specific asteroid by index in an existing save could point at a different rock after this change (asteroids aren't saved by content, just regenerated from the seed, so this only matters if you reload an old save and something *remembered* an asteroid index - worth a quick check, not a known bug).
